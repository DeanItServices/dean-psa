---
phase: 01-foundation-platform-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.js
  - components.json
  - .eslintrc.json
  - .gitignore
  - README.md
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
files_forbidden:
  - docker-compose.yml
  - Dockerfile
  - .env.example
  - prisma/
  - src/lib/db.ts
  - src/lib/auth.ts
  - src/lib/permissions.ts
  - src/middleware.ts
sequential_files:
  - package.json
expected_artifacts:
  - path: "package.json"
    provides: "Next.js + TypeScript project manifest with Tailwind and shadcn/ui dependencies"
    required: true
  - path: "src/app/layout.tsx"
    provides: "Root layout applying Tailwind global styles"
    required: true
  - path: "components.json"
    provides: "shadcn/ui configuration pointing at src/components/ui"
    required: true
autonomous: true
agents: ["engineering-frontend-developer"]
requirements: ["Role-based access control (technician / dispatcher / sales / finance / admin roles)"]
user_setup: []
verification_commands:
  - "test -f package.json && grep -q '\"next\"' package.json"
  - "test -f tailwind.config.ts || grep -q '@import \"tailwindcss\"' src/app/globals.css"
  - "test -f components.json"
  - "test -f src/app/layout.tsx"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "The project builds with `npm run build` using Next.js App Router and TypeScript with zero type errors."
    - "Tailwind CSS is wired through src/app/globals.css and applied in the root layout."
    - "shadcn/ui is initialized with at least four base components generated into src/components/ui/."
    - "No backend, database, auth, or Docker files exist yet -- this plan is frontend tooling only."
  artifacts:
    - path: "package.json"
      provides: "Dependency manifest"
      min_lines: 15
      contains: "next"
    - path: "src/app/globals.css"
      provides: "Tailwind directives (v3 @tailwind rules or v4 @import)"
      min_lines: 1
      contains: "tailwind"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Scaffold a fresh Next.js (App Router) + TypeScript project with Tailwind CSS and shadcn/ui configured, producing the frontend tooling foundation that Plans 01-02 through 01-04 build on.

Purpose: Every later phase (CRM, ticketing, billing, reporting) renders through this Next.js app and its shadcn/ui component system. This plan establishes that skeleton with zero backend/auth/database coupling.
Output: A running `npm run dev` Next.js app with Tailwind + shadcn/ui wired, and a placeholder home page.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-platform-setup/01-CONTEXT.md

This is a fresh, empty repository. No source files exist yet except `.planning/`.

Relevant decisions from 01-CONTEXT.md:
- Framework: Next.js (App Router) + TypeScript
- Styling: Tailwind CSS + shadcn/ui
- Directory structure: standard Next.js App Router conventions with light feature-folder grouping under `src/` (`app/`, `lib/`, `components/`)
- This plan does NOT touch Docker, Prisma, auth, or database -- that is Plan 01-02 and 01-03's scope

**Execution environment**: This project runs on Windows with Git Bash as the shell of record (confirmed: the Bash tool in this environment invokes Git Bash, not PowerShell or cmd.exe). All verification commands in this plan (`test -f`, `grep -q`, etc.) are POSIX shell syntax and MUST be run through Git Bash. If the execution harness offers both a Bash tool and a PowerShell tool, always use the Bash tool for this plan's verification commands -- do not translate them to PowerShell syntax.

**Wave 1 ordering note**: This plan (01-01) and Plan 01-02 both run in Wave 1 and both touch `package.json`. Plan 01-01 is the plan that CREATES `package.json` from scratch via `create-next-app`; Plan 01-02 only APPENDS to it afterward. Because `package.json` is declared in this plan's `sequential_files` (and referenced as a shared file in 01-02), the wave-executor must run 01-01's `package.json`-creating step to completion before 01-02 reads or writes it. If the wave-executor's dispatch model cannot guarantee that ordering within a nominally parallel wave, treat this as a stop condition: do not proceed with 01-02 concurrently with an in-progress 01-01. Complete Task 1 (project scaffold, which creates package.json) fully -- including its verification commands passing -- before any other plan in this wave reads or writes package.json.</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-frontend-developer
Task: Scaffold Next.js + TypeScript + Tailwind + shadcn/ui project structure (no backend/auth/DB work)
Scope:
- Read targets: .planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-foundation-platform-setup/01-CONTEXT.md
- Write targets: package.json, tsconfig.json, next.config.ts, tailwind.config.ts, postcss.config.js, components.json, .eslintrc.json, .gitignore, README.md, src/app/layout.tsx, src/app/globals.css, src/app/page.tsx, src/lib/utils.ts, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx
- Forbidden targets: docker-compose.yml, Dockerfile, .env.example, prisma/ (any files), src/lib/db.ts, src/lib/auth.ts, src/lib/permissions.ts, src/middleware.ts -- these belong to Plans 01-02 and 01-03
Allowed tools/actions:
- Run `npx create-next-app@latest` (non-interactively, with flags for TypeScript, Tailwind, App Router, src directory, ESLint, no import alias prompt -- use `@/*` alias)
- Run `npx shadcn@latest init` and `npx shadcn@latest add button card input label`
- Edit generated config files to match the required structure
- Run `npx tsc --noEmit` and `npm run build` to verify
Forbidden actions:
- Do not modify files outside files_modified.
- Do not add unplanned dependencies beyond Next.js, React, TypeScript, Tailwind, shadcn/ui, and their direct peer dependencies.
- Do not create Prisma schema, Docker files, or auth code in this plan.
- Do not self-defer planned work.
Implementation sequence:
1. Run `npx create-next-app@latest . --typescript --tailwind --app --src-dir --eslint --import-alias "@/*" --use-npm --no-turbopack` in the repository root (use `.` since the directory is empty except for `.planning/`). Before running, check `npx create-next-app@latest --help` output for whether a non-empty-directory override flag exists in the installed version; if `--yes` is not a recognized flag, retry without it (the CLI typically only prompts about non-empty directories when non-hidden files are present, and `.planning/` alone may not trigger that prompt). If `--no-turbopack` is not recognized by the installed version (flag names have changed across releases), retry the command without that flag -- Turbopack vs. Webpack is not a decision this plan depends on, so omit the flag entirely rather than guessing an alternate name. Confirm package.json, tsconfig.json, next.config.ts, and src/app/{layout.tsx,page.tsx,globals.css} were generated. **Tailwind version check**: inspect the generated files to determine whether Tailwind v3 (a `tailwind.config.ts` file exists, `globals.css` contains `@tailwind base/components/utilities` directives) or Tailwind v4 (no `tailwind.config.ts` by default, `globals.css` contains a single `@import "tailwindcss"` line instead) was scaffolded. Record which version was generated in this plan's SUMMARY.md -- downstream verification in this same plan must check for whichever form is actually present, not assume v3.
2. Run `npx shadcn@latest init -d` (defaults: New York style, Zinc base color, CSS variables enabled) to generate components.json and wire src/lib/utils.ts (the `cn()` helper).
3. Run `npx shadcn@latest add button card input label` to generate the four base components into src/components/ui/.
4. Replace the default src/app/page.tsx placeholder content with a minimal placeholder page that imports and renders the shadcn `Card` and `Button` components with the text "MSP PSA -- Foundation" so the scaffold is visibly wired end-to-end (Tailwind classes + shadcn components rendering together). Do not add routing, auth, or navigation logic here -- that is Plan 01-04's scope.
5. Update README.md with a "Getting Started" section: `npm install`, `npm run dev`, note that Docker/database setup is documented separately (added by Plan 01-02).
6. Run `npx tsc --noEmit` to confirm zero type errors, then `npm run build` to confirm the production build succeeds.

> verification: test -f package.json && grep -q '"next"' package.json
> verification: test -f tailwind.config.ts || grep -q '@import "tailwindcss"' src/app/globals.css
> verification: test -f components.json
> verification: test -f src/app/layout.tsx
> verification: grep -q "globals.css" src/app/layout.tsx
> verification: npx tsc --noEmit
  </action>
Required interfaces/content structure:
- package.json must declare "next", "react", "react-dom", "typescript", "tailwindcss" as dependencies/devDependencies
- src/app/layout.tsx must import "./globals.css" and export a default RootLayout component
- src/app/globals.css must contain "@tailwind base", "@tailwind components", "@tailwind utilities" (or the Tailwind v4 equivalent single `@import "tailwindcss"` -- use whichever the installed Tailwind version generates by default)
- components.json must declare aliases pointing at src/components and src/lib/utils
Edge/error cases:
- If `create-next-app` refuses to run in a non-empty directory (e.g. `.planning/` already exists): pass the appropriate non-interactive override flag (`--yes`) rather than deleting `.planning/`. Never delete or modify anything under `.planning/`.
- If shadcn/ui's `init` command prompts for a framework/style choice interactively and cannot be fully suppressed with `-d`, answer with the defaults documented above (New York, Zinc, CSS variables) via any available non-interactive flags; do not leave the command hanging.
- If `npx tsc --noEmit` reports errors from generated boilerplate: fix them directly in the generated files rather than suppressing with `// @ts-ignore`.
Verification criteria:
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- All files in expected_artifacts exist
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `.planning/PROJECT.md` or `.planning/ROADMAP.md` is missing or unreadable.
- `create-next-app` or `shadcn` CLI is unavailable (no npm/npx access) in the execution environment.
- Completing the task would require modifying a file in files_forbidden (e.g., if a generator tries to write docker-compose.yml).
- `npx tsc --noEmit` fails after one focused fix attempt on generated boilerplate.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md and the phase CONTEXT.md at .planning/phases/01-foundation-platform-setup/01-CONTEXT.md.
2. Run `git diff --stat` and inspect `git diff` for every file already changed (if git is initialized; if not, run `ls -la` and compare against files_modified).
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run completed task verification commands before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js + TypeScript project</name>
  <files>package.json, tsconfig.json, next.config.ts, .eslintrc.json, .gitignore, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
Run this command in Git Bash (the shell of record for this project) from the repository root: `npx create-next-app@latest . --typescript --tailwind --app --src-dir --eslint --import-alias "@/*" --use-npm --no-turbopack`. If `--no-turbopack` is rejected as an unrecognized flag by the installed CLI version, retry the exact same command without that flag. If the CLI prompts about the current directory not being empty (because `.planning/` exists), pass whatever non-interactive confirmation flag the installed version's `--help` output documents for this case (commonly `--yes`); if no such flag exists in the installed version, retry the command and answer any interactive prompt affirmatively without deleting or modifying `.planning/`.

Confirm after the command completes that package.json includes "next", "react", "react-dom" dependencies and "typescript", "tailwindcss", "eslint" devDependencies; tsconfig.json has "@/*" path alias mapped to "./src/*"; src/app/layout.tsx exists and imports "./globals.css"; src/app/globals.css contains Tailwind directives (either v3's `@tailwind` rules or v4's `@import "tailwindcss"`). Note in this plan's SUMMARY.md which Tailwind major version was generated.

> verification: test -f package.json && grep -q '"next"' package.json
> verification: test -f tsconfig.json && grep -q '"@/\*"' tsconfig.json
> verification: test -f src/app/globals.css
  </action>
  <verify>
test -f package.json && grep -q '"next"' package.json
test -f tsconfig.json && grep -q '"@/\*"' tsconfig.json
test -f src/app/globals.css
  </verify>
  <done>Next.js + TypeScript + Tailwind project is scaffolded with the App Router and src/ directory structure.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and generate base components</name>
  <files>components.json, src/lib/utils.ts, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx</files>
  <action>
Run `npx shadcn@latest init -d` to initialize shadcn/ui with defaults (New York style, Zinc base color, CSS variables enabled). This generates components.json and src/lib/utils.ts (exporting the `cn()` class-merging helper).

Then run `npx shadcn@latest add button card input label` to generate the four base components into src/components/ui/.

Confirm components.json exists and declares `"utils": "@/lib/utils"` (or equivalent alias); src/lib/utils.ts exports a `cn` function; all four component files exist under src/components/ui/.

> verification: test -f components.json
> verification: test -f src/lib/utils.ts && grep -q "export function cn" src/lib/utils.ts
> verification: test -f src/components/ui/button.tsx
> verification: test -f src/components/ui/card.tsx
  </action>
  <verify>
test -f components.json
test -f src/lib/utils.ts && grep -q "export function cn" src/lib/utils.ts
test -f src/components/ui/button.tsx
test -f src/components/ui/card.tsx
test -f src/components/ui/input.tsx
test -f src/components/ui/label.tsx
  </verify>
  <done>shadcn/ui is initialized with button, card, input, and label components generated into src/components/ui/.</done>
</task>

<task type="auto">
  <name>Task 3: Wire placeholder home page and verify build</name>
  <files>src/app/page.tsx, README.md</files>
  <action>
Replace the default src/app/page.tsx content generated by create-next-app with a minimal placeholder that imports `Card`, `CardHeader`, `CardTitle`, `CardContent` from "@/components/ui/card" and `Button` from "@/components/ui/button", rendering a centered card with the heading "MSP PSA -- Foundation" and a disabled button labeled "Coming in Phase 2+". This proves Tailwind + shadcn/ui render correctly together. Do not add any routing, links, auth, or layout/navigation logic -- that is out of scope for this plan.

Update README.md with a "Getting Started" section covering: `npm install`, `npm run dev` (app runs on http://localhost:3000), and a note: "Docker Compose and database setup are documented in a future update once Plan 01-02 lands."

Run `npx tsc --noEmit` to confirm zero type errors. Run `npm run build` to confirm the production build succeeds.

> verification: grep -q "MSP PSA" src/app/page.tsx
> verification: npx tsc --noEmit
> verification: npm run build
  </action>
  <verify>
grep -q "MSP PSA" src/app/page.tsx
npx tsc --noEmit
npm run build
  </verify>
  <done>The home page renders a shadcn Card + Button with Tailwind styling, and both the TypeScript compiler and production build succeed with zero errors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` exits 0
- [ ] `npx tsc --noEmit` exits 0
- [ ] package.json, components.json, src/app/layout.tsx all exist, and Tailwind is wired via either tailwind.config.ts (v3) or an @import in globals.css (v4)
- [ ] No files in files_forbidden were created or modified
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Next.js + TypeScript project builds and runs via `npm run dev`
- Tailwind CSS classes render correctly in the browser
- shadcn/ui is initialized with button, card, input, and label components available
- No backend, database, Docker, or auth code exists yet (reserved for Plans 01-02 and 01-03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-platform-setup/01-01-SUMMARY.md` documenting: files created, verification command outputs, any deviations from the plan, and confirmation that files_forbidden were untouched.
</output>
