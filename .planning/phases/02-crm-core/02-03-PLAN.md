---
phase: 02-crm-core
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/lib/validations/contact.ts
  - src/lib/actions/contacts.ts
  - src/components/crm/contacts-tab.tsx
  - src/components/crm/contact-form.tsx
files_forbidden:
  - prisma/schema.prisma
  - prisma/migrations/**
  - src/lib/permissions.ts
  - src/lib/session.ts
  - src/lib/db.ts
  - src/app/(dashboard)/clients/page.tsx
  - src/app/(dashboard)/clients/new/page.tsx
  - src/app/(dashboard)/clients/[companyId]/page.tsx
  - src/components/crm/sites-tab.tsx
  - src/components/crm/site-form.tsx
  - src/components/crm/company-form.tsx
  - src/components/crm/contracts-tab.tsx
  - src/components/crm/assets-tab.tsx
  - src/components/nav/app-sidebar.tsx
  - src/lib/actions/companies.ts
  - src/lib/actions/sites.ts
  - src/lib/actions/contracts.ts
  - src/lib/actions/assets.ts
sequential_files: []
expected_artifacts:
  - path: "src/lib/actions/contacts.ts"
    provides: "Server Actions: createContact, updateContact, deleteContact"
    required: true
  - path: "src/components/crm/contacts-tab.tsx"
    provides: "Real Contacts tab implementation replacing Plan 02-02's placeholder stub"
    required: true
autonomous: false
agents: ["engineering-frontend-developer"]
requirements: ["Contacts per client company"]
user_setup: []
verification_commands:
  - "npx tsc --noEmit"
  - "npm run build"
  - "grep -q 'requireRole' src/lib/actions/contacts.ts && grep -q 'CRM_MANAGE_ROLES' src/lib/actions/contacts.ts"
  - "grep -q 'export function ContactsTab' src/components/crm/contacts-tab.tsx && grep -q 'CrmTabProps' src/components/crm/contacts-tab.tsx"
  - "grep -c 'Coming soon' src/components/crm/contacts-tab.tsx | xargs test 0 -eq"

must_haves:
  truths:
    - "A sales/finance/admin user can add a contact to a company, optionally associating it with one of the company's sites."
    - "The ContactsTab component replaces the 02-02 placeholder while preserving the shared CrmTabProps ({ companyId }: { companyId: string }) export signature, imported from ./tab-types."
    - "createContact/updateContact/deleteContact all call requireRole(CRM_MANAGE_ROLES) before any database write, importing the constant from @/lib/permissions rather than hardcoding a role array."
  artifacts:
    - path: "src/components/crm/contacts-tab.tsx"
      provides: "Real contact list + add-contact form for a given company"
      min_lines: 15
      contains: "ContactsTab"
  key_links:
    - from: "src/components/crm/contacts-tab.tsx"
      to: "src/lib/actions/contacts.ts"
      via: "ContactsTab's form submits through createContact"
      pattern: "createContact"
---

<objective>
Replace Plan 02-02's `ContactsTab` placeholder stub with a real implementation: list a company's contacts and allow creating/editing/deleting them, optionally associated with one of the company's sites.

Purpose: Contacts are one of the 5 Phase 2 CRM entities; this plan delivers full CRUD for the "Contacts per client company" requirement.
Output: `src/lib/actions/contacts.ts`, `src/components/crm/contacts-tab.tsx` (real), `src/components/crm/contact-form.tsx`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-crm-core/02-CONTEXT.md
@.planning/phases/02-crm-core/02-01-SUMMARY.md
@.planning/phases/02-crm-core/02-02-SUMMARY.md

Relevant source files:
@prisma/schema.prisma
@src/lib/permissions.ts
@src/lib/session.ts
@src/lib/db.ts
@src/components/crm/contacts-tab.tsx
@src/lib/actions/companies.ts
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-frontend-developer
Task: Implement full Contact CRUD (create, update, delete) scoped to a Company, with optional Site association, replacing the placeholder `ContactsTab` stub from Plan 02-02.
Scope:
- Read targets: `prisma/schema.prisma` (Contact model shape), `src/lib/permissions.ts` (`CRM_MANAGE_ROLES`), `src/lib/session.ts`, `src/lib/db.ts`, `src/components/crm/tab-types.ts` (`CrmTabProps`), the existing `src/components/crm/contacts-tab.tsx` placeholder (to confirm the exact export signature to preserve), `src/lib/actions/companies.ts` (as the Server Action pattern reference from 02-02)
- Write targets: `src/lib/validations/contact.ts`, `src/lib/actions/contacts.ts`, `src/components/crm/contacts-tab.tsx`, `src/components/crm/contact-form.tsx`
- Forbidden targets: everything in `files_forbidden` above, especially `src/app/(dashboard)/clients/[companyId]/page.tsx` (owned by 02-02) and the other tab stubs/actions owned by 02-04/02-05
Allowed tools/actions:
- Read, Edit, Write on files in `files_modified`
- Bash: `npx tsc --noEmit`, `npm run build`
Forbidden actions:
- Do not modify `prisma/schema.prisma` or `src/lib/permissions.ts`.
- Do not modify `src/app/(dashboard)/clients/[companyId]/page.tsx` -- it already imports `ContactsTab` correctly from 02-02; only the internals of `contacts-tab.tsx` change.
- Do not touch `sites-tab.tsx`, `contracts-tab.tsx`, `assets-tab.tsx`, `app-sidebar.tsx`, `tab-types.ts`, or any other plan's action files.
- Do not hardcode a role-array literal for RBAC checks -- import `CRM_MANAGE_ROLES` from `@/lib/permissions`.
- Do not self-defer planned work.
Implementation sequence:
1. Read `prisma/schema.prisma` to confirm the `Contact` model fields (`companyId`, `siteId` nullable, `name`, `email`, `phone`, `title`). Read the current `src/components/crm/contacts-tab.tsx` to confirm the exact placeholder export signature to preserve, and `src/components/crm/tab-types.ts` to confirm `CrmTabProps`'s shape.
2. Write `src/lib/validations/contact.ts` (zod schema).
3. Write `src/lib/actions/contacts.ts` (Server Actions).
4. Write `src/components/crm/contact-form.tsx` and replace `src/components/crm/contacts-tab.tsx`'s body with the real implementation, preserving the `ContactsTab(props: CrmTabProps)` export (importing `CrmTabProps` from `./tab-types`, not re-declaring it inline).
Required interfaces/content structure:
- `src/lib/validations/contact.ts`: `export const contactSchema = z.object({ name: z.string().min(1), email: z.string().email().optional().or(z.literal("")), phone: z.string().optional(), title: z.string().optional(), siteId: z.string().optional() })`
- `src/lib/actions/contacts.ts`: `"use server"` exporting `createContact(companyId: string, formData: FormData)`, `updateContact(id: string, formData: FormData)`, `deleteContact(id: string)`. Each imports `CRM_MANAGE_ROLES` from `@/lib/permissions` and calls `await requireRole(CRM_MANAGE_ROLES)` (imported from `@/lib/session`) first -- do not hardcode a literal role array. Validate with `contactSchema`. Use `db` from `@/lib/db`.
- `src/components/crm/contacts-tab.tsx`: preserve `export function ContactsTab(props: CrmTabProps)`, importing `CrmTabProps` from `./tab-types`. Internally, fetch the company's contacts and sites (via a Server Component wrapper, or accept them as props if the parent already passes them -- check `02-02-SUMMARY.md` for how `sites-tab.tsx` was implemented and follow the same data-fetching pattern for consistency; if `02-02-SUMMARY.md` is missing, unclear, or contradicts the pattern actually found in `sites-tab.tsx`'s source code, follow `sites-tab.tsx`'s actual code as the authoritative source and proceed -- do not block on documentation ambiguity when the real implementation is readable directly) and render a list plus a `ContactForm` for adding new contacts. If a Site dropdown is needed, use the shadcn `select` component (already installed by 02-02).
- `src/components/crm/contact-form.tsx`: `"use client"` component with fields for name/email/phone/title/site (optional select), calling `createContact` on submit.
Edge/error cases:
- A company with zero sites: the site dropdown in `ContactForm` must render gracefully (e.g. "No site" as the only option), not crash.
- Deleting a contact that doesn't exist: `deleteContact` should not throw an unhandled exception; return an error result if the record isn't found (`db.contact.delete` will throw `P2025` if missing -- catch and return `{ error: "Contact not found" }`).
- Invalid email format: rejected by `contactSchema`, form shows inline validation error, no database write attempted.
Verification criteria:
- `npx tsc --noEmit` and `npm run build` both exit 0
- `ContactsTab` export signature unchanged from the placeholder
- No "Coming soon" placeholder text remains in `contacts-tab.tsx`
- `requireRole` called in every contacts.ts action before any database write
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `src/components/crm/contacts-tab.tsx` does not exist or its placeholder export signature differs from `ContactsTab(props: CrmTabProps)`.
- `prisma/schema.prisma` does not contain a `Contact` model matching 02-01's shape.
- The pattern `02-02-SUMMARY.md` documents for `sites-tab.tsx`'s data-fetching (props vs. internal fetch) is ambiguous or contradictory -- pick the props-based pattern (matching the parent Server Component already fetching `include: { sites: true }`) as the default and note the assumption rather than blocking, UNLESS the parent page's actual code (read directly) shows a different established pattern, in which case follow that.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, `.planning/phases/02-crm-core/02-CONTEXT.md`, `02-02-SUMMARY.md`, and any `02-03-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx tsc --noEmit` and `npm run build` before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Contact validation schema and Server Actions</name>
  <files>src/lib/validations/contact.ts, src/lib/actions/contacts.ts</files>
  <action>
Read `prisma/schema.prisma`'s `Contact` model and `src/lib/actions/companies.ts` (as the established Server Action pattern) first.

Create `src/lib/validations/contact.ts` with the exact `contactSchema` given in this plan's "Required interfaces/content structure".

Create `src/lib/actions/contacts.ts` as a `"use server"` module exporting `createContact(companyId, formData)`, `updateContact(id, formData)`, `deleteContact(id)`, each importing `CRM_MANAGE_ROLES` from `@/lib/permissions` and starting with `await requireRole(CRM_MANAGE_ROLES)`. Use `contactSchema.safeParse` for validation, returning `{ error: string }` on failure. Use `db.contact.create/update/delete`. Catch Prisma's `P2025` (record not found) in `deleteContact`/`updateContact` and return `{ error: "Contact not found" }` instead of throwing.

> verification: test -f src/lib/validations/contact.ts
> verification: test -f src/lib/actions/contacts.ts && grep -q 'requireRole' src/lib/actions/contacts.ts && grep -q 'CRM_MANAGE_ROLES' src/lib/actions/contacts.ts
> verification: grep -q 'createContact' src/lib/actions/contacts.ts && grep -q 'updateContact' src/lib/actions/contacts.ts && grep -q 'deleteContact' src/lib/actions/contacts.ts
  </action>
  <verify>
test -f src/lib/validations/contact.ts
test -f src/lib/actions/contacts.ts
grep -q 'requireRole' src/lib/actions/contacts.ts
grep -q 'CRM_MANAGE_ROLES' src/lib/actions/contacts.ts
grep -q 'createContact' src/lib/actions/contacts.ts
grep -q 'updateContact' src/lib/actions/contacts.ts
grep -q 'deleteContact' src/lib/actions/contacts.ts
  </verify>
  <done>contact.ts validation schema and contacts.ts Server Actions exist, all three actions gated via requireRole(CRM_MANAGE_ROLES).</done>
</task>

<task type="auto">
  <name>Task 2: Replace ContactsTab placeholder with real implementation</name>
  <files>src/components/crm/contacts-tab.tsx, src/components/crm/contact-form.tsx</files>
  <action>
Read the current `src/components/crm/contacts-tab.tsx` placeholder to confirm its exact export signature. Read `src/components/crm/sites-tab.tsx` and `02-02-SUMMARY.md` to match its data-fetching pattern (props from parent vs. internal query) for consistency.

Create `src/components/crm/contact-form.tsx` (`"use client"`) with name/email/phone/title inputs and an optional site `select` (using the shadcn `select` component), calling `createContact(companyId, formData)` on submit and displaying returned validation errors.

Replace `src/components/crm/contacts-tab.tsx`'s body, preserving `export function ContactsTab(props: CrmTabProps)` with `CrmTabProps` imported from `./tab-types`, to list the company's existing contacts (name, email, phone, associated site if any) and render `ContactForm` below the list.

> verification: grep -q 'export function ContactsTab' src/components/crm/contacts-tab.tsx
> verification: grep -q 'CrmTabProps' src/components/crm/contacts-tab.tsx
> verification: grep -c 'Coming soon' src/components/crm/contacts-tab.tsx | xargs test 0 -eq
> verification: test -f src/components/crm/contact-form.tsx && grep -q 'createContact' src/components/crm/contact-form.tsx
> verification: npx tsc --noEmit
> verification: npm run build
  </action>
  <verify>
grep -q 'export function ContactsTab' src/components/crm/contacts-tab.tsx
grep -q 'CrmTabProps' src/components/crm/contacts-tab.tsx
grep -c 'Coming soon' src/components/crm/contacts-tab.tsx | xargs test 0 -eq
test -f src/components/crm/contact-form.tsx
grep -q 'createContact' src/components/crm/contact-form.tsx
npx tsc --noEmit
npm run build
  </verify>
  <done>ContactsTab shows real contact data and a working add-contact form, with the original export signature intact and the full build passing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] `npm run build` exits 0
- [ ] `ContactsTab(props: CrmTabProps)` export signature unchanged from 02-02's placeholder, importing CrmTabProps from ./tab-types
- [ ] No "Coming soon" text remains in contacts-tab.tsx
- [ ] createContact/updateContact/deleteContact all gated via requireRole(CRM_MANAGE_ROLES)
- [ ] No files outside files_modified were touched (`git diff --stat` confirms)
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Contacts can be created, edited, and deleted, scoped to a company and optionally a site
- crm:manage roles (sales/finance/admin) can manage contacts; technician and dispatcher cannot mutate (requireRole(CRM_MANAGE_ROLES) enforced server-side)
- The company detail page's Contacts tab renders real data with no changes needed to the parent page file
</success_criteria>

<output>
After completion, create `.planning/phases/02-crm-core/02-03-SUMMARY.md` documenting what was done, verification results, decisions, and any deviations.
</output>
