---
phase: 02-crm-core
plan: 05
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/lib/validations/asset.ts
  - src/lib/actions/assets.ts
  - src/components/crm/assets-tab.tsx
  - src/components/crm/asset-form.tsx
  - src/components/nav/app-sidebar.tsx
files_forbidden:
  - prisma/schema.prisma
  - prisma/migrations/**
  - src/lib/permissions.ts
  - src/lib/session.ts
  - src/lib/db.ts
  - src/app/(dashboard)/clients/page.tsx
  - src/app/(dashboard)/clients/new/page.tsx
  - src/app/(dashboard)/clients/[companyId]/page.tsx
  - src/components/crm/sites-tab.tsx
  - src/components/crm/site-form.tsx
  - src/components/crm/company-form.tsx
  - src/components/crm/contacts-tab.tsx
  - src/components/crm/contact-form.tsx
  - src/components/crm/contracts-tab.tsx
  - src/components/crm/contract-form.tsx
  - src/lib/actions/companies.ts
  - src/lib/actions/sites.ts
  - src/lib/actions/contacts.ts
  - src/lib/actions/contracts.ts
sequential_files: []
expected_artifacts:
  - path: "src/lib/actions/assets.ts"
    provides: "Server Actions: createAsset, updateAsset, deleteAsset"
    required: true
  - path: "src/components/crm/assets-tab.tsx"
    provides: "Real Assets tab implementation replacing Plan 02-02's placeholder stub"
    required: true
  - path: "src/components/nav/app-sidebar.tsx"
    provides: "Sidebar with a new 'Clients' nav link gated by crm:view"
    required: true
autonomous: false
agents: ["engineering-frontend-developer"]
requirements: ["Asset/device tracking tied to clients and tickets"]
user_setup: []
verification_commands:
  - "npx tsc --noEmit"
  - "npm run build"
  - "grep -q 'requireRole' src/lib/actions/assets.ts && grep -q 'CRM_MANAGE_ROLES' src/lib/actions/assets.ts"
  - "grep -q 'export function AssetsTab' src/components/crm/assets-tab.tsx"
  - "grep -c 'Coming soon' src/components/crm/assets-tab.tsx | xargs test 0 -eq"
  - "grep -q 'crm:view' src/components/nav/app-sidebar.tsx && grep -q '/clients' src/components/nav/app-sidebar.tsx"

must_haves:
  truths:
    - "A sales/finance/admin user can add an asset to a company, optionally associating it with one of the company's sites."
    - "The AssetsTab component replaces the 02-02 placeholder while preserving the shared CrmTabProps ({ companyId }: { companyId: string }) export signature, imported from ./tab-types."
    - "createAsset/updateAsset/deleteAsset all call requireRole(CRM_MANAGE_ROLES) before any database write, importing the constant from @/lib/permissions rather than hardcoding a role array."
    - "The sidebar shows a 'Clients' link to all roles with crm:view permission (all 5 roles), using can(role, permission) gating, never a hardcoded role-name check."
  artifacts:
    - path: "src/components/crm/assets-tab.tsx"
      provides: "Real asset list + add-asset form for a given company"
      min_lines: 15
      contains: "AssetsTab"
    - path: "src/components/nav/app-sidebar.tsx"
      provides: "Clients nav link gated by can(role, \"crm:view\")"
      min_lines: 20
      contains: "crm:view"
  key_links:
    - from: "src/components/crm/assets-tab.tsx"
      to: "src/lib/actions/assets.ts"
      via: "AssetsTab's form submits through createAsset"
      pattern: "createAsset"
    - from: "src/components/nav/app-sidebar.tsx"
      to: "src/app/(dashboard)/clients/page.tsx"
      via: "Sidebar 'Clients' link routes to the company list page"
      pattern: "href=\"/clients\""
---

<objective>
Replace Plan 02-02's `AssetsTab` placeholder stub with a real implementation for asset/device CRUD scoped to a company and optional site, and add the "Clients" navigation entry to the dashboard sidebar.

Purpose: Assets are the last of the 5 Phase 2 CRM entities; this plan also makes the entire CRM module reachable from the main navigation (the module is otherwise only accessible by typing `/clients` directly), completing Phase 2's UI integration.
Output: `src/lib/actions/assets.ts`, `src/components/crm/assets-tab.tsx` (real), `src/components/crm/asset-form.tsx`, updated `src/components/nav/app-sidebar.tsx`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-crm-core/02-CONTEXT.md
@.planning/phases/02-crm-core/02-01-SUMMARY.md
@.planning/phases/02-crm-core/02-02-SUMMARY.md

Relevant source files:
@prisma/schema.prisma
@src/lib/permissions.ts
@src/lib/session.ts
@src/lib/db.ts
@src/components/crm/assets-tab.tsx
@src/components/nav/app-sidebar.tsx
@src/lib/actions/companies.ts
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-frontend-developer
Task: Implement full Asset CRUD (create, update, delete) scoped to a Company with optional Site association, replacing the placeholder `AssetsTab` stub from Plan 02-02, and add a permission-gated "Clients" link to the sidebar.
Scope:
- Read targets: `prisma/schema.prisma` (Asset model shape), `src/lib/permissions.ts` (`CRM_MANAGE_ROLES`), `src/lib/session.ts`, `src/lib/db.ts`, `src/components/crm/tab-types.ts` (`CrmTabProps`), the existing `src/components/crm/assets-tab.tsx` placeholder, `src/components/nav/app-sidebar.tsx` (current nav pattern), `src/lib/actions/companies.ts` (Server Action pattern reference)
- Write targets: `src/lib/validations/asset.ts`, `src/lib/actions/assets.ts`, `src/components/crm/assets-tab.tsx`, `src/components/crm/asset-form.tsx`, `src/components/nav/app-sidebar.tsx`
- Forbidden targets: everything in `files_forbidden` above, especially `src/app/(dashboard)/clients/[companyId]/page.tsx` (owned by 02-02) and other plans' tab stubs/actions
Allowed tools/actions:
- Read, Edit, Write on files in `files_modified`
- Bash: `npx tsc --noEmit`, `npm run build`
Forbidden actions:
- Do not modify `prisma/schema.prisma` or `src/lib/permissions.ts`.
- Do not modify `src/app/(dashboard)/clients/[companyId]/page.tsx` -- it already imports `AssetsTab` correctly from 02-02.
- Do not touch `sites-tab.tsx`, `contacts-tab.tsx`, `contracts-tab.tsx`, `tab-types.ts`, or any other plan's action files.
- Do not add a hardcoded role-name comparison (e.g. `role === "admin"`) to the sidebar -- use `can(role, permission)` exactly as the existing "Dashboard"/"Admin" links do.
- Do not hardcode a role-array literal for RBAC checks in assets.ts -- import `CRM_MANAGE_ROLES` from `@/lib/permissions`.
- Do not self-defer planned work.
Implementation sequence:
1. Read `prisma/schema.prisma`'s `Asset` model, the current `src/components/crm/assets-tab.tsx` placeholder, `src/components/crm/tab-types.ts` (`CrmTabProps`), and `src/components/nav/app-sidebar.tsx`'s existing gating pattern.
2. Write `src/lib/validations/asset.ts` (zod schema).
3. Write `src/lib/actions/assets.ts` (Server Actions).
4. Write `src/components/crm/asset-form.tsx` and replace `src/components/crm/assets-tab.tsx`'s body, preserving the `AssetsTab(props: CrmTabProps)` export (importing `CrmTabProps` from `./tab-types`, not re-declaring it inline).
5. Add the "Clients" nav link to `src/components/nav/app-sidebar.tsx`, gated by `can(role, "crm:view")`, following the exact same conditional-rendering pattern already used for "Dashboard" and "Admin".
Required interfaces/content structure:
- `src/lib/validations/asset.ts`: `export const assetSchema = z.object({ name: z.string().min(1), assetType: z.string().min(1), serialNumber: z.string().optional(), notes: z.string().optional(), siteId: z.string().optional() })`
- `src/lib/actions/assets.ts`: `"use server"` exporting `createAsset(companyId: string, formData: FormData)`, `updateAsset(id: string, formData: FormData)`, `deleteAsset(id: string)`. Each imports `CRM_MANAGE_ROLES` from `@/lib/permissions` and calls `await requireRole(CRM_MANAGE_ROLES)` first -- do not hardcode a literal role array. Validate with `assetSchema`. Use `db.asset.create/update/delete`. Catch `P2025`, return `{ error: "Asset not found" }`.
- `src/components/crm/assets-tab.tsx`: preserve `export function AssetsTab(props: CrmTabProps)`, importing `CrmTabProps` from `./tab-types`. List the company's assets (name, asset type, serial number, associated site if any) and render `AssetForm` below.
- `src/components/crm/asset-form.tsx`: `"use client"` component with name/assetType/serialNumber/notes inputs (use the shadcn `textarea` for notes) and an optional site `select`. Calls `createAsset(companyId, formData)` on submit.
- `src/components/nav/app-sidebar.tsx`: insert a new `<li>` block for "Clients" immediately after the existing "Dashboard" `<li>` block (before "Admin"), following the exact same JSX shape: `{can(role, "crm:view") && (<li><Link href="/clients" className="...">Clients</Link></li>)}` using the same `className` string already used for the "Dashboard" link (copy it verbatim for visual consistency).
Edge/error cases:
- A company with zero sites: the site dropdown in `AssetForm` must render gracefully, not crash.
- Deleting/updating a nonexistent asset: caught via `P2025`, returns `{ error: "Asset not found" }`.
- `assetType` is free text (no enum) per 02-CONTEXT.md -- do not add an enum or fixed dropdown list of asset types; use a plain text input.
Verification criteria:
- `npx tsc --noEmit` and `npm run build` both exit 0
- `AssetsTab` export signature unchanged from the placeholder
- No "Coming soon" placeholder text remains in `assets-tab.tsx`
- Sidebar's new "Clients" link uses `can(role, "crm:view")`, not a hardcoded role check
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `src/components/crm/assets-tab.tsx` does not exist or its placeholder export signature differs from `AssetsTab(props: CrmTabProps)`.
- `prisma/schema.prisma` does not contain an `Asset` model matching 02-01's shape.
- `src/components/nav/app-sidebar.tsx`'s existing gating pattern (the `can(role, permission) && (...)` JSX shape) has changed from what 01-CONTEXT.md/01-04-SUMMARY.md describe, in a way that makes following the same pattern ambiguous.
- If `02-02-SUMMARY.md` is missing, unclear, or its documented pattern conflicts with what `src/components/crm/sites-tab.tsx`'s actual source code shows for any data-fetching question relevant to `AssetsTab`: do NOT block. Follow `sites-tab.tsx`'s real code as the authoritative source, note the assumption in the result, and proceed.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, `.planning/phases/02-crm-core/02-CONTEXT.md`, `02-02-SUMMARY.md`, and any `02-05-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx tsc --noEmit` and `npm run build` before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Asset validation schema and Server Actions</name>
  <files>src/lib/validations/asset.ts, src/lib/actions/assets.ts</files>
  <action>
Read `prisma/schema.prisma`'s `Asset` model first, confirming field names (`companyId`, `siteId` nullable, `name`, `assetType`, `serialNumber`, `notes`).

Create `src/lib/validations/asset.ts` with the exact `assetSchema` from this plan's "Required interfaces/content structure".

Create `src/lib/actions/assets.ts` as a `"use server"` module exporting `createAsset(companyId, formData)`, `updateAsset(id, formData)`, `deleteAsset(id)`, each importing `CRM_MANAGE_ROLES` from `@/lib/permissions` and starting with `await requireRole(CRM_MANAGE_ROLES)`. Validate with `assetSchema.safeParse`. Use `db.asset.create/update/delete`. Catch `P2025`, return `{ error: "Asset not found" }`.

> verification: test -f src/lib/validations/asset.ts
> verification: test -f src/lib/actions/assets.ts && grep -q 'requireRole' src/lib/actions/assets.ts && grep -q 'CRM_MANAGE_ROLES' src/lib/actions/assets.ts
> verification: grep -q 'createAsset' src/lib/actions/assets.ts && grep -q 'updateAsset' src/lib/actions/assets.ts && grep -q 'deleteAsset' src/lib/actions/assets.ts
  </action>
  <verify>
test -f src/lib/validations/asset.ts
test -f src/lib/actions/assets.ts
grep -q 'requireRole' src/lib/actions/assets.ts
grep -q 'CRM_MANAGE_ROLES' src/lib/actions/assets.ts
grep -q 'createAsset' src/lib/actions/assets.ts
grep -q 'updateAsset' src/lib/actions/assets.ts
grep -q 'deleteAsset' src/lib/actions/assets.ts
  </verify>
  <done>asset.ts validation schema and assets.ts Server Actions exist, all three actions gated via requireRole(CRM_MANAGE_ROLES).</done>
</task>

<task type="auto">
  <name>Task 2: Replace AssetsTab placeholder with real implementation</name>
  <files>src/components/crm/assets-tab.tsx, src/components/crm/asset-form.tsx</files>
  <action>
Read the current `src/components/crm/assets-tab.tsx` placeholder to confirm its exact export signature.

Create `src/components/crm/asset-form.tsx` (`"use client"`) with name/assetType/serialNumber/notes (textarea) inputs and an optional site `select`. Calls `createAsset(companyId, formData)` on submit, displaying returned validation errors.

Replace `src/components/crm/assets-tab.tsx`'s body, preserving `export function AssetsTab(props: CrmTabProps)` with `CrmTabProps` imported from `./tab-types`, to list the company's existing assets (name, type, serial number, site) and render `AssetForm` below the list.

> verification: grep -q 'export function AssetsTab' src/components/crm/assets-tab.tsx
> verification: grep -q 'CrmTabProps' src/components/crm/assets-tab.tsx
> verification: grep -c 'Coming soon' src/components/crm/assets-tab.tsx | xargs test 0 -eq
> verification: test -f src/components/crm/asset-form.tsx && grep -q 'createAsset' src/components/crm/asset-form.tsx
  </action>
  <verify>
grep -q 'export function AssetsTab' src/components/crm/assets-tab.tsx
grep -q 'CrmTabProps' src/components/crm/assets-tab.tsx
grep -c 'Coming soon' src/components/crm/assets-tab.tsx | xargs test 0 -eq
test -f src/components/crm/asset-form.tsx
grep -q 'createAsset' src/components/crm/asset-form.tsx
  </verify>
  <done>AssetsTab shows real asset data and a working add-asset form, with the original export signature intact.</done>
</task>

<task type="auto">
  <name>Task 3: Add "Clients" nav link to sidebar</name>
  <files>src/components/nav/app-sidebar.tsx</files>
  <action>
Read the current `src/components/nav/app-sidebar.tsx` in full to confirm the exact JSX pattern used for the "Dashboard" `<li>` block (conditional render via `can(role, "dashboard:view")`, `className` string used on the `Link`).

Insert a new `<li>` block immediately after the "Dashboard" block (before "Admin"), conditionally rendered via `can(role, "crm:view")`, linking to `/clients` with the label "Clients", using the identical `className` string already used on the "Dashboard" link for visual consistency. Do not alter the existing "Dashboard" or "Admin" blocks.

> verification: grep -q 'crm:view' src/components/nav/app-sidebar.tsx
> verification: grep -q 'href="/clients"' src/components/nav/app-sidebar.tsx
> verification: grep -q 'dashboard:view' src/components/nav/app-sidebar.tsx
> verification: grep -q 'admin:manage_users' src/components/nav/app-sidebar.tsx
> verification: npx tsc --noEmit
> verification: npm run build
  </action>
  <verify>
grep -q 'crm:view' src/components/nav/app-sidebar.tsx
grep -q 'href="/clients"' src/components/nav/app-sidebar.tsx
grep -q 'dashboard:view' src/components/nav/app-sidebar.tsx
grep -q 'admin:manage_users' src/components/nav/app-sidebar.tsx
npx tsc --noEmit
npm run build
  </verify>
  <done>The sidebar shows a permission-gated "Clients" link alongside the unchanged "Dashboard" and "Admin" entries, and the full build passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] `npm run build` exits 0
- [ ] `AssetsTab(props: CrmTabProps)` export signature unchanged from 02-02's placeholder, importing CrmTabProps from ./tab-types
- [ ] No "Coming soon" text remains in assets-tab.tsx
- [ ] createAsset/updateAsset/deleteAsset all gated via requireRole(CRM_MANAGE_ROLES)
- [ ] Sidebar "Clients" link uses can(role, "crm:view"), existing Dashboard/Admin links unchanged
- [ ] No files outside files_modified were touched (`git diff --stat` confirms)
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Assets can be created, edited, and deleted, scoped to a company and optionally a site
- crm:manage roles (sales/finance/admin) can manage assets; technician and dispatcher cannot mutate (requireRole(CRM_MANAGE_ROLES) enforced server-side)
- The entire CRM module (companies, sites, contacts, contracts, assets) is reachable from the sidebar for any role with crm:view
- The company detail page's Assets tab renders real data with no changes needed to the parent page file
</success_criteria>

<output>
After completion, create `.planning/phases/02-crm-core/02-05-SUMMARY.md` documenting what was done, verification results, decisions, and any deviations.
</output>
