---
phase: 03-ticketing-service-desk
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/email-poller.ts
  - docker-compose.yml
  - Dockerfile
  - package.json
  - .env.example
files_forbidden:
  - prisma/schema.prisma
  - prisma/migrations/**
  - src/lib/permissions.ts
  - src/lib/sla.ts
  - src/app/**
  - src/components/**
  - src/lib/actions/**
  - src/lib/validations/**
  - src/components/nav/app-sidebar.tsx
sequential_files:
  - package.json
expected_artifacts:
  - path: "scripts/email-poller.ts"
    provides: "Standalone Node script polling a shared mailbox via Microsoft Graph API on a 1-2 minute interval, creating Ticket + initial TicketComment records for new messages"
    required: true
  - path: "docker-compose.yml"
    provides: "New email-poller service running scripts/email-poller.ts alongside app/db"
    required: true
  - path: "Dockerfile"
    provides: "runner stage extended to include scripts/, src/, tsconfig.json, and a script runner (tsx) so the email-poller service can actually execute in the built image -- the existing runner stage only copies .next/public/node_modules/package.json/prisma, which is insufficient for running a standalone TS script"
    required: true
  - path: ".env.example"
    provides: "Documented AZURE_TENANT_ID/AZURE_CLIENT_ID/AZURE_CLIENT_SECRET/MAILBOX_ADDRESS env vars"
    required: true
autonomous: false
agents: ["engineering-backend-architect"]
requirements: ["Email-to-ticket creation"]
user_setup: ["Azure AD app registration with Mail.Read (or Mail.ReadWrite) application permission, and the shared mailbox's address, must be provisioned by the user before the poller can authenticate in a real environment -- this plan implements and documents the integration but cannot provision Azure AD credentials itself."]
verification_commands:
  - "npx tsc --noEmit"
  - "test -f scripts/email-poller.ts"
  - "grep -q 'AZURE_TENANT_ID' scripts/email-poller.ts && grep -q 'AZURE_CLIENT_ID' scripts/email-poller.ts && grep -q 'AZURE_CLIENT_SECRET' scripts/email-poller.ts && grep -q 'MAILBOX_ADDRESS' scripts/email-poller.ts"
  - "grep -q 'email-poller' docker-compose.yml"
  - "grep -q 'source: \"email\"\\|source: .email.' scripts/email-poller.ts"
  - "grep -q 'AZURE_TENANT_ID' .env.example"
  - "grep -q 'scripts' Dockerfile"

must_haves:
  truths:
    - "scripts/email-poller.ts polls the shared mailbox via the Microsoft Graph API (not IMAP) on a 1-2 minute interval, per the user's explicit correction recorded in 03-CONTEXT.md and cross-session memory."
    - "The poller creates a Ticket with source: \"email\" and an initial non-internal TicketComment for each new message, best-effort-matching the sender's email address to an existing Contact (unmatched senders still create a ticket with contactId: null)."
    - "The poller tracks a last-polled watermark so restarting the process does not reprocess already-ingested messages."
    - "docker-compose.yml defines an email-poller service sharing DATABASE_URL with the app service and adding the 4 new AZURE_*/MAILBOX_ADDRESS env vars, running scripts/email-poller.ts as its command."
    - "The Dockerfile's runner stage includes scripts/, src/, tsconfig.json, and the tsx script runner (or equivalent), so `docker compose up email-poller` actually executes scripts/email-poller.ts in the built image -- not just in local dev. The original runner stage (copying only .next/public/node_modules/package.json/prisma) cannot run a standalone TS script and was confirmed insufficient by reading the actual Dockerfile during planning."
    - "No UI, Server Action, or permissions file is touched by this plan -- it is fully disjoint from Plan 03-02's file set except for the shared, sequential-dispatch-guarded package.json, since both run in the same wave."
  artifacts:
    - path: "scripts/email-poller.ts"
      provides: "Graph API polling loop with ticket-creation and watermark tracking"
      min_lines: 60
      contains: "MAILBOX_ADDRESS"
    - path: "docker-compose.yml"
      provides: "email-poller service definition"
      min_lines: 5
      contains: "email-poller"
  key_links:
    - from: "scripts/email-poller.ts"
      to: "prisma/schema.prisma"
      via: "Poller creates Ticket rows with source: \"email\" using the Prisma Client generated from Plan 03-01's schema"
      pattern: "source:\\s*[\"']email[\"']"
    - from: "docker-compose.yml"
      to: "scripts/email-poller.ts"
      via: "email-poller service's command runs this script"
      pattern: "email-poller.ts"
---

<objective>
Build the standalone Microsoft Graph API email-to-ticket poller script and wire it into Docker Compose as its own service, per the user's explicit correction rejecting IMAP in favor of Graph API polling a shared mailbox.

Purpose: Delivers the phase's "Email-to-ticket creation" requirement independently of the Kanban UI (Plan 03-02) -- both plans depend only on Plan 03-01's schema and run in parallel on fully disjoint files this wave.
Output: `scripts/email-poller.ts`, an `email-poller` Docker Compose service, and documented environment variables.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ticketing-service-desk/03-CONTEXT.md
@.planning/phases/03-ticketing-service-desk/03-01-SUMMARY.md

Relevant source files:
@prisma/schema.prisma
@src/lib/db.ts
@docker-compose.yml
@package.json
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-backend-architect (Graph API integration, standalone process design)
Task: Create `scripts/email-poller.ts` polling a shared mailbox via Microsoft Graph API, creating Ticket + TicketComment records for new messages; add an `email-poller` Docker Compose service; document required env vars.
Scope:
- Read targets: `prisma/schema.prisma`, `src/lib/db.ts`, `docker-compose.yml`, `Dockerfile`, `package.json`, `.planning/phases/03-ticketing-service-desk/03-CONTEXT.md` (Email-to-ticket decisions section), `03-01-SUMMARY.md`
- Write targets: `scripts/email-poller.ts` (new file), `docker-compose.yml`, `Dockerfile`, `package.json` (dependency additions only, re-read immediately before editing per the `sequential_files` note below), `.env.example` (create if absent, else append)
- Forbidden targets: `prisma/schema.prisma`, `prisma/migrations/**`, `src/lib/permissions.ts`, `src/lib/sla.ts`, `src/app/**`, `src/components/**`, `src/lib/actions/**`, `src/lib/validations/**`, `src/components/nav/app-sidebar.tsx`
Allowed tools/actions:
- Read, Edit, Write on files in `files_modified`
- Bash: `npm install @microsoft/microsoft-graph-client @azure/identity` (or an equivalent minimal Graph HTTP client -- implementer's choice per 03-CONTEXT.md, but must use one of these two approaches, not a raw hand-rolled OAuth flow), `npx tsc --noEmit`
Forbidden actions:
- Do not implement IMAP polling under any circumstance -- this was explicitly rejected by the user during planning (see 03-CONTEXT.md "User correction during planning" and cross-session memory `email-ingestion-graph-api`). If anything in the codebase or environment suggests IMAP is expected, treat 03-CONTEXT.md as authoritative and proceed with Graph API.
- Do not touch `src/app/`, `src/components/`, `src/lib/actions/`, `src/lib/validations/`, or `src/components/nav/app-sidebar.tsx` -- those belong to Plan 03-02, running in parallel this wave on the same file tree.
- Do not modify `prisma/schema.prisma` or run migrations -- Plan 03-01's schema is read-only input to this plan.
- Do not commit real Azure credentials anywhere -- `.env.example` documents variable names only, with placeholder values.
- Do not self-defer planned work; if Azure AD credentials are unavailable in this environment (expected -- see user_setup), implement and unit-verify the script's logic/structure and document the credential requirement, rather than skipping the script's creation.
Implementation sequence:
1. Read `docker-compose.yml`, `Dockerfile`, `package.json`, and `src/lib/db.ts` to confirm current service/build/dependency/DB-client conventions.
2. Install the Graph API client dependency and `tsx` (Task 1).
3. Create `scripts/email-poller.ts` implementing the polling loop, ticket creation, and watermark tracking (Task 1).
4. Add the `email-poller` service to `docker-compose.yml` (Task 2).
5. Extend the `Dockerfile`'s `runner` stage so the built image can actually execute `scripts/email-poller.ts` (Task 3).
6. Create or extend `.env.example` documenting the 4 new env vars (Task 2).
Required interfaces/content structure:
- `scripts/email-poller.ts`: a standalone TypeScript script (run via `tsx` or `ts-node`/compiled -- match whatever the project's existing script-running convention is; check `package.json` scripts for precedent, and if none exists, add an npm script `"email-poller": "tsx scripts/email-poller.ts"` using `tsx` since it's the lowest-friction choice for a Next.js/TypeScript project). Reads `AZURE_TENANT_ID`, `AZURE_CLIENT_ID`, `AZURE_CLIENT_SECRET`, `MAILBOX_ADDRESS` from `process.env`; throws a clear startup error if any are missing (fail fast, do not silently no-op). Uses `ClientSecretCredential` from `@azure/identity` and the Graph client (either `@microsoft/microsoft-graph-client`'s `Client.initWithMiddleware` or direct authenticated `fetch` calls to `https://graph.microsoft.com/v1.0/users/{MAILBOX_ADDRESS}/messages`) to poll for messages with `receivedDateTime` greater than the last-polled watermark, ordered ascending, on a `setInterval` of 90 seconds (within the 1-2 minute range specified in 03-CONTEXT.md). Implements a `pollOnce()` function (exported or top-level, structured so Plan 03-04 can locate and extend it) that: (a) fetches new messages since the watermark, (b) for each message, attempts to match the sender's email address (`message.from.emailAddress.address`) to an existing `Contact.email` via `db.contact.findFirst`, (c) creates a `Ticket` with `source: "email"`, `subject: message.subject`, `description` derived from the message body (plain-text preview or full body per implementer's judgment), `companyId` resolved from the matched Contact's `companyId` if found -- if no Contact match, the ticket cannot be created without a `companyId` (it is a required field per Plan 03-01's schema); in that case, log the unmatched message and skip ticket creation for it, recording it in the watermark anyway so it is not retried forever (this is a documented, deliberate limitation: fully unmatched senders with no resolvable company are not auto-ticketed in this phase -- do not treat this as a bug to work around by inventing a "default" or "unknown" company), (d) computes SLA deadlines via `computeSlaDeadlines` (import from `@/lib/sla` -- if a relative import path is needed because this script runs outside Next.js's module resolution, use a relative path like `../src/lib/sla` and verify it resolves under the script's actual run mechanism), resolving the matched Contact's company's active contract using the exact "Active-contract resolution rule" locked in `.planning/phases/03-ticketing-service-desk/03-CONTEXT.md` (among the company's contracts where `endDate IS NULL OR endDate >= now()`, order by `startDate DESC, id DESC`, take the first row; `null` if none match) -- this rule is shared verbatim with Plan 03-02's `createTicket` and must not be reworded or reimplemented differently, so two contracts tied on `startDate` resolve identically regardless of which plan's code runs, (e) creates an initial `TicketComment` with `isInternal: false`, `authorId: null`, `body` from the message content, (f) advances and persists the watermark. Watermark storage: implementer's choice (a small JSON file under a git-ignored path, e.g. `.email-poller-state.json`, is the simplest and is acceptable per 03-CONTEXT.md's explicit deferral of this detail) -- if using a file, add it to `.gitignore` if not already covered by a pattern, and initialize the watermark to "now" on first run (do not backfill historical mail).
- `docker-compose.yml`: add an `email-poller` service using the same `build`/`image` as the `app` service, `command: ["npm", "run", "email-poller"]` (or equivalent matching the npm script added above), `environment` including `DATABASE_URL` (same value/reference as `app`'s) plus `AZURE_TENANT_ID`, `AZURE_CLIENT_ID`, `AZURE_CLIENT_SECRET`, `MAILBOX_ADDRESS` (sourced from the same `.env` mechanism `app` already uses), `depends_on: [db]`.
- `.env.example`: append (or create) entries for `AZURE_TENANT_ID=`, `AZURE_CLIENT_ID=`, `AZURE_CLIENT_SECRET=`, `MAILBOX_ADDRESS=` with a one-line comment each explaining their purpose, matching the file's existing comment style if the file already exists.
- `Dockerfile`: the current `runner` stage (confirmed by reading the actual file during planning) copies only `public`, `.next`, `node_modules`, `package.json`, `prisma`, `prisma7.config.ts` from `builder`, and sets `ENV NODE_ENV=production` -- this is insufficient to run `scripts/email-poller.ts`, which needs the raw TypeScript source and a script runner. Add to the `runner` stage, after the existing `COPY --from=builder /app/prisma7.config.ts ./prisma7.config.ts` line and before `EXPOSE 3000`: `COPY --from=builder /app/scripts ./scripts`, `COPY --from=builder /app/src ./src`, `COPY --from=builder /app/tsconfig.json ./tsconfig.json`. Do not change the `deps`/`builder` stages, `EXPOSE`, or the existing `CMD ["npm", "start"]` -- the `email-poller` service in `docker-compose.yml` overrides the command via its own `command:` field (per Task 2), so the image's default `CMD` stays app-oriented and unchanged. `tsx` must be present in `node_modules` at runtime for the `email-poller` service's command to work -- since `NODE_ENV=production` is set as an image `ENV` (not passed to the `deps` stage's `npm ci`), `npm ci` in the `deps` stage installs devDependencies as normal (npm only skips devDependencies when `NODE_ENV=production` is set in the environment `npm ci` itself runs under, which it is not here), so installing `tsx` as a devDependency in Task 1 is sufficient -- do not move `tsx` to `dependencies` unless verification in Task 3 shows it is actually missing from the copied `node_modules`.
Edge/error cases:
- `package.json` is also written by Plan 03-02 in this same wave (both plans add different npm dependencies). Because both plans declare `package.json` in `sequential_files`, the wave-executor will fall back to fully sequential dispatch for this wave rather than true parallel execution -- this is expected and correct, not an error. Immediately before editing `package.json` (installing the Graph API client and, if needed, `tsx`), re-read its current contents rather than assuming the state described in this plan's context -- if Plan 03-02 has already run and added its own dependencies, merge additively (do not remove or reorder 03-02's additions).
- Missing/invalid Azure credentials at startup: throw and exit non-zero with a clear message naming which env var is missing -- do not retry-loop on a hard auth failure.
- Graph API rate limiting (HTTP 429): the poller should respect a `Retry-After` header if present before its next tick, or simply let the next scheduled tick (90s later) serve as a natural backoff -- do not implement aggressive retry loops within a single tick.
- A message with no parseable sender address: skip it, log it, advance the watermark past it (do not crash the poller or retry it forever).
- Duplicate processing on restart: the watermark must be persisted (not held only in memory) so a process restart does not reprocess already-ticketed messages.
- Empty poll result (no new messages): no-op, no error.
Verification criteria:
- `npx tsc --noEmit` exits 0 (the script must type-check as part of the project, even if it's a standalone entry point)
- `scripts/email-poller.ts` exists and references all 4 required env vars
- `docker-compose.yml` defines an `email-poller` service
- `Dockerfile`'s `runner` stage copies `scripts`, `src`, and `tsconfig.json`
- `.env.example` documents the 4 new env vars
- No file outside `files_modified` touched
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- Plan 03-01's `Ticket`/`TicketComment` models or `src/lib/sla.ts`'s `computeSlaDeadlines` do not exist or differ from what this plan assumes -- read `03-01-SUMMARY.md` first; if it does not yet exist, read the actual source files directly (`prisma/schema.prisma`, `src/lib/sla.ts`) as the source of truth, and only emit BLOCKED if the real source code itself is missing or contradicts this plan.
- The project has no existing script-running convention (`tsx`, `ts-node`, or a build step) and installing `tsx` as a new dependency would conflict with an existing, different convention already in `package.json` -- report the conflict rather than guessing.
- `@microsoft/microsoft-graph-client` and `@azure/identity` fail to install (network/registry issue) -- report the exact error rather than silently falling back to a different auth mechanism.
- `docker-compose.yml`'s current structure for the `app` service is missing or shaped in a way that makes it unclear how to mirror `DATABASE_URL`/build config for the new service -- report the ambiguity.
- `Dockerfile`'s `runner` stage does not match this plan's assumed current state (copying only `public`/`.next`/`node_modules`/`package.json`/`prisma`/`prisma7.config.ts`) -- report the discrepancy rather than guessing where to insert the new `COPY` lines.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, `.planning/phases/03-ticketing-service-desk/03-CONTEXT.md`, `03-01-SUMMARY.md` (or source files if absent), and any `03-03-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx tsc --noEmit` before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Microsoft Graph email-poller script</name>
  <files>scripts/email-poller.ts, package.json</files>
  <action>
Read `src/lib/db.ts` to confirm the Prisma Client import/instantiation pattern this script must reuse (do not create a second, divergent Prisma Client instantiation -- import and reuse the same pattern, adjusting only if this script's execution context requires its own instance per `@prisma/adapter-pg` driver adapter requirements noted in 03-CONTEXT.md).

Install `@microsoft/microsoft-graph-client` and `@azure/identity` (or confirm an equivalent minimal HTTP-based approach per this plan's specification). If no script-runner (`tsx`/`ts-node`) exists in `package.json`, install `tsx` as a dev dependency and add an `"email-poller": "tsx scripts/email-poller.ts"` script.

Create `scripts/email-poller.ts` implementing the full polling loop, ticket/comment creation, sender-to-Contact matching, watermark tracking, and error handling exactly as specified in this plan's `<execution_contract>` "Required interfaces/content structure" and "Edge/error cases" sections. Structure `pollOnce()` as a clearly separable function so Plan 03-04 can locate and extend it with breach-check logic on the same tick.

> verification: test -f scripts/email-poller.ts
> verification: grep -q 'AZURE_TENANT_ID' scripts/email-poller.ts
> verification: grep -q 'AZURE_CLIENT_ID' scripts/email-poller.ts
> verification: grep -q 'AZURE_CLIENT_SECRET' scripts/email-poller.ts
> verification: grep -q 'MAILBOX_ADDRESS' scripts/email-poller.ts
> verification: grep -q 'pollOnce' scripts/email-poller.ts
> verification: grep -Eq 'source:\s*"email"|source:\s*.email.' scripts/email-poller.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f scripts/email-poller.ts
grep -q 'AZURE_TENANT_ID' scripts/email-poller.ts
grep -q 'AZURE_CLIENT_ID' scripts/email-poller.ts
grep -q 'AZURE_CLIENT_SECRET' scripts/email-poller.ts
grep -q 'MAILBOX_ADDRESS' scripts/email-poller.ts
grep -q 'pollOnce' scripts/email-poller.ts
grep -Eq 'source:\s*"email"|source:\s*.email.' scripts/email-poller.ts
npx tsc --noEmit
  </verify>
  <done>scripts/email-poller.ts implements Graph API polling, ticket/comment creation with source: "email", sender-to-Contact matching, and watermark persistence, structured with a separable pollOnce() function, and the project type-checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Docker Compose service and env documentation</name>
  <files>docker-compose.yml, .env.example</files>
  <action>
Read `docker-compose.yml` in full to confirm the `app` service's current `build`/`environment`/`depends_on` structure.

Add a new `email-poller` service mirroring `app`'s build config, running the npm script added in Task 1, sharing `DATABASE_URL`, and adding `AZURE_TENANT_ID`, `AZURE_CLIENT_ID`, `AZURE_CLIENT_SECRET`, `MAILBOX_ADDRESS` to its environment, with `depends_on: [db]`.

Create `.env.example` if it does not exist, or append to it if it does, documenting the 4 new env vars with one-line explanatory comments each, matching the file's existing style if present.

> verification: grep -q 'email-poller' docker-compose.yml
> verification: grep -q 'AZURE_TENANT_ID' .env.example
> verification: grep -q 'AZURE_CLIENT_ID' .env.example
> verification: grep -q 'AZURE_CLIENT_SECRET' .env.example
> verification: grep -q 'MAILBOX_ADDRESS' .env.example
  </action>
  <verify>
grep -q 'email-poller' docker-compose.yml
grep -q 'AZURE_TENANT_ID' .env.example
grep -q 'AZURE_CLIENT_ID' .env.example
grep -q 'AZURE_CLIENT_SECRET' .env.example
grep -q 'MAILBOX_ADDRESS' .env.example
  </verify>
  <done>docker-compose.yml defines an email-poller service sharing DATABASE_URL with app and the 4 new Azure/mailbox env vars, and .env.example documents those 4 variables.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Dockerfile runner stage to support running the email-poller script</name>
  <files>Dockerfile</files>
  <action>
Read `Dockerfile` in full first. Confirm the `runner` stage currently copies only `public`, `.next`, `node_modules`, `package.json`, `prisma`, `prisma7.config.ts` from `builder`, ending with `EXPOSE 3000` and `CMD ["npm", "start"]`. If it differs from this, emit BLOCKED.

Add three `COPY --from=builder` lines to the `runner` stage, after the existing `COPY --from=builder /app/prisma7.config.ts ./prisma7.config.ts` line and before `EXPOSE 3000`: one copying `/app/scripts` to `./scripts`, one copying `/app/src` to `./src`, one copying `/app/tsconfig.json` to `./tsconfig.json`. Do not modify the `deps` or `builder` stages, `EXPOSE 3000`, or `CMD ["npm", "start"]` -- the `email-poller` Docker Compose service (Task 2) overrides the container's command, so the image's default command must remain app-oriented.

> verification: grep -q 'COPY --from=builder /app/scripts' Dockerfile
> verification: grep -q 'COPY --from=builder /app/src' Dockerfile
> verification: grep -q 'COPY --from=builder /app/tsconfig.json' Dockerfile
> verification: grep -q 'CMD \["npm", "start"\]' Dockerfile
  </action>
  <verify>
grep -q 'COPY --from=builder /app/scripts' Dockerfile
grep -q 'COPY --from=builder /app/src' Dockerfile
grep -q 'COPY --from=builder /app/tsconfig.json' Dockerfile
grep -q 'CMD \["npm", "start"\]' Dockerfile
  </verify>
  <done>Dockerfile's runner stage copies scripts/, src/, and tsconfig.json in addition to its existing artifacts, and the app service's default CMD is unchanged.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] `scripts/email-poller.ts` uses Microsoft Graph API, NOT IMAP
- [ ] Poller creates tickets with `source: "email"` and an initial non-internal comment, best-effort-matching sender to Contact
- [ ] Watermark is persisted (not memory-only) to avoid reprocessing on restart
- [ ] `docker-compose.yml` has an `email-poller` service sharing `DATABASE_URL` with `app` and the 4 new env vars
- [ ] `Dockerfile`'s `runner` stage copies `scripts/`, `src/`, and `tsconfig.json` so the built image can actually run the poller script; app service's default `CMD` is unchanged
- [ ] `.env.example` documents `AZURE_TENANT_ID`/`AZURE_CLIENT_ID`/`AZURE_CLIENT_SECRET`/`MAILBOX_ADDRESS`
- [ ] No files outside `files_modified` were touched (`git diff --stat` confirms), and no file owned by Plan 03-02 (`src/app/**`, `src/components/**`, `src/lib/actions/**`, `src/lib/validations/**`, sidebar nav) was touched -- `package.json` is a shared, sequential-dispatch-guarded exception
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Tickets can be created via inbound email through Microsoft Graph API polling (not IMAP)
- The poller runs as its own Docker Compose service, polling on a 1-2 minute interval
- Sender emails are best-effort matched to existing Contacts; unmatched-but-resolvable-company messages still create tickets
- A persisted watermark prevents reprocessing messages across restarts
- Required Azure AD env vars are documented in .env.example for the user to provision
- The email-poller Docker Compose service can actually execute scripts/email-poller.ts in the built image (Dockerfile's runner stage includes the necessary source and script runner), not just in local dev
</success_criteria>

<output>
After completion, create `.planning/phases/03-ticketing-service-desk/03-03-SUMMARY.md` documenting what was done, verification results, decisions, and any deviations. Explicitly note the `pollOnce()` function's location and structure for Plan 03-04, since it will extend this same file.
</output>
