---
phase: 04-time-tracking-billing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/**
  - src/lib/permissions.ts
files_forbidden:
  - src/app/**
  - src/components/**
  - src/lib/actions/**
  - src/lib/validations/**
  - src/lib/session.ts
  - src/lib/db.ts
  - src/lib/sla.ts
  - src/lib/billing.ts
  - src/lib/timer.ts
  - src/lib/qbo.ts
  - src/auth.ts
  - src/auth.config.ts
  - src/middleware.ts
  - scripts/**
  - docker-compose.yml
  - package.json
sequential_files: []
expected_artifacts:
  - path: "prisma/schema.prisma"
    provides: "TimeEntry, Invoice, InvoiceLineItem, QuickBooksConnection models plus InvoiceStatus enum, Company.qboCustomerId field, and back-relations on Ticket/User/Contract/Company"
    required: true
  - path: "prisma/migrations"
    provides: "Applied migration adding the 4 new tables, 1 new enum, 1 new Company column, and a raw-SQL partial unique index for one-active-timer-per-user"
    required: true
  - path: "src/lib/permissions.ts"
    provides: "Permission union extended with timeentry:manage/invoice:view/invoice:manage/invoice:push_qbo/qbo:manage plus TIME_ENTRY_MANAGE_ROLES/INVOICE_MANAGE_ROLES/QBO_MANAGE_ROLES exported"
    required: true
autonomous: false
agents: ["engineering-backend-architect"]
requirements: ["Timer-based time entry against tickets", "Contract-based billing rules", "Invoice generation from logged time and contract terms", "Accounting integration (QuickBooks or Xero)"]
user_setup: []
verification_commands:
  - "npx prisma validate"
  - "grep -q 'model TimeEntry' prisma/schema.prisma && grep -q 'model Invoice' prisma/schema.prisma && grep -q 'model InvoiceLineItem' prisma/schema.prisma && grep -q 'model QuickBooksConnection' prisma/schema.prisma"
  - "grep -q 'enum InvoiceStatus' prisma/schema.prisma"
  - "grep -q 'qboCustomerId' prisma/schema.prisma"
  - "grep -rq 'one_active_timer_per_user' prisma/migrations/"
  - "docker compose exec -T db psql -U \"$DB_USER\" -d \"$DB_NAME\" -t -c \"SELECT indexname FROM pg_indexes WHERE tablename = 'TimeEntry';\" | grep -q 'TimeEntry_one_active_timer_per_user'"
  - "grep -q 'timeentry:manage' src/lib/permissions.ts && grep -q 'invoice:view' src/lib/permissions.ts && grep -q 'invoice:manage' src/lib/permissions.ts && grep -q 'invoice:push_qbo' src/lib/permissions.ts && grep -q 'qbo:manage' src/lib/permissions.ts"
  - "grep -q 'TIME_ENTRY_MANAGE_ROLES' src/lib/permissions.ts && grep -q 'INVOICE_MANAGE_ROLES' src/lib/permissions.ts && grep -q 'QBO_MANAGE_ROLES' src/lib/permissions.ts"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "TimeEntry, Invoice, InvoiceLineItem, and QuickBooksConnection models exist in prisma/schema.prisma with the exact field shapes specified in this plan."
    - "A Prisma migration exists, has been applied against the running Postgres database, and includes a raw SQL partial unique index enforcing at most one TimeEntry with endedAt IS NULL per userId."
    - "The Permission union includes timeentry:manage (technician/dispatcher/admin), invoice:view/invoice:manage/invoice:push_qbo (finance/admin), and qbo:manage (admin only)."
    - "src/lib/permissions.ts exports TIME_ENTRY_MANAGE_ROLES, INVOICE_MANAGE_ROLES, and QBO_MANAGE_ROLES constants matching the ROLE_PERMISSIONS grants exactly."
    - "Company gains a nullable qboCustomerId field for manual QuickBooks Customer linkage."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "4 new models, 1 new enum, 1 new Company field, and 5 back-relations appended to the existing schema"
      min_lines: 40
      contains: "model QuickBooksConnection"
    - path: "src/lib/permissions.ts"
      provides: "Extended Permission union and ROLE_PERMISSIONS matrix with timeentry:*/invoice:*/qbo:* grants"
      min_lines: 20
      contains: "QBO_MANAGE_ROLES"
  key_links:
    - from: "prisma/schema.prisma"
      to: "src/lib/permissions.ts"
      via: "TIME_ENTRY_MANAGE_ROLES/INVOICE_MANAGE_ROLES/QBO_MANAGE_ROLES key off the same Role enum values used throughout the schema"
      pattern: "technician.*dispatcher.*admin"
    - from: "prisma/schema.prisma"
      to: "prisma/migrations"
      via: "The TimeEntry model's endedAt nullable field is the column targeted by the partial unique index migration"
      pattern: "endedAt"
---

<objective>
Extend the Prisma schema with TimeEntry, Invoice, InvoiceLineItem, and QuickBooksConnection models plus an InvoiceStatus enum and a Company.qboCustomerId field, generate and apply the migration (including a hand-added raw SQL partial unique index for the one-active-timer-per-user invariant), and extend the RBAC permission matrix with 5 new permissions and 3 new shared role-array constants.

Purpose: This is the Wave 1 foundation every later Phase 4 plan (04-02 through 04-06) depends on -- no billing computation, timer UI, invoice generation, or QuickBooks push can be written against models and permissions that don't exist yet.
Output: Updated `prisma/schema.prisma`, an applied migration under `prisma/migrations/` (with the raw SQL partial index), and extended `src/lib/permissions.ts`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-time-tracking-billing/04-CONTEXT.md

Relevant source files:
@prisma/schema.prisma
@src/lib/permissions.ts
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-backend-architect (schema, migration, and RBAC design)
Task: Add TimeEntry/Invoice/InvoiceLineItem/QuickBooksConnection models + InvoiceStatus enum + Company.qboCustomerId to `prisma/schema.prisma`; generate and apply a Prisma migration including a hand-added raw SQL partial unique index; extend `Permission`/`ROLE_PERMISSIONS` in `src/lib/permissions.ts` with 5 new permissions and 3 new role-array constants.
Scope:
- Read targets: `prisma/schema.prisma`, `src/lib/permissions.ts`, `.planning/phases/04-time-tracking-billing/04-CONTEXT.md` (Schema decisions, RBAC decisions sections)
- Write targets: `prisma/schema.prisma`, `prisma/migrations/**` (new migration directory only -- do not edit existing migrations), `src/lib/permissions.ts`
- Forbidden targets: `src/app/**`, `src/components/**`, `src/lib/actions/**`, `src/lib/validations/**`, `src/lib/session.ts`, `src/lib/db.ts`, `src/lib/sla.ts`, `src/lib/billing.ts`, `src/lib/timer.ts`, `src/lib/qbo.ts`, `src/auth.ts`, `src/auth.config.ts`, `src/middleware.ts`, `scripts/**`, `docker-compose.yml`, `package.json`
Allowed tools/actions:
- Read, Edit, Write on `prisma/schema.prisma`, `src/lib/permissions.ts`, and the generated migration's `.sql` file
- Bash: `npx prisma validate`, `npx prisma migrate dev --name add_time_tracking_billing`, `npx prisma generate`, `npx tsc --noEmit`, `docker compose ps db`, `docker compose up -d db` (if the `db` service is not already running)
Forbidden actions:
- Do not modify the existing `Role`, `BillingType`, `TicketStatus`, `TicketPriority`, `TicketSource` enums, or any existing model beyond adding the required back-relation fields and the single `Company.qboCustomerId` field listed below.
- Do not add a database-backed `Permission`/`RolePermission` table -- the permission matrix stays static/in-code per Phase 1-3 precedent.
- Do not add any file under `src/app/`, `src/components/`, `src/lib/actions/`, `src/lib/validations/`, `scripts/` -- those belong to later plans.
- Do not add new npm dependencies or modify `package.json`.
- Do not implement `computeSlaDeadlines`-style or billing computation logic in this plan -- schema and permissions only. `src/lib/billing.ts`/`src/lib/timer.ts` are Plan 04-02's scope.
- Do not skip or substitute the raw SQL partial unique index with an application-only check -- the DB-level constraint is a mandatory artifact of this plan, not an optional hardening.
- Do not implement tax/discount logic on Invoice -- `subtotal` and `total` are both required fields but must be treated as always-equal placeholders in this plan (no computation exists yet; that is Plan 04-05's scope).
- Do not self-defer planned work.
Implementation sequence:
1. Read `prisma/schema.prisma` in full to confirm current state matches this plan's assumptions (Ticket/TicketComment exist as Phase 3 left them; `Contract` has nullable `blockHours`/`flatFeeAmount`/`hourlyRate`/`slaResponseMinutes`/`slaResolutionMinutes`).
2. Read `src/lib/permissions.ts` in full to confirm current state (`Permission` union includes the 7 Phase 1-3 permissions; `TICKET_MANAGE_ROLES`/`TICKET_ASSIGN_ROLES` exported).
3. Append `InvoiceStatus` enum and the 4 new models to `prisma/schema.prisma`, add `qboCustomerId String?` to `Company`, plus the 5 back-relation fields on existing models (Task 1).
4. Ensure the `db` Docker service is running, then generate the migration (Task 2).
5. Hand-edit the generated migration's `.sql` file to append the raw SQL partial unique index statement (Task 2).
6. Apply the migration and regenerate the Prisma Client (Task 2).
7. Extend `src/lib/permissions.ts` with the 5 new permissions, matrix entries, and 3 new role-array constants (Task 3).
Required interfaces/content structure:
- `enum InvoiceStatus { draft finalized pushed }`
- `model TimeEntry { id String @id @default(cuid()) ticketId String ticket Ticket @relation(fields: [ticketId], references: [id], onDelete: Cascade) userId String user User @relation(fields: [userId], references: [id], onDelete: SetNull) contractId String? contract Contract? @relation(fields: [contractId], references: [id], onDelete: SetNull) startedAt DateTime endedAt DateTime? durationMinutes Int? isBillable Boolean @default(true) notes String? invoiceLineItemId String? @unique invoiceLineItem InvoiceLineItem? @relation(fields: [invoiceLineItemId], references: [id], onDelete: SetNull) createdAt DateTime @default(now()) updatedAt DateTime @updatedAt }`. Note: `userId` is declared required (`String`, not `String?`) at the Prisma field-type level per this plan's field list, but its relation uses `onDelete: SetNull` -- this is intentionally inconsistent with strict FK nullability rules ONLY if Prisma rejects `onDelete: SetNull` on a non-optional relation field; if Prisma's validator errors on this combination, change `userId`/`user` to nullable (`String?`/`User?`) to satisfy the constraint (a required user attribution is preferred but SetNull cascade compatibility takes precedence -- resolve this via `npx prisma validate` and use whichever variant validates; document the choice made in the plan's SUMMARY).
- `model Invoice { id String @id @default(cuid()) companyId String company Company @relation(fields: [companyId], references: [id], onDelete: Cascade) contractId String? contract Contract? @relation(fields: [contractId], references: [id], onDelete: SetNull) periodStart DateTime periodEnd DateTime status InvoiceStatus @default(draft) subtotal Decimal @db.Decimal(10, 2) total Decimal @db.Decimal(10, 2) qboInvoiceId String? qboPushedAt DateTime? lineItems InvoiceLineItem[] createdAt DateTime @default(now()) updatedAt DateTime @updatedAt }`
- `model InvoiceLineItem { id String @id @default(cuid()) invoiceId String invoice Invoice @relation(fields: [invoiceId], references: [id], onDelete: Cascade) description String quantity Decimal @db.Decimal(10, 2) unitRate Decimal @db.Decimal(10, 2) amount Decimal @db.Decimal(10, 2) timeEntries TimeEntry[] createdAt DateTime @default(now()) }`
- `model QuickBooksConnection { id String @id @default(cuid()) realmId String accessToken String @db.Text refreshToken String @db.Text accessTokenExpiresAt DateTime createdAt DateTime @default(now()) updatedAt DateTime @updatedAt }`
- `Company` gains: `qboCustomerId String?`
- Back-relations to add on existing models: `Ticket.timeEntries TimeEntry[]`, `User.timeEntries TimeEntry[]`, `Contract.timeEntries TimeEntry[]`, `Contract.invoices Invoice[]`, `Company.invoices Invoice[]`.
- `src/lib/permissions.ts`: `Permission` union gains `| "timeentry:manage" | "invoice:view" | "invoice:manage" | "invoice:push_qbo" | "qbo:manage"`. `ROLE_PERMISSIONS` gains `"timeentry:manage"` for technician, dispatcher, admin (NOT sales, NOT finance); `"invoice:view"`, `"invoice:manage"`, `"invoice:push_qbo"` for finance, admin ONLY (NOT technician, dispatcher, sales); `"qbo:manage"` for admin ONLY. Export `export const TIME_ENTRY_MANAGE_ROLES: Role[] = ["technician", "dispatcher", "admin"];`, `export const INVOICE_MANAGE_ROLES: Role[] = ["finance", "admin"];`, `export const QBO_MANAGE_ROLES: Role[] = ["admin"];` below `ROLE_PERMISSIONS`, following the exact `TICKET_MANAGE_ROLES` pattern already in the file.
- Raw SQL migration addition: after `npx prisma migrate dev --name add_time_tracking_billing` generates the migration's `.sql` file under `prisma/migrations/<timestamp>_add_time_tracking_billing/migration.sql` AND applies it, append this exact statement to the end of that same file (for documentation/history purposes, so the migration directory reflects the full schema change even though it will not be re-run from this file): `CREATE UNIQUE INDEX "TimeEntry_one_active_timer_per_user" ON "TimeEntry" ("userId") WHERE "endedAt" IS NULL;`. Do NOT attempt to "re-run" `npx prisma migrate dev` or use `npx prisma db execute --file` to apply this appended line -- neither is a real capability for retroactively applying part of an already-recorded migration (re-running `migrate dev` no-ops once the migration is recorded in `_prisma_migrations`, and `db execute --file` has no line-scoping and no `--schema` flag in this project's Prisma 7 CLI). Instead, apply the index directly and exclusively via `docker compose exec db psql -U <db-user-from-DATABASE_URL> -d <db-name-from-DATABASE_URL> -c 'CREATE UNIQUE INDEX "TimeEntry_one_active_timer_per_user" ON "TimeEntry" ("userId") WHERE "endedAt" IS NULL;'` -- read the actual user/database name out of the `DATABASE_URL` value in `.env` (do not guess). This is the ONE documented method; there is no fallback chain. If this exact `psql` command reports `relation "TimeEntry_one_active_timer_per_user" already exists`, treat that as SUCCESS (the index already exists from a prior run of this same task, e.g. after a recovery/retry) -- verify its existence with the query below rather than treating the "already exists" error as a failure. After running it (or confirming it already exists), verify the index is present on the live database with `docker compose exec db psql -U <user> -d <database> -c "SELECT indexname FROM pg_indexes WHERE tablename = 'TimeEntry';"` and confirm `TimeEntry_one_active_timer_per_user` appears in the output -- this direct database query is the ONLY acceptable evidence that Task 2 is complete; the presence of the `CREATE UNIQUE INDEX` text in the migration file is NOT sufficient evidence on its own and must never be treated as proof the index exists on the running database.
Edge/error cases:
- If `docker compose ps db` shows the `db` service is not running, start it with `docker compose up -d db` before running `prisma migrate dev` -- do not skip the migration or fake success.
- If `npx prisma migrate dev` fails because the database already has a conflicting migration state, read the error output fully and report the exact error in the result -- do not run `prisma migrate reset` (destructive) without explicit escalation.
- If Task 1's schema change causes Prisma to generate a migration that makes `TimeEntry.userId` nullable in the actual SQL despite the schema declaring it as required (or if applying the migration produces a Postgres-level error about `NOT NULL` combined with `ON DELETE SET NULL`), treat this exactly as the documented `prisma validate`-time `userId` nullability tension -- switch `userId`/`user` to `String?`/`User?` in the schema, regenerate the migration, and document the decision. Do not let this surface only as a silent SQL difference; inspect the generated migration SQL for the `TimeEntry` table's `userId` column definition before considering Task 2 complete.
- `TimeEntry.contractId`/`invoiceLineItemId`, `Invoice.contractId`/`qboInvoiceId`/`qboPushedAt`, `Company.qboCustomerId` are all nullable by design -- do not make them required or add defaults.
- The partial unique index must target exactly `("userId") WHERE "endedAt" IS NULL` -- do not index on any other column combination, and do not use a full (non-partial) unique index, which would incorrectly prevent a user from ever having more than one stopped timer in history.
Verification criteria:
- `npx prisma validate` exits 0
- `npx tsc --noEmit` exits 0
- TimeEntry, Invoice, InvoiceLineItem, QuickBooksConnection models plus InvoiceStatus enum present in the schema file
- Migration applied (`npx prisma migrate status` shows no pending migrations)
- The partial unique index exists on the running database (verified via a direct query, not just presence in the migration file)
- timeentry:manage/invoice:view/invoice:manage/invoice:push_qbo/qbo:manage present in `src/lib/permissions.ts` with correct per-role grants
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made (including which `TimeEntry.userId` nullability variant was used, if the edge case above was triggered)
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `prisma/schema.prisma` does not match this plan's assumed current state (e.g. Ticket lacks `contractId`, or TimeEntry/Invoice already exist with a different shape).
- `docker compose` is not available or the `db` service cannot be started (missing Docker Desktop, port conflict not resolvable without user input).
- `npx prisma migrate dev` fails for a reason other than the database not yet running, after one retry.
- The raw SQL partial unique index cannot be applied to the running database by any of the documented methods (migrate dev re-run, `prisma db execute`, or direct `psql` via `docker compose exec`) -- report the exact error rather than silently proceeding without the constraint.
- `src/lib/permissions.ts`'s current `Permission` union or `ROLE_PERMISSIONS` shape differs from what this plan assumes, in a way that makes the described extension ambiguous.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, `.planning/phases/04-time-tracking-billing/04-CONTEXT.md`, and any `04-01-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx prisma validate` and `npx tsc --noEmit` before continuing.
5. Verify the partial unique index still exists on the running database before considering Task 2 complete (a `docker compose down`/restart between sessions does not drop it, but confirm rather than assume).
6. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Add TimeEntry/Invoice/InvoiceLineItem/QuickBooksConnection models, InvoiceStatus enum, Company.qboCustomerId, and back-relations to Prisma schema</name>
  <files>prisma/schema.prisma</files>
  <action>
Read `prisma/schema.prisma` in full first. Confirm `Ticket`, `TicketComment`, `Contract`, `Company` exist as Phase 3 left them. If it differs from these assumptions, emit BLOCKED.

Append `enum InvoiceStatus`, `model TimeEntry`, `model Invoice`, `model InvoiceLineItem`, `model QuickBooksConnection` to the end of the file using the exact field shapes given in this plan's `<execution_contract>` "Required interfaces/content structure" section. Add `qboCustomerId String?` to the existing `Company` model.

Then edit `Ticket`, `User`, `Contract`, `Company` to add their respective back-relation fields as listed in "Required interfaces/content structure" (`Ticket.timeEntries TimeEntry[]`, `User.timeEntries TimeEntry[]`, `Contract.timeEntries TimeEntry[]`, `Contract.invoices Invoice[]`, `Company.invoices Invoice[]`). Do not remove or reorder any existing fields on these models.

If `npx prisma validate` rejects `TimeEntry.userId`'s required-field + `onDelete: SetNull` combination, switch `userId`/`user` to nullable (`String?`/`User?`) as documented in the execution_contract's required-interfaces note, and record this decision for the SUMMARY.

> verification: grep -q 'model TimeEntry' prisma/schema.prisma
> verification: grep -q 'model Invoice ' prisma/schema.prisma
> verification: grep -q 'model InvoiceLineItem' prisma/schema.prisma
> verification: grep -q 'model QuickBooksConnection' prisma/schema.prisma
> verification: grep -q 'enum InvoiceStatus' prisma/schema.prisma
> verification: grep -q 'qboCustomerId' prisma/schema.prisma
> verification: npx prisma validate
  </action>
  <verify>
grep -q 'model TimeEntry' prisma/schema.prisma
grep -q 'model Invoice ' prisma/schema.prisma
grep -q 'model InvoiceLineItem' prisma/schema.prisma
grep -q 'model QuickBooksConnection' prisma/schema.prisma
grep -q 'enum InvoiceStatus' prisma/schema.prisma
grep -q 'qboCustomerId' prisma/schema.prisma
npx prisma validate
  </verify>
  <done>prisma/schema.prisma contains TimeEntry, Invoice, InvoiceLineItem, QuickBooksConnection, InvoiceStatus, Company.qboCustomerId, and all 5 back-relations, and validates cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and apply the migration, including the raw SQL partial unique index</name>
  <files>prisma/migrations/**</files>
  <action>
Check whether the `db` Docker service is running with `docker compose ps db`. If not running, start it with `docker compose up -d db` and wait for it to become healthy.

Run `npx prisma migrate dev --name add_time_tracking_billing`. This generates a new migration directory under `prisma/migrations/` and applies it.

Open the generated `migration.sql` file and append exactly this statement at the end, for documentation purposes: `CREATE UNIQUE INDEX "TimeEntry_one_active_timer_per_user" ON "TimeEntry" ("userId") WHERE "endedAt" IS NULL;`. Do NOT attempt to re-run `prisma migrate dev` or `prisma db execute --file` to apply this line -- neither retroactively applies part of an already-recorded migration in this Prisma version.

Read the actual database user and database name out of the `DATABASE_URL` value in `.env` (do not guess or assume default values). Apply the index directly via exactly this command: `docker compose exec db psql -U <db-user> -d <db-name> -c 'CREATE UNIQUE INDEX "TimeEntry_one_active_timer_per_user" ON "TimeEntry" ("userId") WHERE "endedAt" IS NULL;'`. If this reports the index/relation already exists, treat that as success (not a failure) and proceed directly to verification.

Verify the index exists on the live database with: `docker compose exec db psql -U <db-user> -d <db-name> -c "SELECT indexname FROM pg_indexes WHERE tablename = 'TimeEntry';"` and confirm `TimeEntry_one_active_timer_per_user` appears in the output. This direct query result is the ONLY acceptable evidence the index exists -- the migration file containing the `CREATE UNIQUE INDEX` text is necessary but not sufficient evidence, since that text alone does not prove the statement was ever executed against the running database.

Run `npx prisma generate` to regenerate the Prisma Client with the new model types.

If any step fails for a reason other than the database not yet running, or if the `psql` command fails with any error other than "already exists", stop and report the exact error as a BLOCKED escalation rather than working around it with `prisma migrate reset` or silently skipping the index.

> verification: npx prisma migrate status
> verification: test -d prisma/migrations
> verification: grep -rq 'one_active_timer_per_user' prisma/migrations/
> verification: docker compose exec -T db psql -U "$DB_USER" -d "$DB_NAME" -t -c "SELECT indexname FROM pg_indexes WHERE tablename = 'TimeEntry';" | grep -q 'TimeEntry_one_active_timer_per_user'
  </action>
  <verify>
npx prisma migrate status
test -d prisma/migrations
grep -rq 'one_active_timer_per_user' prisma/migrations/
docker compose exec -T db psql -U "$DB_USER" -d "$DB_NAME" -t -c "SELECT indexname FROM pg_indexes WHERE tablename = 'TimeEntry';" | grep -q 'TimeEntry_one_active_timer_per_user'
  </verify>
  <done>A new migration exists under prisma/migrations/ containing the raw SQL partial unique index statement, the index has been applied to the running database via a direct psql command (not a re-run migration), its existence is confirmed via a direct pg_indexes query (not just file text), and prisma migrate status reports no pending migrations.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the permission matrix with time entry, invoice, and QBO permissions</name>
  <files>src/lib/permissions.ts</files>
  <action>
Read `src/lib/permissions.ts` in full first. Confirm the current `Permission` type includes the 7 Phase 1-3 permissions and `TICKET_MANAGE_ROLES`/`TICKET_ASSIGN_ROLES` are exported. If it differs, emit BLOCKED.

Edit the `Permission` type to add `| "timeentry:manage" | "invoice:view" | "invoice:manage" | "invoice:push_qbo" | "qbo:manage"`.

Edit `ROLE_PERMISSIONS` so: `"timeentry:manage"` is added to technician, dispatcher, admin ONLY (NOT sales, NOT finance); `"invoice:view"`, `"invoice:manage"`, `"invoice:push_qbo"` are added to finance, admin ONLY (NOT technician, dispatcher, sales); `"qbo:manage"` is added to admin ONLY. Preserve all existing permission entries unchanged -- only add the five new permission strings to the appropriate roles' arrays.

Add three new named exports below `ROLE_PERMISSIONS`, following the exact pattern of `TICKET_MANAGE_ROLES`: `export const TIME_ENTRY_MANAGE_ROLES: Role[] = ["technician", "dispatcher", "admin"];`, `export const INVOICE_MANAGE_ROLES: Role[] = ["finance", "admin"];`, `export const QBO_MANAGE_ROLES: Role[] = ["admin"];`. All three must be kept in sync with what `ROLE_PERMISSIONS` grants above.

Do not change the `can()` function's implementation or signature.

> verification: grep -q 'timeentry:manage' src/lib/permissions.ts
> verification: grep -q 'invoice:view' src/lib/permissions.ts
> verification: grep -q 'invoice:manage' src/lib/permissions.ts
> verification: grep -q 'invoice:push_qbo' src/lib/permissions.ts
> verification: grep -q 'qbo:manage' src/lib/permissions.ts
> verification: grep -q 'TIME_ENTRY_MANAGE_ROLES' src/lib/permissions.ts
> verification: grep -q 'INVOICE_MANAGE_ROLES' src/lib/permissions.ts
> verification: grep -q 'QBO_MANAGE_ROLES' src/lib/permissions.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q 'timeentry:manage' src/lib/permissions.ts
grep -q 'invoice:view' src/lib/permissions.ts
grep -q 'invoice:manage' src/lib/permissions.ts
grep -q 'invoice:push_qbo' src/lib/permissions.ts
grep -q 'qbo:manage' src/lib/permissions.ts
grep -q 'TIME_ENTRY_MANAGE_ROLES' src/lib/permissions.ts
grep -q 'INVOICE_MANAGE_ROLES' src/lib/permissions.ts
grep -q 'QBO_MANAGE_ROLES' src/lib/permissions.ts
npx tsc --noEmit
  </verify>
  <done>src/lib/permissions.ts grants timeentry:manage to technician/dispatcher/admin, invoice:view/invoice:manage/invoice:push_qbo to finance/admin, qbo:manage to admin only, exports all 3 new role-array constants, and still type-checks cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` exits 0
- [ ] `npx prisma migrate status` shows no pending migrations
- [ ] `npx tsc --noEmit` exits 0
- [ ] TimeEntry, Invoice, InvoiceLineItem, QuickBooksConnection, InvoiceStatus, Company.qboCustomerId present in prisma/schema.prisma; all 5 back-relations added
- [ ] The raw SQL partial unique index exists on the running database, confirmed via direct query (not just file presence)
- [ ] timeentry:manage/invoice:view/invoice:manage/invoice:push_qbo/qbo:manage present in src/lib/permissions.ts with correct per-role grants
- [ ] TIME_ENTRY_MANAGE_ROLES/INVOICE_MANAGE_ROLES/QBO_MANAGE_ROLES exported and match the ROLE_PERMISSIONS grants exactly
- [ ] No files outside files_modified were touched (`git diff --stat` confirms)
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- prisma/schema.prisma defines TimeEntry, Invoice, InvoiceLineItem, QuickBooksConnection, InvoiceStatus, and Company.qboCustomerId exactly as specified, with all 5 back-relations
- A migration has been generated and applied against the running Postgres database, including a real database-level partial unique index enforcing one active timer per user
- src/lib/permissions.ts's Permission union and ROLE_PERMISSIONS matrix correctly grant the 5 new permissions per role, and export TIME_ENTRY_MANAGE_ROLES/INVOICE_MANAGE_ROLES/QBO_MANAGE_ROLES matching those grants
- The existing Role/BillingType/TicketStatus/TicketPriority/TicketSource enums, Phase 1-3 models, and prior permissions are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-time-tracking-billing/04-01-SUMMARY.md` documenting what was done, verification results, decisions (including the userId nullability decision if triggered and which method was used to apply the partial index), and any deviations.
</output>
