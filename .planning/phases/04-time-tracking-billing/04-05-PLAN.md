---
phase: 04-time-tracking-billing
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/lib/actions/invoices.ts
  - src/lib/validations/invoice.ts
  - src/app/(dashboard)/invoices/page.tsx
  - "src/app/(dashboard)/invoices/[invoiceId]/page.tsx"
  - src/components/invoices/generate-invoice-form.tsx
  - src/components/invoices/invoice-line-table.tsx
  - src/components/invoices/invoice-status-badge.tsx
  - src/components/nav/app-sidebar.tsx
files_forbidden:
  - prisma/schema.prisma
  - prisma/migrations/**
  - src/lib/permissions.ts
  - src/lib/billing.ts
  - src/lib/timer.ts
  - src/lib/sla.ts
  - src/lib/qbo.ts
  - src/lib/actions/time-entries.ts
  - src/lib/actions/qbo-connection.ts
  - src/lib/validations/time-entry.ts
  - src/components/tickets/**
  - "src/app/(dashboard)/tickets/**"
  - "src/app/(dashboard)/admin/**"
  - src/app/api/qbo/**
  - scripts/**
  - docker-compose.yml
  - package.json
sequential_files: []
expected_artifacts:
  - path: "src/lib/actions/invoices.ts"
    provides: "generateInvoice and finalizeInvoice Server Actions"
    required: true
  - path: "src/lib/validations/invoice.ts"
    provides: "zod schema for invoice generation date-range input"
    required: true
  - path: "src/app/(dashboard)/invoices/page.tsx"
    provides: "Invoice list + generate-invoice form"
    required: true
  - path: "src/app/(dashboard)/invoices/[invoiceId]/page.tsx"
    provides: "Invoice detail page with line items and status (QBO push button added later by Plan 04-06)"
    required: true
autonomous: false
agents: ["engineering-backend-architect", "engineering-frontend-developer"]
requirements: ["Invoice generation from logged time and contract terms", "Billing rules correctly compute charges for block-hour, flat-fee managed services, and hourly break-fix contract types"]
user_setup: []
verification_commands:
  - "grep -q 'export async function generateInvoice' src/lib/actions/invoices.ts && grep -q 'export async function finalizeInvoice' src/lib/actions/invoices.ts"
  - "grep -q 'requireRole(INVOICE_MANAGE_ROLES)' src/lib/actions/invoices.ts"
  - "grep -q 'computeContractCharges' src/lib/actions/invoices.ts"
  - "grep -q 'toNumber' src/lib/actions/invoices.ts"
  - "test -f 'src/app/(dashboard)/invoices/page.tsx'"
  - "test -f 'src/app/(dashboard)/invoices/[invoiceId]/page.tsx'"
  - "grep -q 'invoice:view' 'src/components/nav/app-sidebar.tsx'"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "generateInvoice queries all uninvoiced billable TimeEntry rows for the company/contract in the given date range, calls computeContractCharges from src/lib/billing.ts with a correctly-summed priorInvoicedBillableMinutes value queried from historical invoiced entries against the same contractId, and persists Invoice + InvoiceLineItem in one db.$transaction."
    - "generateInvoice stamps invoiceLineItemId back onto every TimeEntry consumed by the new invoice, preventing double-billing on a subsequent invoice run."
    - "The block-hour cumulative rule is correctly wired: priorInvoicedBillableMinutes sums durationMinutes across ALL TimeEntry rows ever invoiced against the contract (via existing invoiceLineItemId links), not just the current invoice's date range."
    - "generateInvoice re-checks invoiceLineItemId IS NULL on the exact consumed entry ids inside the same transaction before stamping, and verifies the updateMany row count matches the expected count, rolling back and returning a retry error if a concurrent invoice run has already consumed any of the same entries."
    - "All Decimal-typed Contract fields (hourlyRate, flatFeeAmount) are converted to plain numbers via .toNumber() before being passed into computeContractCharges -- never passed through as raw Decimal objects."
    - "Every mutating invoices Server Action calls requireRole(INVOICE_MANAGE_ROLES) as its first line."
    - "The invoice detail page renders line items and status but does NOT yet include a QuickBooks push button or call any qbo.ts function -- that is exclusively Plan 04-06's addition."
  artifacts:
    - path: "src/lib/actions/invoices.ts"
      provides: "generateInvoice and finalizeInvoice Server Actions implementing cumulative block-hour billing"
      min_lines: 60
      contains: "priorInvoicedBillableMinutes"
    - path: "src/app/(dashboard)/invoices/[invoiceId]/page.tsx"
      provides: "Invoice detail page rendering line items and status"
      min_lines: 20
      contains: "InvoiceLineTable"
  key_links:
    - from: "src/lib/actions/invoices.ts"
      to: "src/lib/billing.ts"
      via: "generateInvoice imports and calls computeContractCharges to compute each contract-type's charge"
      pattern: "computeContractCharges"
    - from: "src/lib/actions/invoices.ts"
      to: "src/lib/actions/time-entries.ts"
      via: "generateInvoice queries TimeEntry rows created by Plan 04-03's startTimer/stopTimer, filtering on isBillable and invoiceLineItemId: null"
      pattern: "invoiceLineItemId"
---

<objective>
Implement invoice generation: a Server Action that queries uninvoiced billable time entries for a company/date range, computes charges per the contract's billing type (including the cumulative-lifetime block-hour rule) via `src/lib/billing.ts`, persists an `Invoice` + `InvoiceLineItem` records, and marks the consumed `TimeEntry` rows as invoiced -- plus the invoice list/detail UI and a sidebar nav entry.

Purpose: Delivers the phase's third ROADMAP.md success criterion (invoices generated from a date range of logged time per client) with billing-rule correctness as the primary risk this plan must get right, since Plans 04-01/04-02 already locked in the schema and pure computation logic this plan wires together.
Output: New `src/lib/actions/invoices.ts`, `src/lib/validations/invoice.ts`, `src/app/(dashboard)/invoices/page.tsx`, `src/app/(dashboard)/invoices/[invoiceId]/page.tsx`, 3 new components; modified `src/components/nav/app-sidebar.tsx`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-time-tracking-billing/04-CONTEXT.md
@.planning/phases/04-time-tracking-billing/04-01-SUMMARY.md
@.planning/phases/04-time-tracking-billing/04-02-SUMMARY.md
@.planning/phases/04-time-tracking-billing/04-03-SUMMARY.md

Relevant source files:
@prisma/schema.prisma
@src/lib/permissions.ts
@src/lib/billing.ts
@src/lib/actions/time-entries.ts
@src/lib/actions/tickets.ts
@src/lib/validations/ticket.ts
@src/app/(dashboard)/clients/[companyId]/page.tsx
@src/components/nav/app-sidebar.tsx
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-backend-architect (invoice generation logic, cumulative block-hour query) + engineering-frontend-developer (invoice list/detail UI)
Task: Implement `generateInvoice`/`finalizeInvoice` Server Actions with correct cumulative-lifetime block-hour computation, their zod validation, and the invoice list/detail pages plus a sidebar nav entry.
Scope:
- Read targets: `prisma/schema.prisma`, `src/lib/permissions.ts`, `src/lib/billing.ts`, `src/lib/actions/time-entries.ts` (to confirm actual `TimeEntry` field usage from 04-03), `src/lib/actions/tickets.ts` and `src/lib/validations/ticket.ts` (Server Action/validation pattern reference), `src/app/(dashboard)/clients/[companyId]/page.tsx` (list/detail page pattern reference), `src/components/nav/app-sidebar.tsx`, all 3 prior SUMMARY.md files
- Write targets: `src/lib/actions/invoices.ts` (new), `src/lib/validations/invoice.ts` (new), `src/app/(dashboard)/invoices/page.tsx` (new), `src/app/(dashboard)/invoices/[invoiceId]/page.tsx` (new), `src/components/invoices/generate-invoice-form.tsx` (new), `src/components/invoices/invoice-line-table.tsx` (new), `src/components/invoices/invoice-status-badge.tsx` (new), `src/components/nav/app-sidebar.tsx` (modify -- add one new `<li>` only)
- Forbidden targets: `prisma/schema.prisma`, `prisma/migrations/**`, `src/lib/permissions.ts`, `src/lib/billing.ts` (read-only), `src/lib/timer.ts`, `src/lib/sla.ts`, `src/lib/qbo.ts`, `src/lib/actions/time-entries.ts` (read-only), `src/lib/actions/qbo-connection.ts`, `src/lib/validations/time-entry.ts`, `src/components/tickets/**`, `src/app/(dashboard)/tickets/**`, `src/app/(dashboard)/admin/**`, `src/app/api/qbo/**`, `scripts/**`, `docker-compose.yml`, `package.json`
Allowed tools/actions:
- Read, Edit, Write on the write targets listed above
- Bash: `npx tsc --noEmit`
Forbidden actions:
- Do not modify `src/lib/billing.ts` -- import and call `computeContractCharges`. If its signature differs from what 04-02's SUMMARY documents, emit BLOCKED rather than reimplementing billing math inline in this plan.
- Do not add any QuickBooks-related code, import `src/lib/qbo.ts`, or add a "Push to QuickBooks" button -- the invoice detail page this plan creates must render a placeholder or simply omit that action; Plan 04-06 is solely responsible for adding it.
- Do not implement a per-invoice-period block-hour reset -- `priorInvoicedBillableMinutes` MUST be computed by querying the sum of `durationMinutes` across every `TimeEntry` row already linked (via `invoiceLineItemId`) to a prior `InvoiceLineItem` belonging to an `Invoice` with the SAME `contractId`, regardless of that invoice's date range.
- Do not implement tax or discount logic -- `Invoice.subtotal` and `Invoice.total` must be set to the same computed value.
- Do not touch any part of `src/components/nav/app-sidebar.tsx` other than adding one new `<li>` for Invoices.
- Do not self-defer planned work.
Implementation sequence:
1. Read all context files and prior SUMMARY.md files to confirm actual field names/signatures from 04-01/04-02/04-03's real output.
2. Create `src/lib/validations/invoice.ts` (Task 1).
3. Create `src/lib/actions/invoices.ts` with the cumulative-block-hour-aware `generateInvoice` and `finalizeInvoice` (Task 1).
4. Create the 3 invoice components (Task 2).
5. Create the invoice list and detail pages (Task 2).
6. Add the sidebar Invoices nav entry (Task 3).
Required interfaces/content structure:
- `src/lib/validations/invoice.ts`: `export const generateInvoiceSchema = z.object({ companyId: z.string().min(1), periodStart: z.coerce.date(), periodEnd: z.coerce.date() }).refine((data) => data.periodEnd >= data.periodStart, { message: "Period end must be on or after period start", path: ["periodEnd"] });` `export type GenerateInvoiceInput = z.infer<typeof generateInvoiceSchema>;`
- `src/lib/actions/invoices.ts`: `"use server"`. `export async function generateInvoice(formData: FormData)` -- `requireRole(INVOICE_MANAGE_ROLES)` first. Validates via `generateInvoiceSchema.safeParse`. Resolves the company's active contract using the SAME deterministic rule already locked in `03-CONTEXT.md`/implemented in `tickets.ts`'s `resolveActiveContract` (re-derive the identical query here since this is a different file -- among the company's contracts where `endDate IS NULL OR endDate >= now()`, order by `startDate DESC, id DESC`, take the first row; if the company has no active contract, return `{ error: "Company has no active contract" }`).

  Query `db.timeEntry.findMany({ where: { contractId: contract.id, isBillable: true, invoiceLineItemId: null, endedAt: { not: null }, startedAt: { gte: periodStart, lte: periodEnd } } })` for the current period's billable, uninvoiced, stopped entries. Sum their `durationMinutes` as `currentPeriodBillableMinutes` and collect their ids as `consumedEntryIds`.

  Add a code comment directly above the `priorInvoicedBillableMinutes` query stating exactly this: "ASSUMPTION (locked in 04-CONTEXT.md's Contract deletion policy): this cumulative sum is only correct if Contracts with billing history are never deleted, only updated in place or superseded by a new Contract row for renewals. If a Contract is deleted after being invoiced against, its TimeEntry/Invoice rows' contractId is set to null (onDelete: SetNull), silently removing that history from this sum. This function does not and cannot enforce that policy -- it is an operational assumption, not a code invariant." Then query `priorInvoicedBillableMinutes` as the sum of `durationMinutes` across all `TimeEntry` rows where `contractId` equals this contract's id AND `invoiceLineItemId IS NOT NULL` (already invoiced, from any prior invoice run against this contract, regardless of date) -- use `db.timeEntry.aggregate({ where: { contractId: contract.id, invoiceLineItemId: { not: null } }, _sum: { durationMinutes: true } })`.

  Before calling `computeContractCharges`, convert every `Decimal`-typed Contract field to a plain number: `hourlyRate: contract.hourlyRate !== null ? contract.hourlyRate.toNumber() : null`, `flatFeeAmount: contract.flatFeeAmount !== null ? contract.flatFeeAmount.toNumber() : null`, `blockHours` is already a plain `Int` column and needs no conversion. Never pass a raw Prisma `Decimal` object into `computeContractCharges` -- its arithmetic assumes plain JavaScript numbers, and passing a `Decimal` object through unconverted (e.g. via `contract.hourlyRate ?? 0`) produces silently wrong results since a `Decimal` object is truthy and passes the `??` check without being numerically usable.

  For a `block_hour` contract, if `contract.blockHours === null || contract.hourlyRate === null` (after the conversion above), return `{ error: "Contract is misconfigured for block-hour billing: blockHours and hourlyRate must both be set." }` instead of proceeding -- do not silently generate a $0 overage line item for a misconfigured contract, per the plan-critique finding that this would be an invisible revenue leak.

  Calls `computeContractCharges(contractBillingInput, currentPeriodBillableMinutes, priorInvoicedBillableMinutes)` from `@/lib/billing`. If the resulting charge's `amount` is 0 AND there are zero current-period time entries, still allow invoice creation (a flat-fee contract bills every period regardless of hours) but if a non-flat-fee contract has zero current-period entries AND zero computed amount, return `{ error: "No billable time entries found in this date range" }` instead of creating an empty invoice.

  Inside a single `db.$transaction` (using the callback form, `db.$transaction(async (tx) => { ... })`, not the array form, so intermediate results can gate later steps): (1) create the `Invoice` (status: `draft`, `subtotal`/`total` both set to `Decimal` values derived from the computed `amount`, e.g. `new Prisma.Decimal(amount)` or the equivalent this project's Prisma version expects for writing a Decimal field from a number -- check `src/lib/actions/contracts.ts` or similar existing Decimal-writing code for the established conversion pattern, and if none exists, writing a plain `number` to a `Decimal` field is Prisma's standard supported input type, so this is expected to just work), (2) create one `InvoiceLineItem` from the `BillingChargeResult`, (3) **immediately before stamping**, re-query (within the same `tx`) `tx.timeEntry.findMany({ where: { id: { in: consumedEntryIds }, invoiceLineItemId: null } })` and compare the returned count to `consumedEntryIds.length` -- if they differ, another concurrent `generateInvoice` call has already consumed one or more of these entries since the initial query; throw an error inside the transaction callback (causing Prisma to roll back the whole transaction, including the Invoice/InvoiceLineItem just created) and return `{ error: "Some time entries in this period were invoiced by a concurrent request. Please retry." }` rather than proceeding, (4) if the re-check passes, `tx.timeEntry.updateMany({ where: { id: { in: consumedEntryIds }, invoiceLineItemId: null }, data: { invoiceLineItemId: newLineItem.id } })` and verify the returned `count` equals `consumedEntryIds.length` as a final defense-in-depth check (if it doesn't match even after the re-check passed, throw to roll back -- this covers the narrow race window between the re-check read and this write, which the `WHERE invoiceLineItemId: null` clause on the update itself already closes at the database level, making this update's own row-count check the authoritative guard). Redirect to `/invoices/{invoice.id}` on success. `export async function finalizeInvoice(invoiceId: string)` -- `requireRole(INVOICE_MANAGE_ROLES)`, updates `status: "finalized"` only if current status is `"draft"` (return `{ error: "Only draft invoices can be finalized" }` otherwise), handles P2025, `revalidatePath`.
- `src/app/(dashboard)/invoices/page.tsx`: async Server Component, gated `can(user.role, "invoice:view")` (redirect `/unauthorized` otherwise). Self-fetches all invoices ordered `createdAt desc` with company name. Renders a table (empty-state `colSpan` set correctly) and, gated `invoice:manage`, the `GenerateInvoiceForm` (company select + 2 date inputs).
- `src/app/(dashboard)/invoices/[invoiceId]/page.tsx`: async Server Component, gated `can(user.role, "invoice:view")`. Self-fetches the invoice with `lineItems` and `company`. Renders `InvoiceStatusBadge`, `InvoiceLineTable`, and (gated `invoice:manage`, only when `status === "draft"`) a "Finalize" button calling `finalizeInvoice`. Does NOT render any QuickBooks-related UI -- leave a comment noting Plan 04-06 adds the push button here.
- `src/components/invoices/generate-invoice-form.tsx`: `"use client"`, company `<Select>` + 2 date `<Input type="date">` fields, submits to `generateInvoice`, follows `src/components/tickets/ticket-form.tsx`'s pending-state/error-display pattern.
- `src/components/invoices/invoice-line-table.tsx`: renders `InvoiceLineItem[]` as a table (description/quantity/unitRate/amount columns), empty-state colSpan set correctly if ever rendered with zero items (should not normally happen, but the component must not crash if it does).
- `src/components/invoices/invoice-status-badge.tsx`: renders `InvoiceStatus` as a `Badge` with distinct styling per status (draft/finalized/pushed), following `src/components/tickets/sla-badge.tsx`'s general shape (a small function taking the status and returning a styled `Badge`).
- `src/components/nav/app-sidebar.tsx`: add one new `<li>` for "Invoices" linking to `/invoices`, gated `{can(role, "invoice:view") && (...)}`, inserted after the existing Tickets `<li>` and before the Admin `<li>` block, matching the exact styling/structure of the Tickets/Clients list items.
Edge/error cases:
- If `generateInvoice` is called for a company/period where ALL time entries in range are already invoiced (zero uninvoiced entries) and the contract is not flat-fee, return the "No billable time entries found" error rather than creating a zero-line invoice.
- If the resolved contract is `flat_fee` billingType, `generateInvoice` must still create an invoice even with zero current-period time entries (flat-fee bills regardless of hours) -- do not apply the "no entries" rejection to flat-fee contracts.
- `generateInvoice`'s `db.$transaction` must be atomic: if the `TimeEntry.updateMany` stamping step fails after the Invoice/InvoiceLineItem are created, the whole transaction must roll back (rely on Prisma's `$transaction` callback semantics, not manual compensation).
- Two concurrent `generateInvoice` calls for overlapping periods/same contract must not both succeed in billing the same `TimeEntry` rows -- the transaction-scoped re-check (query `invoiceLineItemId: null` again inside the transaction, then update with the same `invoiceLineItemId: null` condition and verify the row count) is the mandatory guard against this; a plan-critique pre-mortem specifically identified this as a double-invoice risk that the original single-pass design did not close.
- A company with zero contracts at all must return `{ error: "Company has no active contract" }`, not throw.
- `finalizeInvoice` must reject finalizing an already-finalized or pushed invoice with a structured error, not silently no-op or throw.
Verification criteria:
- `npx tsc --noEmit` exits 0
- `generateInvoice` correctly queries and sums `priorInvoicedBillableMinutes` across ALL prior invoices for the same contract, not just the current period
- `generateInvoice` and `finalizeInvoice` both gated by `INVOICE_MANAGE_ROLES`
- Invoice list/detail pages render and are gated by `invoice:view`/`invoice:manage`
- No QuickBooks code or UI exists in this plan's output
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `src/lib/billing.ts`'s `computeContractCharges` signature differs from what 04-02's SUMMARY documents in a way that makes the cumulative block-hour wiring ambiguous.
- `TimeEntry`'s actual field names/types from 04-01's SUMMARY (e.g. if `userId` ended up nullable per that plan's documented edge case) conflict with this plan's query assumptions in a way requiring a design decision.
- The active-contract resolution rule cannot be re-derived identically to `tickets.ts`'s `resolveActiveContract` because that function's current implementation has changed from what 03-CONTEXT.md/04-CONTEXT.md describe.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, all 3 prior SUMMARY.md files, and any `04-05-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx tsc --noEmit` before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Create invoice validation and generateInvoice/finalizeInvoice Server Actions</name>
  <files>src/lib/validations/invoice.ts, src/lib/actions/invoices.ts</files>
  <action>
Read `src/lib/actions/tickets.ts` in full (especially `resolveActiveContract`), `src/lib/billing.ts`, `src/lib/actions/time-entries.ts`, and all 3 prior SUMMARY.md files to confirm actual field names and function signatures.

Create `src/lib/validations/invoice.ts` with `generateInvoiceSchema` as specified.

Create `src/lib/actions/invoices.ts` with `generateInvoice` and `finalizeInvoice` exactly as specified, implementing: the cumulative-lifetime `priorInvoicedBillableMinutes` query correctly (sum across ALL prior invoiced TimeEntry rows for the contract, not scoped to any date range) with the required assumption comment; the `.toNumber()` conversion of `Decimal` Contract fields before calling `computeContractCharges`; the block-hour misconfiguration guard; the flat-fee zero-entries exception; and the transaction-scoped double-billing guard (re-check `invoiceLineItemId: null` on the exact consumed entries inside the transaction, verify the `updateMany` row count matches, roll back with a retry error on mismatch).

> verification: test -f src/lib/validations/invoice.ts
> verification: test -f src/lib/actions/invoices.ts
> verification: grep -q 'export async function generateInvoice' src/lib/actions/invoices.ts
> verification: grep -q 'export async function finalizeInvoice' src/lib/actions/invoices.ts
> verification: grep -q 'requireRole(INVOICE_MANAGE_ROLES)' src/lib/actions/invoices.ts
> verification: grep -q 'computeContractCharges' src/lib/actions/invoices.ts
> verification: grep -q 'priorInvoicedBillableMinutes' src/lib/actions/invoices.ts
> verification: grep -q 'toNumber' src/lib/actions/invoices.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f src/lib/validations/invoice.ts
test -f src/lib/actions/invoices.ts
grep -q 'export async function generateInvoice' src/lib/actions/invoices.ts
grep -q 'export async function finalizeInvoice' src/lib/actions/invoices.ts
grep -q 'requireRole(INVOICE_MANAGE_ROLES)' src/lib/actions/invoices.ts
grep -q 'computeContractCharges' src/lib/actions/invoices.ts
grep -q 'priorInvoicedBillableMinutes' src/lib/actions/invoices.ts
grep -q 'toNumber' src/lib/actions/invoices.ts
npx tsc --noEmit
  </verify>
  <done>src/lib/actions/invoices.ts exports generateInvoice and finalizeInvoice, correctly implementing cumulative-lifetime block-hour billing via computeContractCharges with Decimal-to-number conversion, the transaction-scoped double-billing guard, gated by INVOICE_MANAGE_ROLES, and the project type-checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create invoice components and pages</name>
  <files>src/components/invoices/generate-invoice-form.tsx, src/components/invoices/invoice-line-table.tsx, src/components/invoices/invoice-status-badge.tsx, src/app/(dashboard)/invoices/page.tsx, src/app/(dashboard)/invoices/[invoiceId]/page.tsx</files>
  <action>
Read `src/app/(dashboard)/clients/[companyId]/page.tsx` and `src/components/tickets/sla-badge.tsx` for pattern reference.

Create the 3 components and 2 pages exactly as specified in this plan's `<execution_contract>`. The invoice detail page must NOT include any QuickBooks push UI -- leave a code comment noting Plan 04-06 adds it there.

> verification: test -f src/components/invoices/generate-invoice-form.tsx
> verification: test -f src/components/invoices/invoice-line-table.tsx
> verification: test -f src/components/invoices/invoice-status-badge.tsx
> verification: test -f 'src/app/(dashboard)/invoices/page.tsx'
> verification: test -f 'src/app/(dashboard)/invoices/[invoiceId]/page.tsx'
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f src/components/invoices/generate-invoice-form.tsx
test -f src/components/invoices/invoice-line-table.tsx
test -f src/components/invoices/invoice-status-badge.tsx
test -f 'src/app/(dashboard)/invoices/page.tsx'
test -f 'src/app/(dashboard)/invoices/[invoiceId]/page.tsx'
npx tsc --noEmit
  </verify>
  <done>All 3 components and 2 pages exist, correctly gated by invoice:view/invoice:manage, with no QuickBooks-related code, and the project type-checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Add Invoices nav entry to sidebar</name>
  <files>src/components/nav/app-sidebar.tsx</files>
  <action>
Read `src/components/nav/app-sidebar.tsx` in full. Add exactly one new `<li>` for "Invoices" linking to `/invoices`, gated `can(role, "invoice:view")`, matching the Tickets/Clients list items' exact structure and styling, inserted after Tickets and before the Admin block. Do not modify any other part of the file.

> verification: grep -q 'invoice:view' 'src/components/nav/app-sidebar.tsx'
> verification: grep -q '/invoices' 'src/components/nav/app-sidebar.tsx'
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q 'invoice:view' 'src/components/nav/app-sidebar.tsx'
grep -q '/invoices' 'src/components/nav/app-sidebar.tsx'
npx tsc --noEmit
  </verify>
  <done>The sidebar has exactly one new Invoices nav item gated by invoice:view, with no other changes to the file.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] generateInvoice correctly sums priorInvoicedBillableMinutes across ALL prior invoices for the contract (cumulative, not per-period)
- [ ] generateInvoice converts Decimal Contract fields via .toNumber() before calling computeContractCharges
- [ ] generateInvoice rejects block-hour contracts with null blockHours/hourlyRate rather than silently computing a $0 overage
- [ ] generateInvoice re-checks invoiceLineItemId IS NULL inside the transaction before stamping, and rolls back with a retry error if a concurrent request already consumed any of the same entries
- [ ] generateInvoice stamps invoiceLineItemId on every consumed TimeEntry within the same transaction as Invoice/InvoiceLineItem creation
- [ ] flat_fee contracts can generate an invoice even with zero current-period time entries; other contract types cannot
- [ ] finalizeInvoice only transitions draft -> finalized
- [ ] Invoice list/detail pages exist and are correctly role-gated
- [ ] No QuickBooks code exists anywhere in this plan's output
- [ ] Sidebar has exactly one new Invoices nav item
- [ ] No files outside files_modified were touched (`git diff --stat` confirms)
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Invoices can be generated from a date range of logged time per client (ROADMAP.md success criterion)
- Billing rules correctly compute charges for block-hour (cumulative lifetime), flat-fee, and hourly break-fix contract types (ROADMAP.md success criterion)
- Double-billing is structurally prevented via the invoiceLineItemId stamp
- The invoice detail page is ready for Plan 04-06 to add QBO push functionality without needing further changes to this plan's files
</success_criteria>

<output>
After completion, create `.planning/phases/04-time-tracking-billing/04-05-SUMMARY.md` documenting what was done, verification results, decisions, and any deviations.
</output>
