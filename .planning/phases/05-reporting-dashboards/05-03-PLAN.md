---
phase: 05-reporting-dashboards
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/(dashboard)/reports/sla/page.tsx
  - src/components/reports/sla-compliance-summary.tsx
  - src/components/reports/company-contract-filter.tsx
  - src/components/nav/app-sidebar.tsx
files_forbidden:
  - prisma/schema.prisma
  - prisma/migrations/**
  - src/lib/permissions.ts
  - src/lib/reporting.ts
  - src/lib/sla.ts
  - src/lib/billing.ts
  - src/lib/timer.ts
  - src/lib/actions/**
  - src/app/(dashboard)/reports/utilization/**
  - src/app/(dashboard)/reports/profitability/**
  - src/components/reports/date-range-filter.tsx
  - src/components/reports/utilization-table.tsx
  - package.json
sequential_files:
  - src/components/nav/app-sidebar.tsx
expected_artifacts:
  - path: "src/app/(dashboard)/reports/sla/page.tsx"
    provides: "SLA compliance report page, filterable by company/contract"
    required: true
  - path: "src/components/reports/sla-compliance-summary.tsx"
    provides: "Component rendering response/resolution compliance percentages"
    required: true
  - path: "src/components/reports/company-contract-filter.tsx"
    provides: "Shared client-component filter for company/contract, reused by the profitability report"
    required: true
autonomous: false
agents: ["engineering-frontend-developer"]
requirements: ["SLA compliance report shows % of tickets meeting response/resolution targets, filterable by client/contract"]
user_setup: []
verification_commands:
  - "test -f 'src/app/(dashboard)/reports/sla/page.tsx'"
  - "grep -q 'report:view_all' 'src/app/(dashboard)/reports/sla/page.tsx'"
  - "grep -q 'getSlaCompliance' 'src/app/(dashboard)/reports/sla/page.tsx'"
  - "test -f src/components/reports/sla-compliance-summary.tsx"
  - "test -f src/components/reports/company-contract-filter.tsx && grep -q '\"use client\"' src/components/reports/company-contract-filter.tsx"
  - "grep -q 'href=\"/reports/utilization\"' src/components/nav/app-sidebar.tsx"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "The page gates access with can(user.role, \"report:view_all\") only -- a technician or sales user is redirected, not shown a partial/empty report."
    - "A null responseCompliancePct/resolutionCompliancePct (no denominator) renders as an explicit \"No data\" state, never coerced to a misleading 0%."
    - "Company and contract filters are optional query params -- omitting both shows org-wide compliance across all tickets in the date range."
  artifacts:
    - path: "src/app/(dashboard)/reports/sla/page.tsx"
      provides: "Async Server Component rendering the SLA compliance report"
      min_lines: 25
      contains: "getSlaCompliance"
  key_links:
    - from: "src/app/(dashboard)/reports/sla/page.tsx"
      to: "src/lib/reporting.ts"
      via: "page imports and calls getSlaCompliance"
      pattern: "getSlaCompliance"
---

<objective>
Build the SLA compliance report: a new route showing % of tickets meeting response/resolution targets over a selectable date range, filterable by client/contract, restricted to dispatcher/finance/admin.

Purpose: Delivers "SLA compliance report shows % of tickets meeting response/resolution targets, filterable by client/contract" (ROADMAP.md success criterion).
Output: New `src/app/(dashboard)/reports/sla/page.tsx`, `src/components/reports/sla-compliance-summary.tsx`, `src/components/reports/company-contract-filter.tsx`; one append-only edit to `src/components/nav/app-sidebar.tsx` (only if 05-02 has not already added the Reports nav item -- see edge cases).
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-reporting-dashboards/05-CONTEXT.md
@.planning/phases/05-reporting-dashboards/05-01-SUMMARY.md

Relevant source files:
@src/lib/permissions.ts
@src/lib/reporting.ts
@src/lib/session.ts
@src/lib/sla.ts
@src/app/(dashboard)/invoices/page.tsx
@src/components/nav/app-sidebar.tsx
@prisma/schema.prisma
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-frontend-developer
Task: Build the SLA compliance report page, a compliance-summary component, and a shared company/contract filter component.
Scope:
- Read targets: `src/lib/permissions.ts`, `src/lib/reporting.ts`, `src/lib/session.ts`, `src/lib/sla.ts` (SLA status/terminology reference), `src/app/(dashboard)/invoices/page.tsx` (gated list-page pattern reference), `src/components/nav/app-sidebar.tsx`, `prisma/schema.prisma` (Company/Contract fields for the filter dropdown), `.planning/phases/05-reporting-dashboards/05-01-SUMMARY.md` (confirm actual `reporting.ts` exports/signatures), `.planning/phases/05-reporting-dashboards/05-02-SUMMARY.md` if it exists (confirm whether the sidebar's Reports item was already added)
- Write targets: `src/app/(dashboard)/reports/sla/page.tsx` (new), `src/components/reports/sla-compliance-summary.tsx` (new), `src/components/reports/company-contract-filter.tsx` (new), `src/components/nav/app-sidebar.tsx` (conditional append-only edit -- see edge cases)
- Forbidden targets: `prisma/schema.prisma`, `prisma/migrations/**`, `src/lib/permissions.ts`, `src/lib/reporting.ts` (read-only), `src/lib/sla.ts`, `src/lib/billing.ts`, `src/lib/timer.ts`, `src/lib/actions/**`, `src/app/(dashboard)/reports/utilization/**`, `src/app/(dashboard)/reports/profitability/**`, `src/components/reports/date-range-filter.tsx`, `src/components/reports/utilization-table.tsx`, `package.json`
Allowed tools/actions:
- Read, Edit, Write on the write targets listed above
- Bash: `npx tsc --noEmit`
Forbidden actions:
- Do not modify `src/lib/reporting.ts` or `src/lib/permissions.ts` -- import and use them, do not edit them. If any expected export is missing or has a different signature than documented, emit BLOCKED rather than reimplementing it locally.
- Do not build the utilization or profitability pages, or `src/components/reports/date-range-filter.tsx`/`utilization-table.tsx` -- those belong to Plans 05-02/05-04. If `date-range-filter.tsx` already exists (created by 05-02), import and reuse it rather than building a second date filter; if it does not exist yet at execution time, build a minimal local date-range control inline in this plan's page rather than creating a competing shared file, and note this in the SUMMARY.
- Do not add a charting library or any new dependency.
- Do not restructure or reformat any part of `app-sidebar.tsx` beyond the single conditional nav-item edit described in Task 3.
- Do not self-defer planned work.
Implementation sequence:
1. Read `05-01-SUMMARY.md` to confirm `getSlaCompliance`'s actual exported signature.
2. Read `src/lib/sla.ts` for existing SLA terminology/precedent to keep this report's language consistent with the ticket detail page's `SlaBadge`.
3. Create `src/components/reports/company-contract-filter.tsx` (Task 1) -- a shared, reusable `"use client"` component.
4. Create `src/components/reports/sla-compliance-summary.tsx` (Task 2).
5. Create `src/app/(dashboard)/reports/sla/page.tsx` (Task 2), wiring the filters and summary together, gated correctly.
6. Read the current `src/components/nav/app-sidebar.tsx`; if a "Reports" nav item pointing at `/reports/utilization` or `/reports/sla` already exists (added by 05-02), leave the file untouched. If it does not exist (05-02 has not yet run), add it exactly as 05-02 would (Task 3).
7. Run `npx tsc --noEmit`.
Required interfaces/content structure:
- `src/components/reports/company-contract-filter.tsx`: `"use client"`. Props: `{ companies: { id: string; name: string }[]; selectedCompanyId?: string; selectedContractId?: string; basePath: string }`. Renders a company `<select>` (using shadcn `Select`, with a `"none"` sentinel value per the codebase's established nullable-relation-select convention, stripped before navigating) and, if a company is selected, fetches nothing itself (contracts list is passed in or fetched by the parent Server Component and passed as a prop -- keep this component presentational). On change, navigates via `useRouter().push` preserving the existing `from`/`to` query params and updating `companyId`/`contractId`. Exported as `export function CompanyContractFilter(...)`.
- `src/components/reports/sla-compliance-summary.tsx`: Props: `{ result: SlaComplianceResult }` (import the type from `@/lib/reporting`). Renders two summary blocks (Response, Resolution), each showing met count, breached count, and compliance % -- when the corresponding `*CompliancePct` is `null`, render an explicit "No data in this range" message instead of "0%" or a blank value.
- `src/app/(dashboard)/reports/sla/page.tsx`: async Server Component. `export default async function SlaComplianceReportPage({ searchParams }: { searchParams: Promise<{ from?: string; to?: string; companyId?: string; contractId?: string }> })`. Calls `getCurrentUser()`; **first line: `if (!user) redirect("/login");`** (matching `src/app/(dashboard)/invoices/page.tsx`'s exact null-session-guard pattern -- must precede any `can()` call); then if `!can(user.role, "report:view_all")`, redirect to `/unauthorized`. Parses `from`/`to` from `searchParams` (awaited) as raw strings; if either is absent or fails a basic `YYYY-MM-DD` shape check, use `getCurrentMonthRange()` from `@/lib/reporting` instead (do NOT construct `Date` objects in this page -- pass raw strings straight through to `getSlaCompliance`, which owns all date-boundary parsing internally, per the locked timezone decision in `05-CONTEXT.md`). Parses optional `companyId`/`contractId`. Fetches the company list for the filter dropdown via a direct `db.company.findMany({ select: { id: true, name: true }, orderBy: { name: "asc" } })` call. Calls `getSlaCompliance(from, to, companyId, contractId)`. Renders the filter and `SlaComplianceSummary`.
Edge/error cases:
- A `report:view_own`-only user (technician, sales) hitting `/reports/sla` directly must be redirected to `/unauthorized`, not shown a degraded/empty version of the report.
- `companyId`/`contractId` query params referencing a non-existent id must not throw -- `getSlaCompliance` (per 05-01) simply returns a zero-row result in that case, which the page must render via the "No data" state, not crash.
- An invalid or malformed `from`/`to` query param must fall back to `getCurrentMonthRange()`'s default rather than being passed through as-is -- this page never constructs `Date` objects itself.
- Sidebar edit is CONDITIONAL: read the file first; only add the nav item if it is not already present (from 05-02). Document in the SUMMARY which case applied.
Verification criteria:
- `npx tsc --noEmit` exits 0
- Page redirects report:view_own-only users away from `/reports/sla`
- Null compliance percentages render as an explicit "No data" state, not 0% or blank
- No files outside `files_modified` were touched
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `src/lib/reporting.ts` does not export `getSlaCompliance` with the signature documented in `05-01-SUMMARY.md`.
- `src/lib/permissions.ts` does not export `report:view_all`-gated `can()` support as expected.
- `src/components/nav/app-sidebar.tsx`'s current structure has changed in a way that makes confirming/inserting the Reports nav item ambiguous.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, `05-01-SUMMARY.md`, `05-02-SUMMARY.md` if it exists, and any `05-03-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx tsc --noEmit` before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared company/contract filter component</name>
  <files>src/components/reports/company-contract-filter.tsx</files>
  <action>
Read an existing nullable-relation-select pattern (e.g. in a CRM form component) for the `"none"`-sentinel convention. Create `src/components/reports/company-contract-filter.tsx` as specified: a `"use client"` presentational component with a company select that navigates on change, preserving existing date-range query params.

> verification: test -f src/components/reports/company-contract-filter.tsx
> verification: grep -q '"use client"' src/components/reports/company-contract-filter.tsx
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f src/components/reports/company-contract-filter.tsx
grep -q '"use client"' src/components/reports/company-contract-filter.tsx
npx tsc --noEmit
  </verify>
  <done>CompanyContractFilter exists as a client component accepting a companies list and selected ids, navigating on change while preserving date params.</done>
</task>

<task type="auto">
  <name>Task 2: Create the SLA compliance summary and report page</name>
  <files>src/components/reports/sla-compliance-summary.tsx, src/app/(dashboard)/reports/sla/page.tsx</files>
  <action>
Read `05-01-SUMMARY.md` to confirm `getSlaCompliance`'s exact signature and the `SlaComplianceResult` shape. Read `src/lib/sla.ts` for terminology consistency.

Create `src/components/reports/sla-compliance-summary.tsx` rendering `SlaComplianceResult` with explicit "No data" handling for null percentages.

Create `src/app/(dashboard)/reports/sla/page.tsx` as an async Server Component: await `searchParams`, resolve the date range and optional company/contract filters, gate on `report:view_all`, fetch the company list, call `getSlaCompliance`, and render `CompanyContractFilter` + `SlaComplianceSummary`.

> verification: test -f src/components/reports/sla-compliance-summary.tsx
> verification: test -f 'src/app/(dashboard)/reports/sla/page.tsx'
> verification: grep -q 'report:view_all' 'src/app/(dashboard)/reports/sla/page.tsx'
> verification: grep -q 'getSlaCompliance' 'src/app/(dashboard)/reports/sla/page.tsx'
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f src/components/reports/sla-compliance-summary.tsx
test -f 'src/app/(dashboard)/reports/sla/page.tsx'
grep -q 'report:view_all' 'src/app/(dashboard)/reports/sla/page.tsx'
grep -q 'getSlaCompliance' 'src/app/(dashboard)/reports/sla/page.tsx'
npx tsc --noEmit
  </verify>
  <done>The SLA compliance report page renders correctly gated, filtered data through SlaComplianceSummary with explicit no-data handling, and the project type-checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm or add the Reports nav item</name>
  <files>src/components/nav/app-sidebar.tsx</files>
  <action>
Read the current `src/components/nav/app-sidebar.tsx` in full. Check: `grep -q 'href="/reports/utilization"' src/components/nav/app-sidebar.tsx` -- if this matches (added by Plan 05-02), leave the file completely untouched and note this in the SUMMARY. If it does not match, insert this EXACT block (verbatim, from `05-CONTEXT.md`'s locked "Sidebar nav single-source-of-truth" decision -- do not reconstruct it from memory or paraphrase it) after the Invoices `<li>` and before the Admin `<li>`:
```tsx
{can(role, "report:view_own") && (
  <li>
    <Link
      href="/reports/utilization"
      className="block rounded-md px-2 py-1.5 text-sm text-sidebar-foreground hover:bg-sidebar-accent hover:text-sidebar-accent-foreground"
    >
      Reports
    </Link>
  </li>
)}
```

> verification: grep -q 'href="/reports/utilization"' src/components/nav/app-sidebar.tsx
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q 'href="/reports/utilization"' src/components/nav/app-sidebar.tsx
npx tsc --noEmit
  </verify>
  <done>The sidebar has exactly one Reports nav entry using the exact locked markup (either pre-existing from 05-02 or added by this task), and the SUMMARY documents which case applied.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] SLA compliance page redirects report:view_own-only users
- [ ] Null compliance percentages render as explicit "No data", never 0% or blank
- [ ] Company/contract filters are optional and org-wide compliance shows when both are absent
- [ ] Sidebar has exactly one Reports nav entry (not duplicated)
- [ ] No files outside `files_modified` were touched (`git diff --stat` confirms)
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- SLA compliance report shows % of tickets meeting response/resolution targets, filterable by client/contract (ROADMAP.md success criterion)
- Dashboards respect role permissions -- restricted to dispatcher/finance/admin (ROADMAP.md success criterion)
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporting-dashboards/05-03-SUMMARY.md` documenting what was done, verification results, decisions made, and any deviations. State explicitly whether this plan found the sidebar's Reports nav item already present (from 05-02) or added it itself.
</output>
