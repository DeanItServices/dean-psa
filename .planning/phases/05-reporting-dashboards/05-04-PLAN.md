---
phase: 05-reporting-dashboards
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/(dashboard)/reports/profitability/page.tsx
  - src/components/reports/profitability-table.tsx
  - src/components/nav/app-sidebar.tsx
files_forbidden:
  - prisma/schema.prisma
  - prisma/migrations/**
  - src/lib/permissions.ts
  - src/lib/reporting.ts
  - src/lib/sla.ts
  - src/lib/billing.ts
  - src/lib/timer.ts
  - src/lib/actions/**
  - src/app/(dashboard)/reports/utilization/**
  - src/app/(dashboard)/reports/sla/**
  - src/components/reports/date-range-filter.tsx
  - src/components/reports/utilization-table.tsx
  - src/components/reports/company-contract-filter.tsx
  - src/components/reports/sla-compliance-summary.tsx
  - package.json
sequential_files:
  - src/components/nav/app-sidebar.tsx
expected_artifacts:
  - path: "src/app/(dashboard)/reports/profitability/page.tsx"
    provides: "Client profitability report page: billed revenue vs. hours invested per client"
    required: true
  - path: "src/components/reports/profitability-table.tsx"
    provides: "Table rendering ProfitabilityRow[] with a visible 'hours invested, not dollar cost' disclaimer"
    required: true
autonomous: false
agents: ["engineering-frontend-developer", "testing-qa-verification-specialist"]
requirements: ["Client profitability view compares billed revenue against time invested per client"]
user_setup: []
verification_commands:
  - "test -f 'src/app/(dashboard)/reports/profitability/page.tsx'"
  - "grep -q 'report:view_all' 'src/app/(dashboard)/reports/profitability/page.tsx'"
  - "grep -q 'getClientProfitability' 'src/app/(dashboard)/reports/profitability/page.tsx'"
  - "test -f src/components/reports/profitability-table.tsx"
  - "grep -q 'href=\"/reports/utilization\"' src/components/nav/app-sidebar.tsx"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "The page gates access with can(user.role, \"report:view_all\") only -- a technician or sales user is redirected, not shown a partial/empty report."
    - "Every dollar figure rendered on this page originates from a value that was already converted via .toNumber() inside src/lib/reporting.ts -- this plan performs no further Decimal handling itself, but the QA self-check must confirm no raw Decimal object is ever passed to a formatting function."
    - "The report is explicitly labeled/framed as billed revenue vs. hours invested, never as a dollar \"cost\" or \"profit\" figure, matching 05-CONTEXT.md's locked framing decision."
  artifacts:
    - path: "src/app/(dashboard)/reports/profitability/page.tsx"
      provides: "Async Server Component rendering the client profitability report"
      min_lines: 25
      contains: "getClientProfitability"
  key_links:
    - from: "src/app/(dashboard)/reports/profitability/page.tsx"
      to: "src/lib/reporting.ts"
      via: "page imports and calls getClientProfitability"
      pattern: "getClientProfitability"
---

<objective>
Build the client profitability report: a new route showing billed revenue vs. hours invested per client over a selectable date range, restricted to dispatcher/finance/admin, with a QA self-check on financial-data correctness before completion.

Purpose: Delivers "Client profitability view compares billed revenue against time invested per client" (ROADMAP.md success criterion) -- the most financially sensitive of the three reports, warranting a dual-role build-then-verify pass.
Output: New `src/app/(dashboard)/reports/profitability/page.tsx`, `src/components/reports/profitability-table.tsx`; one conditional append-only edit to `src/components/nav/app-sidebar.tsx`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-reporting-dashboards/05-CONTEXT.md
@.planning/phases/05-reporting-dashboards/05-01-SUMMARY.md

Relevant source files:
@src/lib/permissions.ts
@src/lib/reporting.ts
@src/lib/session.ts
@src/app/(dashboard)/invoices/page.tsx
@src/components/invoices/invoice-line-table.tsx
@src/components/nav/app-sidebar.tsx
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-frontend-developer (build) + testing-qa-verification-specialist (dual-role QA self-check before reporting complete, matching Plan 04-03/03-02's precedent -- financial-data correctness warrants this on the profitability report specifically)
Task: Build the client profitability report page and table, add the Reports nav item if not already present, then perform an evidence-based QA self-check on Decimal handling and RBAC gating.
Scope:
- Read targets: `src/lib/permissions.ts`, `src/lib/reporting.ts`, `src/lib/session.ts`, `src/app/(dashboard)/invoices/page.tsx` (gated list-page pattern reference), `src/components/invoices/invoice-line-table.tsx` (Decimal-formatting-in-UI reference), `src/components/nav/app-sidebar.tsx`, `.planning/phases/05-reporting-dashboards/05-01-SUMMARY.md` (confirm actual `reporting.ts` exports/signatures), `.planning/phases/05-reporting-dashboards/05-02-SUMMARY.md` and `05-03-SUMMARY.md` if they exist (confirm sidebar nav-item state)
- Write targets: `src/app/(dashboard)/reports/profitability/page.tsx` (new), `src/components/reports/profitability-table.tsx` (new), `src/components/nav/app-sidebar.tsx` (conditional append-only edit)
- Forbidden targets: `prisma/schema.prisma`, `prisma/migrations/**`, `src/lib/permissions.ts`, `src/lib/reporting.ts` (read-only), `src/lib/sla.ts`, `src/lib/billing.ts`, `src/lib/timer.ts`, `src/lib/actions/**`, `src/app/(dashboard)/reports/utilization/**`, `src/app/(dashboard)/reports/sla/**`, `src/components/reports/date-range-filter.tsx`, `src/components/reports/utilization-table.tsx`, `src/components/reports/company-contract-filter.tsx`, `src/components/reports/sla-compliance-summary.tsx`, `package.json`
Allowed tools/actions:
- Read, Edit, Write on the write targets listed above
- Bash: `npx tsc --noEmit`
Forbidden actions:
- Do not modify `src/lib/reporting.ts` or `src/lib/permissions.ts` -- import and use them, do not edit them. If any expected export is missing or has a different signature than documented, emit BLOCKED rather than reimplementing it locally.
- Do not build the utilization or SLA compliance pages, or any of the other Wave 2 plans' components -- reuse `DateRangeFilter` if it already exists (import it), otherwise build a minimal local date control inline and note this in the SUMMARY (same fallback rule as 05-03).
- Do not compute or display any dollar "cost" or "profit" figure -- only billed revenue and hours invested, per the locked framing decision.
- Do not restructure or reformat any part of `app-sidebar.tsx` beyond the single conditional nav-item edit.
- Do not self-defer planned work.
Implementation sequence:
1. Read `05-01-SUMMARY.md` to confirm `getClientProfitability`'s actual exported signature and confirm it already returns plain numbers (not Decimal objects).
2. Read `src/components/invoices/invoice-line-table.tsx` for the established currency-formatting-in-UI pattern.
3. Create `src/components/reports/profitability-table.tsx` (Task 1).
4. Create `src/app/(dashboard)/reports/profitability/page.tsx` (Task 1), wiring the date filter and table together, gated correctly.
5. Read the current `src/components/nav/app-sidebar.tsx`; if a "Reports" nav item already exists, leave the file untouched; otherwise add it (Task 2).
6. As testing-qa-verification-specialist, self-check (Task 2): confirm every dollar/hour value rendered by `ProfitabilityTable` traces back to a plain `number` from `getClientProfitability` (grep `reporting.ts` for `.toNumber()` usage as the evidence trail, do not just trust `05-01-SUMMARY.md`'s claim); confirm `report:view_all` is the literal gate on the page (not `report:view_own`, not an ungated route); confirm no "cost" or "profit" terminology appears anywhere in the new files.
7. Run `npx tsc --noEmit`.
Required interfaces/content structure:
- `src/components/reports/profitability-table.tsx`: Props: `{ rows: ProfitabilityRow[] }` (import the type from `@/lib/reporting`). Renders a table with columns: Client, Billed Revenue (formatted as currency, e.g. `$1,234.56`, following `invoice-line-table.tsx`'s existing currency-formatting helper/pattern), Hours Invested (formatted as hours, e.g. "42.5h"). Empty-state row sets `colSpan` matching the actual column count. Includes a visible caption/disclaimer stating this compares billed revenue to hours invested, not a computed dollar cost or profit margin (per the locked framing decision).
- `src/app/(dashboard)/reports/profitability/page.tsx`: async Server Component. `export default async function ProfitabilityReportPage({ searchParams }: { searchParams: Promise<{ from?: string; to?: string; companyId?: string; contractId?: string }> })`. Calls `getCurrentUser()`; **first line: `if (!user) redirect("/login");`** (matching `src/app/(dashboard)/invoices/page.tsx`'s exact null-session-guard pattern -- must precede any `can()` call); then if `!can(user.role, "report:view_all")`, redirect to `/unauthorized`. Parses `from`/`to` from `searchParams` (awaited) as raw strings; if either is absent or fails a basic `YYYY-MM-DD` shape check, use `getCurrentMonthRange()` from `@/lib/reporting` instead (do NOT construct `Date` objects in this page -- pass raw strings straight through to `getClientProfitability`, which owns all date-boundary parsing internally, per the locked timezone decision in `05-CONTEXT.md`). Parses optional `companyId`/`contractId` (note: `getClientProfitability`'s signature was corrected during plan critique to accept `contractId` as a 4th parameter -- confirm this against `05-01-SUMMARY.md`). Calls `getClientProfitability(from, to, companyId, contractId)`. Renders a date-range control (reuse `DateRangeFilter` from `@/components/reports/date-range-filter` if the file exists at read-time; otherwise a minimal inline equivalent, noted in the SUMMARY) and `ProfitabilityTable`.
Edge/error cases:
- A `report:view_own`-only user (technician, sales) hitting `/reports/profitability` directly must be redirected to `/unauthorized`.
- A company with billed revenue but zero hours invested in range (or vice versa) must still appear as a row with the other value shown as `0`/`0h`, per `getClientProfitability`'s documented merge behavior -- not silently dropped.
- An invalid or malformed `from`/`to` query param must fall back to `getCurrentMonthRange()`'s default rather than being passed through as-is -- this page never constructs `Date` objects itself.
- Sidebar edit is CONDITIONAL: read the file first; only add the nav item if not already present (from 05-02 or 05-03). Document in the SUMMARY which case applied.
Verification criteria:
- `npx tsc --noEmit` exits 0
- Page redirects report:view_own-only users away from `/reports/profitability`
- No "cost" or "profit" terminology anywhere in the new files (grep-verifiable)
- QA self-check evidence documented in SUMMARY (Decimal-handling trace, RBAC gate confirmation, terminology confirmation)
- No files outside `files_modified` were touched
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `src/lib/reporting.ts` does not export `getClientProfitability` with the signature documented in `05-01-SUMMARY.md`, or its return values are not confirmed plain `number`s (a raw `Decimal` object reaching this plan's UI layer is a blocking correctness issue, not something to work around locally).
- `src/lib/permissions.ts` does not export `report:view_all`-gated `can()` support as expected.
- `src/components/nav/app-sidebar.tsx`'s current structure has changed in a way that makes confirming/inserting the Reports nav item ambiguous.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, `05-01-SUMMARY.md`, `05-02-SUMMARY.md`/`05-03-SUMMARY.md` if they exist, and any `05-04-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx tsc --noEmit` before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Create the profitability table and report page</name>
  <files>src/components/reports/profitability-table.tsx, src/app/(dashboard)/reports/profitability/page.tsx</files>
  <action>
Read `05-01-SUMMARY.md` to confirm `getClientProfitability`'s exact signature and the `ProfitabilityRow` shape. Read `src/components/invoices/invoice-line-table.tsx` for the established currency-formatting pattern.

Create `src/components/reports/profitability-table.tsx` rendering `ProfitabilityRow[]` with Client/Billed Revenue/Hours Invested columns and the required disclaimer caption.

Create `src/app/(dashboard)/reports/profitability/page.tsx` as an async Server Component: await `searchParams`, resolve the date range and optional company filter, gate on `report:view_all`, call `getClientProfitability`, and render the date control (reusing `DateRangeFilter` if present) + `ProfitabilityTable`.

> verification: test -f src/components/reports/profitability-table.tsx
> verification: test -f 'src/app/(dashboard)/reports/profitability/page.tsx'
> verification: grep -q 'report:view_all' 'src/app/(dashboard)/reports/profitability/page.tsx'
> verification: grep -q 'getClientProfitability' 'src/app/(dashboard)/reports/profitability/page.tsx'
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f src/components/reports/profitability-table.tsx
test -f 'src/app/(dashboard)/reports/profitability/page.tsx'
grep -q 'report:view_all' 'src/app/(dashboard)/reports/profitability/page.tsx'
grep -q 'getClientProfitability' 'src/app/(dashboard)/reports/profitability/page.tsx'
npx tsc --noEmit
  </verify>
  <done>The client profitability report page renders correctly gated data through ProfitabilityTable with the required revenue-vs-hours framing, and the project type-checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm/add Reports nav item; QA self-check</name>
  <files>src/components/nav/app-sidebar.tsx</files>
  <action>
Read the current `src/components/nav/app-sidebar.tsx` in full. Check: `grep -q 'href="/reports/utilization"' src/components/nav/app-sidebar.tsx` -- if this matches (added by 05-02 or 05-03), leave the file untouched. If it does not match, insert this EXACT block (verbatim, from `05-CONTEXT.md`'s locked "Sidebar nav single-source-of-truth" decision) after the Invoices `<li>` and before the Admin `<li>`:
```tsx
{can(role, "report:view_own") && (
  <li>
    <Link
      href="/reports/utilization"
      className="block rounded-md px-2 py-1.5 text-sm text-sidebar-foreground hover:bg-sidebar-accent hover:text-sidebar-accent-foreground"
    >
      Reports
    </Link>
  </li>
)}
```

As testing-qa-verification-specialist, before reporting complete, verify by direct code reading (not by re-stating the plan): (1) grep `src/lib/reporting.ts` to confirm `getClientProfitability`'s revenue values are produced via `.toNumber()` on the `Invoice.groupBy`'s `_sum.total`, and that the hours-invested half is computed via a `findMany` + JS reduce over `TimeEntry` joined through `ticket.companyId` -- NOT a `db.timeEntry.groupBy({ by: ["companyId"] })` call, which is invalid since `TimeEntry` has no direct `companyId` column (this was a CRITICAL finding from plan critique; confirm the fix actually landed in the code, don't just trust `05-01-SUMMARY.md`'s claim); (2) confirm `can(user.role, "report:view_all")` (not `report:view_own`) is the literal gate in `profitability/page.tsx`, preceded by an explicit `if (!user) redirect("/login")` null-guard, and that a failing `can()` check redirects rather than falling through; (3) grep the new files (`profitability-table.tsx`, `profitability/page.tsx`) for the literal strings "cost" and "profit" (case-insensitive) and confirm zero matches outside of comments explaining why those terms are deliberately avoided. Document each check's evidence (file:line or grep output) in the plan SUMMARY.

> verification: grep -q 'href="/reports/utilization"' src/components/nav/app-sidebar.tsx
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q 'href="/reports/utilization"' src/components/nav/app-sidebar.tsx
npx tsc --noEmit
  </verify>
  <done>The sidebar has exactly one Reports nav entry, and the QA self-check's 3 evidence-based confirmations are documented in the SUMMARY.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] Profitability page redirects report:view_own-only users
- [ ] No "cost"/"profit" terminology in the new UI/table
- [ ] A company with revenue-but-no-hours (or vice versa) still renders as a row, not dropped
- [ ] Sidebar has exactly one Reports nav entry (not duplicated across 05-02/05-03/05-04)
- [ ] QA self-check evidence documented in SUMMARY
- [ ] No files outside `files_modified` were touched (`git diff --stat` confirms)
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Client profitability view compares billed revenue against time invested per client (ROADMAP.md success criterion)
- Dashboards respect role permissions -- restricted to dispatcher/finance/admin, consistent with finance/leadership visibility (ROADMAP.md success criterion)
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporting-dashboards/05-04-SUMMARY.md` documenting what was done, verification results, the QA self-check's evidence, decisions made, and any deviations. State explicitly whether this plan found the sidebar's Reports nav item already present or added it itself.
</output>
