---
phase: 06-polish-launch-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/tickets.ts
  - src/middleware.ts
files_forbidden:
  - src/lib/crypto.ts
  - src/lib/qbo.ts
  - src/lib/permissions.ts
  - prisma/schema.prisma
  - prisma/migrations/**
  - playwright.config.ts
  - package.json
  - src/components/**
  - src/app/**
  - .env.example
sequential_files: []
expected_artifacts:
  - path: "src/lib/actions/tickets.ts"
    provides: "deleteTicket restricts technicians to deleting only their own assigned tickets"
    required: true
  - path: "src/middleware.ts"
    provides: "IP-keyed in-memory rate limiter applied before every matched route"
    required: true
autonomous: false
agents: ["engineering-backend-architect", "engineering-security-engineer"]
requirements: ["No critical or high-severity issues open from QA review (ownership-scoped delete, rate limiting)"]
user_setup: []
verification_commands:
  - "grep -q 'assignedToId' src/lib/actions/tickets.ts"
  - "grep -A 20 'export async function deleteTicket' src/lib/actions/tickets.ts | grep -q 'technician'"
  - "grep -q 'rateLimit\\|rate-limit\\|RateLimit' src/middleware.ts"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "A technician-role user calling deleteTicket on a ticket NOT assigned to them is rejected with a clear error, not a silent no-op or a generic P2025."
    - "A technician-role user calling deleteTicket on a ticket assigned to them succeeds exactly as before this plan."
    - "dispatcher and admin roles retain unrestricted delete (no ownership check applied to them) -- this is a technician-only restriction, matching their narrower day-to-day scope."
    - "Rate limiting is applied inside middleware.ts (Edge runtime) and rejects excess requests with HTTP 429, not a silent drop or a 500."
    - "/api/auth/* is genuinely covered by the rate limiter (matcher widened to include it) -- this was a CRITICAL plan-critique correction to an earlier draft that wrongly assumed this route was already covered; the session-check auth gate must NOT apply to /api/auth/* (login must still work for requests under the limit)."
    - "Rate limiting does not break the existing coarse-auth-check behavior middleware.ts already performs -- both checks run, auth-check behavior is unchanged for requests under the rate limit."
  artifacts:
    - path: "src/lib/actions/tickets.ts"
      provides: "ownership-scoped deleteTicket"
      min_lines: 240
      contains: "assignedToId"
  key_links:
    - from: "src/lib/actions/tickets.ts"
      to: "src/lib/permissions.ts"
      via: "deleteTicket still imports TICKET_MANAGE_ROLES for the baseline requireRole gate; ownership is an additional in-function check, not a new Permission"
      pattern: "TICKET_MANAGE_ROLES"
---

<objective>
Close two accumulated RBAC/security gaps flagged across prior phase reviews: (1) any technician can currently delete any ticket regardless of assignment (a real ownership-scoping gap, not a cosmetic suggestion), and (2) no rate limiting exists on any route, including the auth and QBO OAuth endpoints, which matters now that this phase is explicitly preparing the app for real (self-hosted, internet-reachable) daily use.

Purpose: Both items were selected by the user as in-scope for Phase 6's "no critical or high-severity issues open from QA review" success criterion during planning.
Output: Modified `src/lib/actions/tickets.ts` (`deleteTicket` gains an ownership check for technicians); modified `src/middleware.ts` (adds an IP-keyed in-memory rate limiter ahead of the existing coarse auth check).
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-launch-prep/06-CONTEXT.md

Relevant source files:
@src/lib/actions/tickets.ts
@src/lib/permissions.ts
@src/middleware.ts
@src/lib/session.ts
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-backend-architect (ownership-scoped delete) + engineering-security-engineer (rate limiting)
Task: Add an ownership check to `deleteTicket` restricting technicians to their own assigned tickets; add an IP-keyed in-memory rate limiter to `src/middleware.ts` covering all matched routes (including `/api/auth/*` and `/api/qbo/*`).
Scope:
- Read targets: `src/lib/actions/tickets.ts` (full file, confirm `deleteTicket`'s exact current implementation and the `requireRole` return shape), `src/lib/permissions.ts` (confirm `TICKET_MANAGE_ROLES`, `Role` type, and that no new `Permission` literal is warranted per the locked approach in `06-CONTEXT.md`), `src/middleware.ts` (full file, confirm the exact `authConfig`/matcher structure), `src/lib/session.ts` (confirm `requireRole`'s return type -- what fields does the resolved user object expose, specifically `.id` and `.role`)
- Write targets: `src/lib/actions/tickets.ts` (modify `deleteTicket` only -- do not touch `createTicket`/`updateTicket`/`updateTicketStatus`/`assignTicket`), `src/middleware.ts` (modify -- add rate limiting logic, preserve all existing auth-check behavior)
- Forbidden targets: `src/lib/crypto.ts`, `src/lib/qbo.ts`, `src/lib/permissions.ts` (read-only -- do not add a new Permission literal for this; ownership is an in-function check), `prisma/schema.prisma`, `prisma/migrations/**`, `playwright.config.ts`, `package.json`, any file under `src/components/**` or `src/app/**`, `.env.example`
Allowed tools/actions:
- Read, Edit, Write on the write targets listed above
- Bash: `npx tsc --noEmit`
Forbidden actions:
- Do not add a new `Permission` literal to `permissions.ts` for ticket ownership -- per the locked approach in `06-CONTEXT.md`, this is an in-function ownership check layered on top of the existing `ticket:manage` gate, not a new authorization dimension.
- Do not add a new npm dependency for rate limiting (no `express-rate-limit`, no Redis client, no Upstash SDK) -- an in-memory token-bucket/sliding-window keyed by IP is sufficient and required for this self-hosted, single-instance, <25-user deployment. Document this scope limitation (not distributed, resets on process restart) in a code comment.
- Do not change `deleteTicket`'s behavior for `dispatcher` or `admin` roles -- they retain unrestricted delete.
- Do not weaken `middleware.ts`'s existing matcher or auth-check logic while adding rate limiting -- both must coexist; the rate limiter runs first (or wraps the existing logic), auth-check runs as before for any request that passes the rate limit.
- Do not self-defer planned work.
Implementation sequence:
1. Read `src/lib/actions/tickets.ts` in full, focusing on `deleteTicket` (currently lines 228-242 per the phase CONTEXT) and `requireRole`'s call signature/return value (check `src/lib/session.ts` for the exact resolved-user shape).
2. Modify `deleteTicket`: after `const user = await requireRole(TICKET_MANAGE_ROLES);` (capture the return value -- currently the code discards it via a bare `await requireRole(...)` call; this must change to capture `user`), fetch the ticket's `assignedToId` (either via a preliminary `db.ticket.findUnique({ where: { id }, select: { assignedToId: true } })` before the delete, or by inspecting the delete's own P2025-guarded try/catch structure -- implementer's choice, but the ownership check must happen BEFORE the delete is attempted, not after, so a rejected technician gets a clear ownership error rather than "not found"). If `user.role === "technician"` AND the ticket's `assignedToId !== user.id`, return a structured error (matching the existing return-shape convention used elsewhere in this file, e.g. `{ error: "You can only delete tickets assigned to you" }`) and do NOT perform the delete. If the role is `dispatcher` or `admin`, or the technician IS the assignee, proceed with the existing delete logic unchanged (including its existing P2025 handling).
3. Read `src/middleware.ts` in full and `src/middleware.ts`'s companion `auth.config.ts` if it exists (grep for it) to understand the exact current control flow (`NextAuth(authConfig).auth` as the default export, `config.matcher`).
4. **CORRECTED after plan critique -- read this step in full before implementing.** `src/middleware.ts`'s CURRENT matcher (`["/((?!login|api/auth|_next/static|_next/image|favicon.ico).*)"]`) explicitly EXCLUDES `/api/auth/*` -- this was independently confirmed by two plan-critique passes against the real file, correcting an earlier (wrong) draft of `06-CONTEXT.md` that claimed the opposite. `/api/auth/[...nextauth]` is NextAuth's own credential-check endpoint -- exactly the brute-force surface a rate limiter exists to protect -- so leaving it uncovered defeats a meaningful part of this task's purpose. This plan requires REAL coverage of `/api/auth/*`, not a documented-and-accepted gap. Implement as follows:
   - Widen `config.matcher` to include `api/auth` in its coverage (remove or narrow the `api/auth` exclusion in the negative-lookahead pattern), so the middleware function itself runs for `/api/auth/*` requests too.
   - Inside the middleware function, branch on path: for `/api/auth/*` requests, apply ONLY the rate-limit check (do NOT apply the existing `NextAuth(authConfig).auth` coarse-session-check logic to `/api/auth/*` routes -- those routes ARE the auth system itself; running a "do you have a session" gate in front of the login endpoint would break login). For all other previously-matched routes, behavior is unchanged: rate-limit check, then the existing `NextAuth(authConfig).auth` logic.
   - Add an IP-keyed in-memory rate limiter: a module-level `Map<string, { count: number; windowStart: number }>` (or equivalent), a fixed window (e.g. 60 requests per 60 seconds per IP for general routes; consider a tighter window specifically for `/api/auth/*` given it's the credential-check surface, e.g. 10 requests per 60 seconds -- document the chosen numbers and rationale in a comment) keyed by the request's IP (`request.headers.get("x-forwarded-for")` with a fallback, since Edge middleware does not have a direct socket-address API the same way Node does -- research/confirm the correct Next.js 16 Edge-runtime way to obtain the client IP and use it; if genuinely unavailable, key by a coarser identifier and document why in a comment, do not silently disable rate limiting).
   - On limit exceeded (any covered route, including `/api/auth/*`), return a `NextResponse` with status 429 and a `Retry-After` header, short-circuiting before any further logic runs.
   - **Shared-IP caveat**: an MSP office may have all technicians behind one NAT/shared public IP. Choose thresholds generous enough that normal multi-user office traffic doesn't trip the limiter (this is a brute-force speed bump, not a per-user quota) -- document this consideration explicitly in a code comment near the threshold constants.
   - `/api/qbo/callback` and `/api/qbo/connect` are already covered by the existing matcher (not in the exclusion list) -- no special-casing needed for them beyond the standard rate-limit + existing-auth-check flow.
5. Run `npx tsc --noEmit`.
Required interfaces/content structure:
- `deleteTicket(id: string)`: unchanged external signature. Internally captures `requireRole`'s return, adds a pre-delete ownership check for `technician` role only, returns `{ error: string }` on ownership rejection (never throws for this case -- it's an expected authorization outcome, not a system error).
- `src/middleware.ts`: default export remains a middleware function; `config.matcher` is WIDENED to include `api/auth` (correcting the previous exclusion); rate-limit check runs for every route the widened matcher covers; the existing `NextAuth(authConfig).auth` session-check logic still runs for all previously-covered routes but is explicitly SKIPPED for `/api/auth/*` requests (which must remain reachable to unauthenticated users attempting to log in).
Edge/error cases:
- A ticket with `assignedToId: null` (unassigned): a technician attempting to delete it is rejected (null !== their user id) -- this is correct, matches the "only your own assigned tickets" rule; do not special-case null as "anyone can delete."
- Ticket not found (already deleted, race condition): the ownership check's preliminary lookup returns nothing -- treat as the existing P2025 "Ticket not found" error path, do not let a missing-ticket case fall through to a confusing ownership-rejection message.
- Rate limiter's in-memory Map grows unbounded over a long-running process if never cleaned up -- add a simple cleanup (e.g. delete stale entries when their window has expired, checked opportunistically on each request) to avoid a slow memory leak; this does not need to be a separate scheduled job.
- Multiple requests from the same IP in rapid succession right at the window boundary: acceptable to be approximate (fixed-window is fine, sliding-window is not required) -- document this as an accepted simplification, not a bug.
Verification criteria:
- `npx tsc --noEmit` exits 0
- `deleteTicket` rejects a technician deleting a ticket not assigned to them, with a clear error message
- `deleteTicket`'s behavior for dispatcher/admin and for a technician deleting their own ticket is unchanged from before this plan
- Rate limiting returns 429 with a `Retry-After` header when the limit is exceeded, and does not affect requests under the limit
- `/api/auth/*` IS covered by the rate limiter after this plan (matcher widened) -- verify by reading the final `config.matcher` and confirming a request to `/api/auth/*` is actually subject to the rate-limit check, not just documenting that it isn't. Login must still work normally for requests under the limit (the session-check gate must NOT apply to `/api/auth/*`).
- No files outside `files_modified` were touched
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made (rate limit window/threshold chosen and why, IP-extraction method used, whether /api/auth is covered)
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `requireRole`'s return value does not expose both `.id` and `.role` on the resolved user (needed for the ownership check) -- check `src/lib/session.ts` directly rather than assuming.
- `src/middleware.ts`'s actual structure differs materially from `06-CONTEXT.md`'s description in a way that makes rate-limiter placement ambiguous (e.g. if `NextAuth(authConfig).auth` is not a simple default-export wrapper).
- Next.js 16's Edge middleware runtime genuinely provides no way to obtain a client IP address (confirm via actual research/testing, not assumption) -- in that case, emit BLOCKED with the specific limitation found rather than silently keying rate-limiting by something meaningless.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, and `06-02-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx tsc --noEmit` before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Ownership-scope deleteTicket for technicians</name>
  <files>src/lib/actions/tickets.ts</files>
  <action>
Read `src/lib/actions/tickets.ts` in full and `src/lib/session.ts` to confirm `requireRole`'s resolved-user shape. Modify `deleteTicket(id: string)`: capture `requireRole(TICKET_MANAGE_ROLES)`'s return value as `user` (currently discarded). Before performing the delete, look up the ticket's `assignedToId`. If `user.role === "technician"` and `assignedToId !== user.id`, return `{ error: "You can only delete tickets assigned to you" }` without deleting. Otherwise (dispatcher, admin, or the technician's own ticket), proceed with the existing delete + P2025 handling unchanged. A not-found ticket during the ownership lookup falls through to the existing "Ticket not found" P2025 path, not a confusing ownership message.

> verification: grep -q 'assignedToId' src/lib/actions/tickets.ts
> verification: grep -A 20 'export async function deleteTicket' src/lib/actions/tickets.ts | grep -q 'technician'
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q 'assignedToId' src/lib/actions/tickets.ts
grep -A 20 'export async function deleteTicket' src/lib/actions/tickets.ts | grep -q 'technician'
npx tsc --noEmit
  </verify>
  <done>deleteTicket rejects technician-role deletes of tickets not assigned to them, while dispatcher/admin and self-assigned-technician deletes work exactly as before.</done>
</task>

<task type="auto">
  <name>Task 2: Add IP-keyed rate limiting to middleware.ts</name>
  <files>src/middleware.ts</files>
  <action>
Read `src/middleware.ts` in full (and `auth.config.ts` if present) to confirm the exact current control flow and matcher configuration. Add an in-memory, IP-keyed fixed-window rate limiter (module-level `Map`, e.g. 60 requests/60s per IP -- document the chosen values and rationale) that runs before the existing `NextAuth(authConfig).auth` call and returns HTTP 429 with a `Retry-After` header when exceeded. Requests under the limit fall through to the existing auth-check behavior unchanged. Use the correct Next.js 16 Edge-runtime method to extract the client IP (research and confirm; do not guess). Add opportunistic cleanup of stale Map entries to avoid unbounded memory growth. Explicitly document in a comment (and in the SUMMARY) whether `/api/auth/*` is covered given the existing matcher's exclusions.

> verification: grep -q 'rateLimit\|rate-limit\|RateLimit' src/middleware.ts
> verification: grep -q '429' src/middleware.ts
> verification: grep -q 'Retry-After' src/middleware.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q 'rateLimit\|rate-limit\|RateLimit' src/middleware.ts
grep -q '429' src/middleware.ts
grep -q 'Retry-After' src/middleware.ts
npx tsc --noEmit
  </verify>
  <done>middleware.ts rejects excess requests per-IP with HTTP 429 + Retry-After, existing auth-check behavior is preserved for requests under the limit, and the SUMMARY documents whether /api/auth is covered.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] Technician-role deleteTicket is ownership-scoped; dispatcher/admin unchanged
- [ ] Rate limiter returns 429 + Retry-After on excess requests, IP-keyed, in-memory (no new dependency)
- [ ] Existing middleware auth-check behavior is unchanged for requests under the rate limit
- [ ] SUMMARY documents whether /api/auth/* is covered by the rate limiter
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Blanket ticket:manage-based delete no longer lets any technician delete tickets not assigned to them (accumulated RBAC gap, selected as in-scope for Phase 6)
- Rate limiting exists before the application is exposed to the internet (accumulated Phase 1 deferred suggestion, selected as in-scope for Phase 6)
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-launch-prep/06-02-SUMMARY.md` documenting what was done, verification results, the rate-limit window/threshold chosen, the IP-extraction method used, whether /api/auth is covered, and any deviations.
</output>
