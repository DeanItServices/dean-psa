---
phase: 06-polish-launch-prep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/**
files_forbidden:
  - src/lib/crypto.ts
  - src/lib/qbo.ts
  - src/lib/actions/tickets.ts
  - src/middleware.ts
  - playwright.config.ts
  - package.json
  - src/components/**
  - src/app/**
  - .env.example
sequential_files: []
expected_artifacts:
  - path: "prisma/schema.prisma"
    provides: "unique index on Invoice.qboInvoiceId; @@index([companyId]) on Contact, Contract, Asset"
    required: true
autonomous: false
agents: ["engineering-backend-architect"]
requirements: ["No critical or high-severity issues open from QA review (defense-in-depth indexes)"]
user_setup: []
verification_commands:
  - "npx prisma validate"
  - "grep -rq 'qboInvoiceId' prisma/migrations/*/migration.sql"
  - "grep -rq 'Contact' prisma/migrations/*/migration.sql"

must_haves:
  truths:
    - "Invoice.qboInvoiceId has a unique index that allows multiple NULL values (Prisma's standard behavior for a unique index on a nullable column) -- most invoices are not yet pushed to QBO and must not collide on a shared NULL value."
    - "Contact, Contract, and Asset each gain @@index([companyId]) matching the exact pattern already used by Ticket.companyId (added in Phase 5) -- same declaration style, no deviation."
    - "The migration is generated via prisma migrate dev and its SQL file is verified to contain the expected CREATE INDEX/CREATE UNIQUE INDEX statements -- migration file text is accepted as sufficient evidence for this plan (no live DB connection is assumed available in a planning/execution worktree; if one is available, live pg_indexes verification is a bonus but not required to pass this plan)."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "4 new index declarations across 4 models"
      min_lines: 1
      contains: "@@index([companyId])"
  key_links: []
---

<objective>
Add defense-in-depth database indexes flagged as deferred suggestions across Phase 2 and Phase 4 reviews: a unique index on `Invoice.qboInvoiceId` (beyond the already-correct atomic claim mechanism) and `@@index([companyId])` on the three CRM models (`Contact`, `Contract`, `Asset`) that are FK-filtered in CRM tab queries but have never had an index.

Purpose: Selected by the user as in-scope for Phase 6's "no critical or high-severity issues open from QA review" success criterion during planning. This is the only other schema change in Phase 6 besides the already-locked encryption work (which does not touch the schema) -- narrowly scoped to these 4 index additions, no other schema change.
Output: Modified `prisma/schema.prisma` (4 new `@@index`/`@@unique` declarations); one new Prisma migration.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-launch-prep/06-CONTEXT.md

Relevant source files:
@prisma/schema.prisma
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-backend-architect
Task: Add a unique index on `Invoice.qboInvoiceId` and `@@index([companyId])` on `Contact`, `Contract`, `Asset`, via one migration.
Scope:
- Read targets: `prisma/schema.prisma` (full file -- confirm exact current field names/types on `Invoice`, `Contact`, `Contract`, `Asset`, and confirm `Ticket.companyId`'s existing `@@index` declaration style from Phase 5 to match exactly)
- Write targets: `prisma/schema.prisma` (modify -- index additions ONLY), `prisma/migrations/**` (new migration directory)
- Forbidden targets: `src/lib/crypto.ts`, `src/lib/qbo.ts`, `src/lib/actions/tickets.ts`, `src/middleware.ts`, `playwright.config.ts`, `package.json`, any file under `src/components/**` or `src/app/**`, `.env.example`
Allowed tools/actions:
- Read, Edit, Write on the write targets listed above
- Bash: `npx prisma validate`, `npx prisma migrate dev`, `docker compose exec db psql` (or equivalent) if a live DB connection is available in this environment
Forbidden actions:
- Do not add any Prisma model, field, or index beyond the four specified (`Invoice.qboInvoiceId` unique, `Contact.companyId`/`Contract.companyId`/`Asset.companyId` non-unique) -- this plan is narrowly scoped, matching the established pattern from Phase 5's index plan.
- Do not modify any existing field, relation, or constraint on these models beyond adding the index declarations.
- Do not touch `TimeEntry`, `Ticket`, `User`, `Company`, `QuickBooksConnection`, or any other model.
- Do not self-defer planned work.
Implementation sequence:
1. Read `prisma/schema.prisma` in full. Confirm `Invoice.qboInvoiceId`'s exact current field declaration (expected: `qboInvoiceId String?` per `06-CONTEXT.md`) and confirm `Ticket`'s existing `@@index([companyId])` declaration (added in Phase 5) for the exact syntax style to replicate on `Contact`/`Contract`/`Asset`.
2. Add `@@unique([qboInvoiceId])` to the `Invoice` model (Prisma's unique constraint on a nullable field allows multiple NULLs by default in PostgreSQL, which is the desired behavior here -- most invoices have `qboInvoiceId: null` until pushed).
3. Add `@@index([companyId])` to `Contact`, `Contract`, and `Asset` models, matching `Ticket`'s existing declaration style exactly.
4. Run `npx prisma migrate dev --name add_defense_in_depth_indexes` to generate and apply the migration.
5. Verify the generated `migration.sql` contains the expected `CREATE UNIQUE INDEX`/`CREATE INDEX` statements for all 4 additions.
6. If a live DB connection is available in this environment (check via `npx prisma migrate status` or attempt the migrate command and observe the result), additionally verify via a direct `pg_indexes` query that all 4 indexes exist live, matching the verification-over-trust pattern established in Phases 2/4/5. If no live DB connection is available (a documented, accepted environment gap in prior phases' review worktrees), the migration file's text is sufficient evidence for this plan -- state clearly in the SUMMARY which case applied.
Required interfaces/content structure:
- `Invoice` model: gains `@@unique([qboInvoiceId])`.
- `Contact`, `Contract`, `Asset` models: each gains `@@index([companyId])`.
- No other schema change.
Edge/error cases:
- `npx prisma migrate dev` may trigger the same `package.json` `allowScripts` auto-write side effect observed during Phase 5's 05-01 plan -- if this occurs, revert `package.json` via `git checkout -- package.json` immediately after detection (this file is in `files_forbidden`), and re-verify `npx prisma validate` / `npx tsc --noEmit` still pass post-revert, exactly as Phase 5's 05-01-SUMMARY.md documented handling the same issue.
- If a live DB in this environment already contains a duplicate non-null `qboInvoiceId` value across two Invoice rows (unlikely in a fresh/dev environment, but possible if test data was seeded oddly), the unique-constraint migration will fail to apply -- if this happens, do not silently work around it; emit `BLOCKED` with the exact conflicting rows found via a direct query, since resolving duplicate production-adjacent data is a data decision, not a schema-authoring one.
Verification criteria:
- `npx prisma validate` exits 0
- Migration SQL file contains `CREATE UNIQUE INDEX` referencing `qboInvoiceId` and 3 `CREATE INDEX` statements referencing `companyId` on `Contact`/`Contract`/`Asset`
- No files outside `files_modified` were touched (verify `package.json` was not left modified, per the edge case above)
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `Invoice.qboInvoiceId`, `Contact.companyId`, `Contract.companyId`, or `Asset.companyId` do not exist with the field names/types this plan assumes.
- `npx prisma migrate dev` fails for a reason other than the known `package.json` side effect (e.g. a genuine data conflict on the new unique constraint).
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, and `06-03-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists (especially `package.json`), emit `BLOCKED` or revert per the documented edge case.
4. Re-run `npx prisma validate` before continuing.
5. If the migration was already applied in a prior partial run, do not re-run `prisma migrate dev` blindly -- check the migration directory and (if a live DB is available) `pg_indexes` first.
6. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 defense-in-depth indexes via migration</name>
  <files>prisma/schema.prisma, prisma/migrations/**</files>
  <action>
Read `prisma/schema.prisma` in full, confirming `Invoice.qboInvoiceId`'s field declaration and `Ticket.companyId`'s existing `@@index` style (from Phase 5). Add `@@unique([qboInvoiceId])` to `Invoice`. Add `@@index([companyId])` to `Contact`, `Contract`, and `Asset`, matching `Ticket`'s style exactly. Run `npx prisma migrate dev --name add_defense_in_depth_indexes`. If `package.json` is auto-modified by the migrate command (a known Phase 5 side effect), revert it via `git checkout -- package.json` and re-verify. If a live DB connection is available, confirm all 4 indexes exist via a direct `pg_indexes` query; otherwise, the migration file's text is accepted evidence -- state which case applied in the SUMMARY.

> verification: npx prisma validate
> verification: grep -rq 'qboInvoiceId' prisma/migrations/*/migration.sql
> verification: grep -rq 'Contact' prisma/migrations/*/migration.sql
> verification: grep -rq 'Contract' prisma/migrations/*/migration.sql
> verification: grep -rq 'Asset' prisma/migrations/*/migration.sql
  </action>
  <verify>
npx prisma validate
grep -rq 'qboInvoiceId' prisma/migrations/*/migration.sql
grep -rq 'Contact' prisma/migrations/*/migration.sql
grep -rq 'Contract' prisma/migrations/*/migration.sql
grep -rq 'Asset' prisma/migrations/*/migration.sql
  </verify>
  <done>Invoice.qboInvoiceId has a unique index (nullable-safe) and Contact/Contract/Asset each have @@index([companyId]), applied via a real migration matching the established schema-change verification pattern.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` exits 0
- [ ] Migration SQL contains the expected unique index on `qboInvoiceId` and 3 indexes on `companyId`
- [ ] `package.json` was not left modified (revert-if-touched pattern applied if the known side effect occurred)
- [ ] SUMMARY states whether live `pg_indexes` verification was possible in this environment
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Invoice.qboInvoiceId has a unique index as defense-in-depth beyond the already-correct atomic claim mechanism (accumulated Phase 4 deferred suggestion)
- Contact/Contract/Asset have @@index([companyId]) matching the established Phase 5 pattern (accumulated Phase 2 deferred suggestion)
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-launch-prep/06-03-SUMMARY.md` documenting what was done, verification results, whether live pg_indexes verification was possible, and any deviations.
</output>
