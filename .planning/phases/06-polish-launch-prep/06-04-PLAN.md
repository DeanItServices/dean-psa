---
phase: 06-polish-launch-prep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - package.json
  - e2e/fixtures.ts
  - .gitignore
files_forbidden:
  - src/lib/crypto.ts
  - src/lib/qbo.ts
  - src/lib/actions/tickets.ts
  - src/middleware.ts
  - prisma/schema.prisma
  - prisma/migrations/**
  - src/components/**
  - src/app/**
  - .env.example
  - e2e/tickets.spec.ts
  - e2e/time-entry-to-invoice.spec.ts
  - e2e/sla-tracking.spec.ts
sequential_files: []
expected_artifacts:
  - path: "playwright.config.ts"
    provides: "Playwright config with webServer running npm run dev against the local DB"
    required: true
  - path: "e2e/fixtures.ts"
    provides: "Shared login-as-role helper and seed-data constants for Wave 2's 3 spec plans"
    required: true
autonomous: false
agents: ["engineering-frontend-developer", "testing-qa-verification-specialist"]
requirements: ["Core workflows (ticket lifecycle, time entry to invoice, SLA tracking) pass end-to-end verification"]
user_setup:
  - "Run `npx playwright install --with-deps chromium` once after this plan lands, before running `npm run test:e2e` for the first time (downloads the Chromium browser binary Playwright needs; not run automatically by this plan since it requires network access and disk space the execution environment may not have)."
verification_commands:
  - "test -f playwright.config.ts"
  - "grep -q '@playwright/test' package.json"
  - "grep -q 'test:e2e' package.json"
  - "test -f e2e/fixtures.ts"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "playwright.config.ts's webServer option starts the app via `npm run dev` (or an equivalent already-defined script) and waits for it to be ready before running tests -- specs never run against a server that isn't actually up."
    - "e2e/fixtures.ts exports a reusable login-as-role helper that Wave 2's 3 spec plans can import without each reimplementing Auth.js login flow navigation."
    - "The seeded local dev test users documented in STATE.md (technician/dispatcher/sales/finance/admin @mspdemo.local, password Password123!) are the credentials the login fixture uses -- no new user-seeding mechanism is introduced by this plan."
    - "package.json's test:e2e script and the new @playwright/test devDependency do not remove or alter any existing script or dependency."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright test configuration"
      min_lines: 15
      contains: "webServer"
    - path: "e2e/fixtures.ts"
      provides: "shared login/seed-data helpers"
      min_lines: 20
      contains: "export"
  key_links: []
---

<objective>
Install and configure Playwright as this project's E2E testing framework, and build a shared fixture module (login-as-role helper + seed-data references) that Wave 2's three workflow-specific spec plans will import, so none of them need to reimplement Auth.js login navigation or guess at seeded test-user credentials.

Purpose: The user selected Playwright + automated E2E specs (over a manual checklist) for Phase 6's "core workflows pass end-to-end verification" success criterion during planning, despite zero E2E infrastructure existing today -- this plan lays that foundation.
Output: New `playwright.config.ts`, `e2e/fixtures.ts`; modified `package.json` (new devDependency + `test:e2e` script); modified `.gitignore` (exclude Playwright's local report/output directories).
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-launch-prep/06-CONTEXT.md
@.planning/STATE.md

Relevant source files:
@package.json
@src/app/login/page.tsx
@.gitignore
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-frontend-developer (Playwright config + fixtures) + testing-qa-verification-specialist (dual-role QA self-check on fixture correctness before completion -- confirm the login flow actually works against the real login page's DOM structure, not an assumed one)
Task: Install `@playwright/test`, create `playwright.config.ts`, create `e2e/fixtures.ts` with a login-as-role helper, add a `test:e2e` script, update `.gitignore`.
Scope:
- Read targets: `package.json` (confirm exact existing scripts/dependencies before editing additively), `src/app/login/page.tsx` (confirm the actual login form's field selectors -- input names/ids/labels -- so the fixture's login helper targets real elements, not guessed ones), `.gitignore` (confirm existing exclusion patterns and style), `.planning/STATE.md` (confirm the exact seeded test-user emails/password documented there)
- Write targets: `playwright.config.ts` (new), `package.json` (modify -- additive only: one new devDependency, one new script), `e2e/fixtures.ts` (new), `.gitignore` (modify -- additive only: Playwright output directories)
- Forbidden targets: `src/lib/crypto.ts`, `src/lib/qbo.ts`, `src/lib/actions/tickets.ts`, `src/middleware.ts`, `prisma/schema.prisma`, `prisma/migrations/**`, any file under `src/components/**` or `src/app/**`, `.env.example`, and the three Wave-2 spec files (`e2e/tickets.spec.ts`, `e2e/time-entry-to-invoice.spec.ts`, `e2e/sla-tracking.spec.ts` -- those belong to Plans 06-06/06-07/06-08, do not create placeholder versions of them)
Allowed tools/actions:
- Read, Edit, Write on the write targets listed above
- Bash: `npm install --save-dev @playwright/test`, `npx tsc --noEmit`
Forbidden actions:
- Do not run `npx playwright install` (browser binary download) -- this requires network/disk resources this plan's execution environment may not have; instead document it as a `user_setup` step (already captured in this plan's frontmatter) for the user to run once after this plan lands.
- Do not write any of the three Wave-2 spec files -- this plan only builds the shared infrastructure they will import.
- Do not remove or modify any existing `package.json` script or dependency.
- Do not introduce a separate test-database strategy (e.g. a Docker Compose test-db service, a `.env.test` file) -- per the locked approach in `06-CONTEXT.md`, specs run against the local dev server/database; this is an explicit, documented scope limitation for this phase, not an oversight.
- Do not self-defer planned work.
Implementation sequence:
1. Read `package.json` in full to confirm exact current scripts/dependencies before adding to them.
2. Read `src/app/login/page.tsx` in full to confirm the actual login form's input `name`/`id` attributes (or associated `<label>` text) for email and password fields, and the submit button's accessible name -- the fixture's login helper must target real, confirmed selectors, not assumed ones like `#email`/`#password`.
3. Read `.planning/STATE.md` to confirm the exact seeded test-user credentials (documented as `technician/dispatcher/sales/finance/admin @mspdemo.local, all password Password123!`, local dev only, guarded by a `NODE_ENV=production` check in the seed script per STATE.md).
4. Install `@playwright/test` as a devDependency: `npm install --save-dev @playwright/test`.
5. Create `playwright.config.ts` at the repo root: `testDir: "./e2e"`, a `webServer` block that runs `npm run dev`, points at `http://localhost:3000`, with `reuseExistingServer: !process.env.CI` (standard Playwright convention -- reuses an already-running dev server locally, always starts fresh in CI), and a reasonable `timeout` for server startup (e.g. 60000ms, since `next dev` cold-start can be slow). Set `use: { baseURL: "http://localhost:3000" }` so specs can use relative paths.
6. Create `e2e/fixtures.ts` exporting:
   - A `ROLE_CREDENTIALS` constant: `Record<"technician" | "dispatcher" | "sales" | "finance" | "admin", { email: string; password: string }>` populated from the confirmed STATE.md-documented seed values (e.g. `technician@mspdemo.local` / `Password123!` -- confirm exact email pattern from STATE.md/the actual seed script, do not guess the format).
   - An exported `async function loginAs(page: Page, role: keyof typeof ROLE_CREDENTIALS): Promise<void>` that navigates to `/login`, fills the confirmed email/password fields, submits, and waits for a successful redirect away from `/login` (e.g. `await page.waitForURL((url) => !url.pathname.includes("/login"))`) before returning -- so every spec that calls `loginAs` can assume the session is established when it resolves.
7. Update `.gitignore`: add Playwright's default output directories (`/test-results/`, `/playwright-report/`, `/blob-report/`, `/playwright/.cache/`) if not already covered by an existing broad pattern -- check first, don't duplicate.
8. Add `"test:e2e": "playwright test"` to `package.json`'s `scripts`.
9. Run `npx tsc --noEmit`.
Required interfaces/content structure:
- `playwright.config.ts`: default-exports a Playwright `defineConfig(...)` result. `testDir: "./e2e"`. `webServer` block present and correctly configured. `use.baseURL` set.
- `e2e/fixtures.ts`: exports `ROLE_CREDENTIALS` and `loginAs(page, role)`. No test specs in this file -- it is a helper module only, imported by Wave 2's spec files, not itself a `.spec.ts` file (so Playwright's test runner does not try to execute it directly).
Edge/error cases:
- If `src/app/login/page.tsx`'s actual form structure uses different field identifiers than a typical email/password form assumes (e.g. a combined `identifier` field, or a `name` attribute that doesn't match `email`), the fixture must target what actually exists -- read the file, do not guess.
- If seeded test users described in STATE.md don't match what's actually in the seed script (`prisma/seed.ts` -- check if it exists and cross-reference), prefer the actual seed script's values over STATE.md's prose description, and note any discrepancy in the SUMMARY.
- `loginAs`'s wait-for-redirect must not hang indefinitely on a login failure -- Playwright's default action timeout will surface a clear test failure in that case, which is acceptable (do not add a custom infinite-retry loop).
Verification criteria:
- `npx tsc --noEmit` exits 0
- `playwright.config.ts` exists with a working `webServer` block
- `e2e/fixtures.ts` exports `loginAs` targeting the real login page's actual field selectors (confirmed by reading the source, not assumed)
- `package.json` gained exactly one devDependency and one script, nothing removed
- No files outside `files_modified` were touched, and none of the 3 Wave-2 spec files were created
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made (exact selectors used for the login form, exact seed credential format confirmed)
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `src/app/login/page.tsx` does not exist at the expected path, or its form structure is too dynamic/unclear to derive stable selectors from.
- The seeded test-user credentials in STATE.md and in the actual seed script (if found) conflict with each other.
- `npm install --save-dev @playwright/test` fails (network/registry issue in this environment).
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, and `06-04-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx tsc --noEmit` before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright and create config</name>
  <files>playwright.config.ts, package.json</files>
  <action>
Read `package.json` in full. Run `npm install --save-dev @playwright/test`. Create `playwright.config.ts` at the repo root with `testDir: "./e2e"`, a `webServer` block running `npm run dev` against `http://localhost:3000` with `reuseExistingServer: !process.env.CI` and a 60000ms startup timeout, and `use: { baseURL: "http://localhost:3000" }`. Add `"test:e2e": "playwright test"` to `package.json`'s scripts, additive only.

> verification: test -f playwright.config.ts
> verification: grep -q '@playwright/test' package.json
> verification: grep -q 'test:e2e' package.json
> verification: grep -q 'webServer' playwright.config.ts
  </action>
  <verify>
test -f playwright.config.ts
grep -q '@playwright/test' package.json
grep -q 'test:e2e' package.json
grep -q 'webServer' playwright.config.ts
  </verify>
  <done>Playwright is installed as a devDependency, playwright.config.ts exists with a working webServer block pointing at the local dev server, and package.json gained the test:e2e script additively.</done>
</task>

<task type="auto">
  <name>Task 2: Build shared login/seed-data fixture module</name>
  <files>e2e/fixtures.ts, .gitignore</files>
  <action>
Read `src/app/login/page.tsx` in full to confirm the real login form's field selectors, and `.planning/STATE.md` (cross-referencing `prisma/seed.ts` if present) to confirm the exact seeded test-user email/password format. Create `e2e/fixtures.ts` exporting `ROLE_CREDENTIALS` (one entry per role: technician/dispatcher/sales/finance/admin) and `async function loginAs(page, role)` that navigates to `/login`, fills the confirmed real selectors, submits, and waits for redirect away from `/login` before returning. Update `.gitignore` to exclude Playwright's default output directories if not already covered.

> verification: test -f e2e/fixtures.ts
> verification: grep -q 'export' e2e/fixtures.ts
> verification: grep -q 'loginAs' e2e/fixtures.ts
> verification: grep -q 'ROLE_CREDENTIALS' e2e/fixtures.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f e2e/fixtures.ts
grep -q 'export' e2e/fixtures.ts
grep -q 'loginAs' e2e/fixtures.ts
grep -q 'ROLE_CREDENTIALS' e2e/fixtures.ts
npx tsc --noEmit
  </verify>
  <done>e2e/fixtures.ts exports a working loginAs helper targeting the real login page's confirmed selectors and the actual seeded credentials, ready for Wave 2's 3 spec plans to import; the project type-checks cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] `playwright.config.ts` exists with a correct `webServer` configuration
- [ ] `e2e/fixtures.ts` exports `loginAs`/`ROLE_CREDENTIALS` using real, confirmed login-page selectors and real seeded credentials
- [ ] `package.json` gained exactly the new devDependency and script, nothing else changed
- [ ] None of the 3 Wave-2 spec files were created by this plan
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Playwright E2E infrastructure exists and is ready for Wave 2's 3 workflow specs to build on, without any of them needing to reimplement login/seed-data logic
- Core workflow E2E verification (ROADMAP.md success criterion) has a working foundation
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-launch-prep/06-04-SUMMARY.md` documenting what was done, verification results, the exact login-page selectors and seed credentials confirmed, and any deviations.
</output>
