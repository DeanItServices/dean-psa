---
phase: 06-polish-launch-prep
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - src/app/(dashboard)/loading.tsx
  - src/app/(dashboard)/error.tsx
  - src/components/tickets/kanban-board.tsx
  - src/components/tickets/sla-badge.tsx
files_forbidden:
  - src/lib/crypto.ts
  - src/lib/qbo.ts
  - src/lib/actions/tickets.ts
  - src/middleware.ts
  - prisma/schema.prisma
  - prisma/migrations/**
  - playwright.config.ts
  - package.json
  - e2e/**
  - .env.example
  - src/lib/sla.ts
sequential_files: []
expected_artifacts:
  - path: "src/app/(dashboard)/loading.tsx"
    provides: "Loading UI shown while any dashboard route's Server Component data-fetch is in flight"
    required: true
  - path: "src/app/(dashboard)/error.tsx"
    provides: "Scoped error UI shown when any dashboard route throws, instead of Next.js's generic error screen"
    required: true
  - path: "src/components/tickets/kanban-board.tsx"
    provides: "Kanban board with a usable narrow-viewport layout"
    required: true
  - path: "src/components/tickets/sla-badge.tsx"
    provides: "SlaBadge using theme tokens instead of hardcoded Tailwind colors"
    required: true
autonomous: false
agents: ["engineering-frontend-developer"]
requirements: ["UI is consistent and responsive across all modules"]
user_setup: []
verification_commands:
  - "test -f 'src/app/(dashboard)/loading.tsx'"
  - "test -f 'src/app/(dashboard)/error.tsx'"
  - "grep -q '\"use client\"' 'src/app/(dashboard)/error.tsx'"
  - "grep -q 'md:\\|sm:\\|lg:' src/components/tickets/kanban-board.tsx"
  - "grep -qv 'bg-green-600\\|bg-yellow-500' src/components/tickets/sla-badge.tsx"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "loading.tsx and error.tsx are placed at src/app/(dashboard)/ (the route-group root), covering all 13 routes currently in that group with a single pair of files, not per-route duplicates."
    - "error.tsx is a Client Component (Next.js requirement for error boundaries -- error.tsx MUST have \"use client\" or the framework itself rejects it) and receives the standard error/reset props, offering the user a way to retry."
    - "SlaBadge's rendered semantics (which status maps to which visual treatment) are unchanged -- only the underlying color values move from hardcoded hex/Tailwind-color-utility classes to theme-token-based classes/CSS variables already used elsewhere in this codebase's shadcn/ui setup."
    - "The Kanban board's narrow-viewport behavior is a genuine usability improvement (e.g. horizontal scroll with visible columns, or a stacked/single-column view below a breakpoint) -- not a cosmetic-only change that still leaves the board unusable on a phone/tablet width."
  artifacts:
    - path: "src/app/(dashboard)/error.tsx"
      provides: "Client Component error boundary"
      min_lines: 10
      contains: "use client"
  key_links:
    - from: "src/components/tickets/sla-badge.tsx"
      to: "src/app/globals.css"
      via: "theme tokens used by SlaBadge are defined in the project's existing Tailwind/shadcn theme configuration, not invented ad hoc"
      pattern: "var(--"
---

<objective>
Close three accumulated UI-consistency gaps flagged across Phase 2/3 reviews and confirmed absent during Phase 6 planning: no `loading.tsx`/`error.tsx` exist anywhere in the app (confirmed via Glob -- every route currently shows Next.js's generic, unstyled error/loading screen on failure/delay), the Kanban board has no responsive breakpoint (desktop-only by apparent accident, not documented decision), and `SlaBadge` hardcodes Tailwind colors instead of theme tokens (no dark-mode support).

Purpose: Directly implements ROADMAP.md's "UI is consistent and responsive across all modules" Phase 6 success criterion, using the specific items the user selected as in-scope during planning.
Output: New `src/app/(dashboard)/loading.tsx`, `src/app/(dashboard)/error.tsx` (covering all 13 routes in the group); modified `src/components/tickets/kanban-board.tsx` (responsive breakpoint); modified `src/components/tickets/sla-badge.tsx` (theme tokens).
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-launch-prep/06-CONTEXT.md

Relevant source files:
@src/app/(dashboard)/layout.tsx
@src/components/tickets/kanban-board.tsx
@src/components/tickets/sla-badge.tsx
@src/components/tickets/kanban-column.tsx
@src/app/globals.css
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-frontend-developer
Task: Add route-group-level `loading.tsx`/`error.tsx`; add a responsive breakpoint to the Kanban board; migrate `SlaBadge` from hardcoded colors to theme tokens.
Scope:
- Read targets: `src/app/(dashboard)/layout.tsx` (confirm the route group's exact structure this plan's new files will sit alongside), `src/components/tickets/kanban-board.tsx` (full file, already summarized in `06-CONTEXT.md` -- re-read directly), `src/components/tickets/kanban-column.tsx` (confirm column-level layout this plan may need to coordinate with), `src/components/tickets/sla-badge.tsx` (full file, already summarized in `06-CONTEXT.md`), `src/app/globals.css` (confirm the actual theme-token/CSS-variable names this project's shadcn/Tailwind setup defines, e.g. for success/warning/destructive semantics -- do not invent token names that don't exist in this codebase's theme)
- Write targets: `src/app/(dashboard)/loading.tsx` (new), `src/app/(dashboard)/error.tsx` (new), `src/components/tickets/kanban-board.tsx` (modify), `src/components/tickets/sla-badge.tsx` (modify)
- Forbidden targets: `src/lib/crypto.ts`, `src/lib/qbo.ts`, `src/lib/actions/tickets.ts`, `src/middleware.ts`, `prisma/schema.prisma`, `prisma/migrations/**`, `playwright.config.ts`, `package.json`, any file under `e2e/**`, `.env.example`, `src/lib/sla.ts` (read-only -- `SlaBadge` consumes `getSlaStatus` from here but this plan does not change SLA-status derivation logic, only its visual rendering)
Allowed tools/actions:
- Read, Edit, Write on the write targets listed above
- Bash: `npx tsc --noEmit`
Forbidden actions:
- Do not change `SlaBadge`'s status-to-label or status-to-variant mapping (`STATUS_LABEL`, `STATUS_VARIANT`) -- only `STATUS_CLASS`'s hardcoded color values change to theme-token-based classes. The five SLA statuses (`no_sla`/`on_track`/`approaching`/`breached`/`met`) must remain visually distinguishable from one another after this change.
- Do not change `getSlaStatus`'s derivation logic in `src/lib/sla.ts` -- this plan only touches rendering.
- Do not remove `KeyboardSensor` or any existing accessibility affordance from `KanbanBoard` while adding responsive behavior.
- Do not add a new CSS/design-token system or a new dependency (e.g. a responsive-table library) -- use Tailwind's existing responsive-prefix utilities (`sm:`/`md:`/`lg:`) and this project's existing theme tokens, matching the codebase's established Tailwind v4 + shadcn/ui conventions.
- Do not self-defer planned work.
Implementation sequence:
1. Read `src/app/(dashboard)/layout.tsx` to confirm the route group root and that `loading.tsx`/`error.tsx` siblings at that path will correctly apply to all 13 nested routes (Next.js App Router convention: a `loading.tsx`/`error.tsx` at a route-group segment applies to all routes nested under it).
2. Create `src/app/(dashboard)/loading.tsx`: a simple, reusable loading UI (e.g. a centered spinner or skeleton block using existing shadcn/ui primitives if a suitable one exists, otherwise a minimal Tailwind-styled placeholder) shown automatically by Next.js while any nested route's Server Component data-fetch is in flight. Keep this genuinely simple -- this is a baseline consistency fix, not a bespoke per-page skeleton design.
3. Create `src/app/(dashboard)/error.tsx`: MUST start with `"use client"` (Next.js requirement -- error boundaries are Client Components). Accepts the standard `{ error, reset }: { error: Error & { digest?: string }; reset: () => void }` props Next.js passes to error boundary components. Renders a clear, non-generic error message and a "Try again" button wired to call `reset()`. Do not leak `error.message`/`error.stack` details that could expose internals to a non-admin user in production -- show a generic user-facing message, and only log the actual error server-side-adjacent details via `console.error(error)` inside a `useEffect` (the standard Next.js error.tsx pattern for capturing the error for observability while showing a safe message to the user).
4. Modify `src/components/tickets/kanban-board.tsx`: add a responsive breakpoint. Read `kanban-column.tsx` first to understand the current column-rendering structure. Implement one of: (a) below a breakpoint (e.g. `md:`), switch from a horizontal multi-column layout to a horizontally-scrollable container with visible columns (`overflow-x-auto` + fixed/min column widths, so a user can swipe/scroll between columns on narrow viewports), or (b) below the breakpoint, render columns stacked vertically with a way to see all 5 statuses without losing context. Choose whichever fits the existing component structure with the smallest, safest diff -- document the choice in the SUMMARY. Preserve all existing drag-and-drop, keyboard-sensor, and optimistic-update-race-guard behavior (do not touch the `isMutatingRef`/`pendingTicketsRef` logic).
5. Modify `src/components/tickets/sla-badge.tsx`: read `src/app/globals.css` to find this project's actual defined theme tokens/CSS variables (e.g. semantic color variables already used elsewhere for success/warning/danger states, if they exist; if this project's theme only defines the shadcn/ui default set with no custom success/warning tokens, use the closest existing semantic token and note the limitation in the SUMMARY rather than inventing new tokens not defined anywhere in this codebase). Replace `STATUS_CLASS`'s hardcoded `bg-green-600`/`bg-yellow-500` values with theme-token-based classes so the badge adapts correctly in dark mode. Do not change `STATUS_LABEL` or `STATUS_VARIANT`.
6. Run `npx tsc --noEmit`.
Required interfaces/content structure:
- `src/app/(dashboard)/loading.tsx`: default export, no props (Next.js `loading.tsx` convention), renders a lightweight loading indicator.
- `src/app/(dashboard)/error.tsx`: `"use client"` first line. Default export accepting `{ error, reset }`. Calls `reset()` on retry action. Does not render raw `error.message`/`error.stack` to the user.
- `kanban-board.tsx`: existing exported `KanbanBoard({ tickets, currentUserRole })` signature unchanged. Responsive behavior is CSS/layout-only -- no new props, no new client-state beyond what already exists.
- `sla-badge.tsx`: existing exported `SlaBadge({ ticket })` signature and `STATUS_LABEL`/`STATUS_VARIANT` unchanged. Only `STATUS_CLASS` values change.
Edge/error cases:
- `error.tsx` must handle the case where `error.digest` is present (Next.js includes this for server-side errors) vs. absent (client-side errors) -- displaying the digest (if present) as a small reference code for support purposes is acceptable and often useful, but do not display the raw error message/stack.
- The Kanban board's responsive fix must not break the existing desktop layout -- verify the desktop (`lg:`+) appearance is visually unchanged from before this plan.
- `SlaBadge`'s `breached` status currently has an EMPTY `STATUS_CLASS` entry (`breached: ""`) relying entirely on `STATUS_VARIANT`'s `"destructive"` badge variant for its color -- confirm whether this is intentional (shadcn's `destructive` variant may already be theme-token-based) before deciding whether `breached` needs a class change too; if `STATUS_VARIANT`'s `destructive` variant is already theme-token-based, `breached` needs no `STATUS_CLASS` change and this should be noted, not treated as a gap.
Verification criteria:
- `npx tsc --noEmit` exits 0
- `loading.tsx` and `error.tsx` exist once each at the route-group root, not duplicated per-route
- `error.tsx` is a Client Component, calls `reset()`, does not leak raw error details
- Kanban board has verifiably different (improved) layout behavior below its chosen breakpoint, with desktop layout unchanged
- `SlaBadge`'s 5 statuses remain visually distinguishable from each other, now via theme tokens instead of hardcoded colors
- No files outside `files_modified` were touched
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made (responsive strategy chosen for Kanban, theme tokens used for SlaBadge, breached-status class decision)
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `src/app/globals.css` defines no usable semantic theme tokens at all (not even the shadcn/ui defaults), making a token-based `SlaBadge` migration impossible without inventing new tokens.
- `kanban-column.tsx`'s structure is incompatible with either responsive strategy described in Implementation sequence Step 4 without a much larger refactor than this plan's scope allows.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, and `06-05-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx tsc --noEmit` before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Add route-group loading and error boundaries</name>
  <files>src/app/(dashboard)/loading.tsx, src/app/(dashboard)/error.tsx</files>
  <action>
Read `src/app/(dashboard)/layout.tsx` to confirm the route-group root. Create `src/app/(dashboard)/loading.tsx` with a simple loading indicator (default export, no props). Create `src/app/(dashboard)/error.tsx` starting with `"use client"`, accepting `{ error, reset }` props per Next.js's error-boundary convention, rendering a clear generic user-facing message plus a "Try again" button calling `reset()`, and logging the actual error via `console.error` in a `useEffect` without displaying raw error details to the user.

> verification: test -f 'src/app/(dashboard)/loading.tsx'
> verification: test -f 'src/app/(dashboard)/error.tsx'
> verification: grep -q '"use client"' 'src/app/(dashboard)/error.tsx'
> verification: grep -q 'reset' 'src/app/(dashboard)/error.tsx'
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f 'src/app/(dashboard)/loading.tsx'
test -f 'src/app/(dashboard)/error.tsx'
grep -q '"use client"' 'src/app/(dashboard)/error.tsx'
grep -q 'reset' 'src/app/(dashboard)/error.tsx'
npx tsc --noEmit
  </verify>
  <done>All 13 dashboard routes now show a real loading indicator during data-fetch and a scoped, retry-capable error boundary on failure, instead of Next.js's generic screens.</done>
</task>

<task type="auto">
  <name>Task 2: Add a responsive breakpoint to the Kanban board</name>
  <files>src/components/tickets/kanban-board.tsx</files>
  <action>
Read `kanban-board.tsx` and `kanban-column.tsx` in full. Add a responsive layout strategy (horizontal scroll with visible columns, or stacked columns below a breakpoint -- choose the smaller, safer diff for this component's existing structure) so the board is usable below desktop width. Preserve all existing drag-and-drop, `KeyboardSensor`, and the `isMutatingRef`/`pendingTicketsRef` optimistic-update-race guard untouched. Desktop (`lg:`+) layout must remain visually unchanged.

> verification: grep -q 'md:\|sm:\|lg:' src/components/tickets/kanban-board.tsx
> verification: grep -q 'KeyboardSensor' src/components/tickets/kanban-board.tsx
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q 'md:\|sm:\|lg:' src/components/tickets/kanban-board.tsx
grep -q 'KeyboardSensor' src/components/tickets/kanban-board.tsx
npx tsc --noEmit
  </verify>
  <done>The Kanban board has a genuinely usable narrow-viewport layout while desktop appearance and all existing drag/keyboard/race-guard behavior are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate SlaBadge to theme tokens</name>
  <files>src/components/tickets/sla-badge.tsx</files>
  <action>
Read `src/app/globals.css` to confirm this project's actual defined theme tokens. Replace `STATUS_CLASS`'s hardcoded `bg-green-600`/`bg-yellow-500` Tailwind color-utility values with theme-token-based classes so the badge adapts in dark mode. Confirm whether `breached`'s empty `STATUS_CLASS` (relying on `STATUS_VARIANT`'s `destructive` variant) is already theme-token-based and needs no change, or needs one -- document the finding. Do not change `STATUS_LABEL` or `STATUS_VARIANT`; all 5 statuses must remain visually distinguishable.

> verification: grep -qv 'bg-green-600\|bg-yellow-500' src/components/tickets/sla-badge.tsx
> verification: grep -q 'STATUS_LABEL' src/components/tickets/sla-badge.tsx
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -qv 'bg-green-600\|bg-yellow-500' src/components/tickets/sla-badge.tsx
grep -q 'STATUS_LABEL' src/components/tickets/sla-badge.tsx
npx tsc --noEmit
  </verify>
  <done>SlaBadge renders all 5 SLA statuses using this project's existing theme tokens instead of hardcoded colors, correctly adapting to dark mode, with unchanged labels/variants.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] `loading.tsx`/`error.tsx` exist once at the route-group root, covering all 13 routes
- [ ] `error.tsx` is a Client Component, retries via `reset()`, does not leak raw error details
- [ ] Kanban board has an improved narrow-viewport layout with desktop layout and all existing interaction behavior unchanged
- [ ] SlaBadge uses theme tokens, all 5 statuses remain visually distinguishable
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- UI is consistent and responsive across all modules (ROADMAP.md success criterion) -- specifically: loading/error states exist project-wide, Kanban board works at narrow viewports, SlaBadge supports dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-launch-prep/06-05-SUMMARY.md` documenting what was done, verification results, the responsive strategy and theme tokens chosen, and any deviations.
</output>
