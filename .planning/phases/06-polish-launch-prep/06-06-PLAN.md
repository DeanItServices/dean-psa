---
phase: 06-polish-launch-prep
plan: 06
type: execute
wave: 2
depends_on: ["06-04", "06-02"]
files_modified:
  - e2e/tickets.spec.ts
files_forbidden:
  - src/lib/crypto.ts
  - src/lib/qbo.ts
  - src/lib/actions/tickets.ts
  - src/middleware.ts
  - prisma/schema.prisma
  - prisma/migrations/**
  - playwright.config.ts
  - package.json
  - e2e/fixtures.ts
  - e2e/time-entry-to-invoice.spec.ts
  - e2e/sla-tracking.spec.ts
  - src/components/**
  - src/app/**
sequential_files: []
expected_artifacts:
  - path: "e2e/tickets.spec.ts"
    provides: "Playwright spec covering ticket creation, Kanban status transitions, assignment, and ownership-scoped delete"
    required: true
autonomous: false
agents: ["testing-qa-verification-specialist"]
requirements: ["Core workflows (ticket lifecycle) pass end-to-end verification"]
user_setup: []
verification_commands:
  - "test -f e2e/tickets.spec.ts"
  - "grep -q 'loginAs' e2e/tickets.spec.ts"
  - "grep -q 'test(' e2e/tickets.spec.ts"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "The spec imports and uses loginAs/ROLE_CREDENTIALS from e2e/fixtures.ts rather than reimplementing login navigation."
    - "The spec covers, at minimum: creating a ticket, moving it through at least one Kanban status transition, assigning it to a technician, and the ownership-scoped delete behavior added by Plan 06-02 (a technician can delete their own assigned ticket; a technician cannot delete a ticket assigned to someone else)."
    - "Selectors used in the spec are derived from reading the actual rendered pages (ticket-form.tsx, kanban-board.tsx, kanban-column.tsx, ticket-card.tsx, the ticket detail page) during this plan's execution, not guessed or invented."
    - "The spec creates its own test data (a fresh ticket) rather than depending on specific pre-seeded ticket rows existing, so it is safely re-runnable without manual DB cleanup between runs."
  artifacts:
    - path: "e2e/tickets.spec.ts"
      provides: "Playwright test file"
      min_lines: 40
      contains: "loginAs"
  key_links:
    - from: "e2e/tickets.spec.ts"
      to: "e2e/fixtures.ts"
      via: "spec imports loginAs and ROLE_CREDENTIALS"
      pattern: "from \"./fixtures\""
---

<objective>
Write a Playwright E2E spec covering the ticket lifecycle workflow: creation, Kanban board status transitions, assignment, and the newly ownership-scoped delete behavior from Plan 06-02.

Purpose: Implements one of the three core workflows named in ROADMAP.md's "Core workflows (ticket lifecycle, time entry to invoice, SLA tracking) pass end-to-end verification" Phase 6 success criterion.
Output: New `e2e/tickets.spec.ts`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-launch-prep/06-CONTEXT.md
@.planning/phases/06-polish-launch-prep/06-04-SUMMARY.md
@.planning/phases/06-polish-launch-prep/06-02-SUMMARY.md

Relevant source files:
@e2e/fixtures.ts
@src/components/tickets/ticket-form.tsx
@src/components/tickets/kanban-board.tsx
@src/components/tickets/kanban-column.tsx
@src/app/(dashboard)/tickets/new/page.tsx
@src/app/(dashboard)/tickets/[ticketId]/page.tsx
@src/lib/actions/tickets.ts
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: testing-qa-verification-specialist
Task: Write `e2e/tickets.spec.ts` covering ticket creation, Kanban status transition, assignment, and ownership-scoped delete.
Scope:
- Read targets: `e2e/fixtures.ts` (confirm the actual exported `loginAs`/`ROLE_CREDENTIALS` signatures from Plan 06-04's completed work -- read `06-04-SUMMARY.md` first to confirm what was actually built), `06-02-SUMMARY.md` (confirm the actual ownership-check error message/behavior Plan 06-02 implemented, so this spec asserts against real behavior not assumed behavior), `src/components/tickets/ticket-form.tsx`, `src/components/tickets/kanban-board.tsx`, `src/components/tickets/kanban-column.tsx`, `src/app/(dashboard)/tickets/new/page.tsx`, `src/app/(dashboard)/tickets/[ticketId]/page.tsx`, `src/lib/actions/tickets.ts` (all read to derive real selectors and real Server Action behavior -- do not invent DOM structure)
- Write targets: `e2e/tickets.spec.ts` (new)
- Forbidden targets: `src/lib/crypto.ts`, `src/lib/qbo.ts`, `src/lib/actions/tickets.ts` (read-only), `src/middleware.ts`, `prisma/schema.prisma`, `prisma/migrations/**`, `playwright.config.ts`, `package.json`, `e2e/fixtures.ts` (read-only -- do not modify the shared fixture, only import from it; if it's missing an export this spec needs, emit BLOCKED rather than editing it), `e2e/time-entry-to-invoice.spec.ts`, `e2e/sla-tracking.spec.ts`, any file under `src/components/**` or `src/app/**`
Allowed tools/actions:
- Read, Edit, Write on the write targets listed above
- Bash: `npx tsc --noEmit`
Forbidden actions:
- Do not modify `e2e/fixtures.ts` -- if it lacks something this spec needs, emit BLOCKED with the exact missing export named, rather than editing a file owned by Plan 06-04.
- Do not run `npx playwright test` as part of this plan's verification (the browser binary may not be installed in this execution environment per Plan 06-04's `user_setup` note) -- `npx tsc --noEmit` passing is this plan's verification bar; actual spec execution is a follow-up manual/CI step, not required for this plan to be marked complete. State this clearly in the SUMMARY.
- Do not hardcode a specific pre-existing ticket ID or company ID from seed data -- create the ticket(s) this spec needs within the spec itself (or via `beforeEach`/`beforeAll`), so the spec is safely re-runnable.
- Do not self-defer planned work.
Implementation sequence:
1. Read `06-04-SUMMARY.md` and `06-02-SUMMARY.md` to confirm exactly what Plans 06-04 and 06-02 actually built (fixture exports, ownership-check error message/UI behavior) -- do not assume the PLAN.md descriptions are exactly what was implemented; SUMMARY.md is the authoritative record of what shipped.
2. Read `e2e/fixtures.ts` directly to confirm its real exports.
3. Read `ticket-form.tsx`, `tickets/new/page.tsx`, and `tickets/[ticketId]/page.tsx` to derive real form-field selectors (labels, input names/ids) for ticket creation and the detail page's structure.
4. Read `kanban-board.tsx` and `kanban-column.tsx` to derive real selectors for drag-and-drop or an alternative status-change UI affordance (check whether the ticket detail page also offers a non-drag way to change status, e.g. a select/button, which is easier to automate reliably in Playwright than simulating a full drag gesture -- prefer that if it exists; if status change is ONLY possible via drag-and-drop, use Playwright's drag simulation APIs, or invoke the underlying Server Action's effect via the UI's actual interactive elements, whichever is more reliable given what's actually rendered).
5. Write `e2e/tickets.spec.ts` with (at minimum) these test cases, using Playwright's `test.describe`/`test` structure:
   - **Create a ticket**: `loginAs(page, "dispatcher")` (a role with `ticket:manage`), navigate to the new-ticket form, fill required fields (a real seeded company must be selectable -- confirm via reading the form how company selection works), submit, assert the ticket detail page renders with the submitted subject/description.
   - **Kanban status transition**: from the tickets list/board, move the just-created ticket from its initial status to a different status (via whatever real UI affordance exists per Step 4's findings), assert the change persisted (e.g. reload and confirm the new status, or assert the UI reflects it without reload if that's how the app behaves).
   - **Assignment**: as a `dispatcher` (has `ticket:assign`), assign the ticket to a technician; assert the assignment persisted.
   - **Ownership-scoped delete (Plan 06-02's new behavior)**: `loginAs(page, "technician")`, attempt to delete a ticket NOT assigned to that technician (create a second ticket assigned to a different technician, or use the dispatcher-created one from above if it wasn't assigned to this specific technician), assert the delete is rejected with the real error message/behavior confirmed from `06-02-SUMMARY.md` in Step 1. Then attempt to delete a ticket that IS assigned to the logged-in technician, and assert it succeeds.
6. Run `npx tsc --noEmit`.
Required interfaces/content structure:
- File imports `loginAs`/`ROLE_CREDENTIALS` from `"./fixtures"` (relative import within `e2e/`).
- Uses Playwright's standard `test`/`expect` imports from `@playwright/test`.
- Each `test(...)` block is independently runnable (creates whatever data it needs, does not depend on execution order between tests unless explicitly structured via `test.describe.serial` with a documented reason).
Edge/error cases:
- If ticket creation requires selecting a company/contact/asset from a dropdown populated by seeded CRM data, and no seeded CRM data is confirmed to exist in a fresh environment, note this as a dependency in the SUMMARY (this spec assumes the standard seeded dev dataset described in STATE.md exists) rather than silently failing to handle it.
- If the ownership-scoped delete UI doesn't surface the Server Action's error message directly in a way Playwright can assert against (e.g. if `deleteTicket`'s `{ error: ... }` return isn't rendered anywhere in the current UI), the spec should instead assert the negative case correctly (ticket still exists after the attempted delete, e.g. by reloading and confirming it's still listed) rather than asserting on UI text that may not exist -- verify this by reading the actual ticket detail page's delete-button wiring before writing the assertion.
Verification criteria:
- `npx tsc --noEmit` exits 0
- Spec imports and uses `e2e/fixtures.ts`'s real exports
- Spec covers all 4 named workflow segments (create, status transition, assign, ownership-scoped delete)
- No files outside `files_modified` were touched
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made (exact selectors/UI affordances used, whether status change used drag simulation or an alternative)
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `e2e/fixtures.ts` does not export a `loginAs`/`ROLE_CREDENTIALS` matching what `06-04-SUMMARY.md` claims was built.
- `06-02-SUMMARY.md` does not exist yet or does not clearly document the ownership-check's actual error/UI behavior needed to write a correct assertion.
- The ticket creation form requires a piece of seeded reference data (company/contact/asset) that cannot be confirmed to exist via reading the seed script.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, `06-04-SUMMARY.md`, `06-02-SUMMARY.md`, and `06-06-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx tsc --noEmit` before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Write the ticket lifecycle E2E spec</name>
  <files>e2e/tickets.spec.ts</files>
  <action>
Read `06-04-SUMMARY.md`, `06-02-SUMMARY.md`, `e2e/fixtures.ts`, and the ticket UI source files listed in this plan's context to derive real selectors and real ownership-check behavior. Write `e2e/tickets.spec.ts` covering: ticket creation (as dispatcher), a Kanban/detail-page status transition, assignment to a technician, and the ownership-scoped delete behavior from Plan 06-02 (technician cannot delete another's ticket, can delete their own). Import `loginAs`/`ROLE_CREDENTIALS` from `./fixtures`. Create all needed test data within the spec itself.

> verification: test -f e2e/tickets.spec.ts
> verification: grep -q 'loginAs' e2e/tickets.spec.ts
> verification: grep -q 'test(' e2e/tickets.spec.ts
> verification: grep -qi 'delete' e2e/tickets.spec.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f e2e/tickets.spec.ts
grep -q 'loginAs' e2e/tickets.spec.ts
grep -q 'test(' e2e/tickets.spec.ts
grep -qi 'delete' e2e/tickets.spec.ts
npx tsc --noEmit
  </verify>
  <done>e2e/tickets.spec.ts exercises ticket creation, status transition, assignment, and the ownership-scoped delete behavior, using real page selectors and the shared login fixture, type-checking cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] Spec covers creation, status transition, assignment, and ownership-scoped delete
- [ ] Spec uses real selectors derived from reading actual page source, not guessed
- [ ] Spec creates its own test data, safely re-runnable
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Ticket lifecycle core workflow has automated E2E verification (ROADMAP.md success criterion)
- The spec verifies Plan 06-02's ownership-scoping fix actually works end-to-end through the real UI, not just at the Server Action level
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-launch-prep/06-06-SUMMARY.md` documenting what was done, verification results, exact selectors/UI affordances used, and any deviations.
</output>
