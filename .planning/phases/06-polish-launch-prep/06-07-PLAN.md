---
phase: 06-polish-launch-prep
plan: 07
type: execute
wave: 2
depends_on: ["06-04"]
files_modified:
  - e2e/time-entry-to-invoice.spec.ts
files_forbidden:
  - src/lib/crypto.ts
  - src/lib/qbo.ts
  - src/lib/actions/tickets.ts
  - src/middleware.ts
  - prisma/schema.prisma
  - prisma/migrations/**
  - playwright.config.ts
  - package.json
  - e2e/fixtures.ts
  - e2e/tickets.spec.ts
  - e2e/sla-tracking.spec.ts
  - src/components/**
  - src/app/**
sequential_files: []
expected_artifacts:
  - path: "e2e/time-entry-to-invoice.spec.ts"
    provides: "Playwright spec covering timer start/stop, time entry creation, and invoice generation from logged time"
    required: true
autonomous: false
agents: ["testing-qa-verification-specialist"]
requirements: ["Core workflows (time entry to invoice) pass end-to-end verification"]
user_setup: []
verification_commands:
  - "test -f e2e/time-entry-to-invoice.spec.ts"
  - "grep -q 'loginAs' e2e/time-entry-to-invoice.spec.ts"
  - "grep -q 'test(' e2e/time-entry-to-invoice.spec.ts"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "The spec imports and uses loginAs/ROLE_CREDENTIALS from e2e/fixtures.ts rather than reimplementing login navigation."
    - "The spec covers, at minimum: starting and stopping a timer on a ticket (or creating a manual time entry if the timer flow is not reliably automatable within a short E2E test duration), and generating an invoice from that logged time, asserting the invoice reflects the correct amount per the ticket's contract billing type."
    - "Selectors used in the spec are derived from reading the actual rendered pages during this plan's execution, not guessed or invented."
    - "The spec creates its own test data (ticket, time entry) rather than depending on specific pre-seeded rows, so it is safely re-runnable."
  artifacts:
    - path: "e2e/time-entry-to-invoice.spec.ts"
      provides: "Playwright test file"
      min_lines: 35
      contains: "loginAs"
  key_links:
    - from: "e2e/time-entry-to-invoice.spec.ts"
      to: "e2e/fixtures.ts"
      via: "spec imports loginAs and ROLE_CREDENTIALS"
      pattern: "from \"./fixtures\""
---

<objective>
Write a Playwright E2E spec covering the time-entry-to-invoice workflow: logging billable time against a ticket and generating an invoice from it.

Purpose: Implements one of the three core workflows named in ROADMAP.md's "Core workflows (ticket lifecycle, time entry to invoice, SLA tracking) pass end-to-end verification" Phase 6 success criterion.
Output: New `e2e/time-entry-to-invoice.spec.ts`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-launch-prep/06-CONTEXT.md
@.planning/phases/06-polish-launch-prep/06-04-SUMMARY.md

Relevant source files:
@e2e/fixtures.ts
@src/components/tickets/time-entry-list.tsx
@src/app/(dashboard)/tickets/[ticketId]/page.tsx
@src/app/(dashboard)/invoices/page.tsx
@src/app/(dashboard)/invoices/[invoiceId]/page.tsx
@src/lib/billing.ts
@src/lib/timer.ts
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: testing-qa-verification-specialist
Task: Write `e2e/time-entry-to-invoice.spec.ts` covering timer/time-entry creation and invoice generation.
Scope:
- Read targets: `e2e/fixtures.ts` (confirm real exports, cross-reference `06-04-SUMMARY.md`), `src/components/tickets/time-entry-list.tsx`, `src/app/(dashboard)/tickets/[ticketId]/page.tsx` (timer UI location), `src/app/(dashboard)/invoices/page.tsx` (invoice generation entry point -- likely a date-range-driven generate action per Phase 4), `src/app/(dashboard)/invoices/[invoiceId]/page.tsx` (invoice detail rendering, for assertions), `src/lib/billing.ts` (read-only, to understand what a correct computed total should look like for the contract type this spec's test data uses -- so the spec's assertion is mathematically correct, not a guess), `src/lib/timer.ts` (read-only, timer start/stop semantics)
- Write targets: `e2e/time-entry-to-invoice.spec.ts` (new)
- Forbidden targets: `src/lib/crypto.ts`, `src/lib/qbo.ts`, `src/lib/actions/tickets.ts`, `src/middleware.ts`, `prisma/schema.prisma`, `prisma/migrations/**`, `playwright.config.ts`, `package.json`, `e2e/fixtures.ts` (read-only), `e2e/tickets.spec.ts`, `e2e/sla-tracking.spec.ts`, any file under `src/components/**` or `src/app/**`
Allowed tools/actions:
- Read, Edit, Write on the write targets listed above
- Bash: `npx tsc --noEmit`
Forbidden actions:
- Do not modify `e2e/fixtures.ts` -- if it lacks something needed, emit BLOCKED naming the exact missing export.
- Do not run `npx playwright test` as part of this plan's verification -- `npx tsc --noEmit` passing is the bar; note in the SUMMARY that actual execution is a follow-up step.
- Do not hardcode a real-time `sleep`/`wait` longer than a few seconds to simulate elapsed timer duration if the app's timer UI requires real wall-clock time to accumulate billable minutes -- if the actual timer implementation requires waiting for real elapsed time to produce a non-zero duration, prefer creating a time entry through whatever manual/direct entry UI exists (if any) instead of a live timer wait, to keep the spec fast and reliable; if no manual entry UI exists and only a live timer is available, use the shortest defensible wait and document why in the SUMMARY.
- Do not self-defer planned work.
Implementation sequence:
1. Read `06-04-SUMMARY.md` and `e2e/fixtures.ts` to confirm real fixture exports.
2. Read `time-entry-list.tsx` and the ticket detail page to find the actual timer start/stop UI and/or manual time-entry creation UI -- determine which is more reliably automatable (per the Forbidden actions guidance above) and use that.
3. Read `src/lib/billing.ts` and `src/lib/timer.ts` to understand the contract-type-specific billing computation this spec's assertion needs to match (e.g. if the test ticket's company has an `hourly_breakfix` contract, the invoice total should equal `hourlyRate * hours logged`, computable from the spec's own known inputs).
4. Read `invoices/page.tsx` to find the actual invoice-generation UI trigger (date range picker + generate button, or similar).
5. Write `e2e/time-entry-to-invoice.spec.ts`:
   - `loginAs(page, "technician")`, navigate to a ticket (create one if needed, matching the pattern from Plan 06-06's spec for consistency), log time against it (timer or manual entry per Step 2's finding) for a known, deterministic duration.
   - `loginAs(page, "finance")` (has `invoice:manage`), navigate to invoice generation, generate an invoice covering the date range containing the just-logged time entry.
   - Assert the generated invoice's total matches the expected computed value for the test ticket's contract type and logged duration (per Step 3's billing logic), and that the time entry is now marked as invoiced/billed in the UI.
6. Run `npx tsc --noEmit`.
Required interfaces/content structure:
- File imports `loginAs`/`ROLE_CREDENTIALS` from `"./fixtures"`.
- Uses Playwright's standard `test`/`expect` imports from `@playwright/test`.
Edge/error cases:
- If the seeded/created test company's contract type is ambiguous or not confirmed, the spec should explicitly create (or select) a company/contract with a KNOWN billing type so the expected-total assertion is deterministic -- do not write an assertion against an unknown/variable contract type.
- If invoice generation requires selecting a specific company from a list (not just a date range), confirm this via reading `invoices/page.tsx` and select the correct one, not the first one in the list.
Verification criteria:
- `npx tsc --noEmit` exits 0
- Spec imports and uses `e2e/fixtures.ts`'s real exports
- Spec covers time logging AND invoice generation, with a mathematically correct total assertion
- No files outside `files_modified` were touched
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made (timer vs. manual entry chosen and why, contract type used for the deterministic assertion)
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `e2e/fixtures.ts` does not export what `06-04-SUMMARY.md` claims.
- Neither a live timer nor a manual time-entry UI can be confirmed to exist by reading the actual source -- if time entries can ONLY be created by a mechanism this spec cannot reliably automate, escalate rather than fabricating a workaround.
- No company/contract with a known, deterministic billing type can be confirmed to exist or be creatable within this spec's scope.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, `06-04-SUMMARY.md`, and `06-07-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx tsc --noEmit` before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Write the time-entry-to-invoice E2E spec</name>
  <files>e2e/time-entry-to-invoice.spec.ts</files>
  <action>
Read `06-04-SUMMARY.md`, `e2e/fixtures.ts`, the time-entry/timer UI source, `src/lib/billing.ts`, and `invoices/page.tsx` to derive real selectors and a mathematically correct expected-total computation. Write `e2e/time-entry-to-invoice.spec.ts` covering: logging time against a ticket (as technician) for a known duration, generating an invoice covering that time (as finance), and asserting the invoice total matches the correct billing computation for the test contract's billing type.

> verification: test -f e2e/time-entry-to-invoice.spec.ts
> verification: grep -q 'loginAs' e2e/time-entry-to-invoice.spec.ts
> verification: grep -q 'test(' e2e/time-entry-to-invoice.spec.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f e2e/time-entry-to-invoice.spec.ts
grep -q 'loginAs' e2e/time-entry-to-invoice.spec.ts
grep -q 'test(' e2e/time-entry-to-invoice.spec.ts
npx tsc --noEmit
  </verify>
  <done>e2e/time-entry-to-invoice.spec.ts exercises time logging and invoice generation with a deterministic, mathematically correct total assertion, using real page selectors and the shared login fixture, type-checking cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] Spec covers time logging and invoice generation with a correct total assertion
- [ ] Spec uses real selectors derived from reading actual page source
- [ ] Spec uses a known, deterministic contract billing type for its assertion
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Time-entry-to-invoice core workflow has automated E2E verification (ROADMAP.md success criterion)
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-launch-prep/06-07-SUMMARY.md` documenting what was done, verification results, the timer-vs-manual-entry decision and contract type used, and any deviations.
</output>
