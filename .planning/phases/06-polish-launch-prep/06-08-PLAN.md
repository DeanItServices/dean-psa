---
phase: 06-polish-launch-prep
plan: 08
type: execute
wave: 2
depends_on: ["06-04"]
files_modified:
  - e2e/sla-tracking.spec.ts
files_forbidden:
  - src/lib/crypto.ts
  - src/lib/qbo.ts
  - src/lib/actions/tickets.ts
  - src/middleware.ts
  - prisma/schema.prisma
  - prisma/migrations/**
  - playwright.config.ts
  - package.json
  - e2e/fixtures.ts
  - e2e/tickets.spec.ts
  - e2e/time-entry-to-invoice.spec.ts
  - src/components/**
  - src/app/**
sequential_files: []
expected_artifacts:
  - path: "e2e/sla-tracking.spec.ts"
    provides: "Playwright spec covering SLA badge display on ticket creation and the SLA compliance report"
    required: true
autonomous: false
agents: ["testing-qa-verification-specialist"]
requirements: ["Core workflows (SLA tracking) pass end-to-end verification"]
user_setup: []
verification_commands:
  - "test -f e2e/sla-tracking.spec.ts"
  - "grep -q 'loginAs' e2e/sla-tracking.spec.ts"
  - "grep -q 'test(' e2e/sla-tracking.spec.ts"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "The spec imports and uses loginAs/ROLE_CREDENTIALS from e2e/fixtures.ts rather than reimplementing login navigation."
    - "The spec covers, at minimum: a newly created ticket against a company with SLA-defining contract terms displays a real SlaBadge with a visible, non-'No SLA' status, and the SLA compliance report (a report:view_all-gated page) renders and reflects at least the created ticket's presence in its underlying data (a full breach-timing simulation is out of scope for a fast E2E test -- verifying the badge appears and the report page loads with real data is the achievable, deterministic assertion for this workflow)."
    - "Selectors used in the spec are derived from reading the actual rendered pages during this plan's execution, not guessed or invented."
    - "The spec creates its own test data (ticket against a company with a known SLA-bearing contract) rather than depending on specific pre-seeded rows, so it is safely re-runnable."
  artifacts:
    - path: "e2e/sla-tracking.spec.ts"
      provides: "Playwright test file"
      min_lines: 25
      contains: "loginAs"
  key_links:
    - from: "e2e/sla-tracking.spec.ts"
      to: "e2e/fixtures.ts"
      via: "spec imports loginAs and ROLE_CREDENTIALS"
      pattern: "from \"./fixtures\""
---

<objective>
Write a Playwright E2E spec covering the SLA tracking workflow: a newly created ticket against an SLA-bearing contract shows a real SLA status badge, and the SLA compliance report page renders real data reflecting the created ticket.

Purpose: Implements one of the three core workflows named in ROADMAP.md's "Core workflows (ticket lifecycle, time entry to invoice, SLA tracking) pass end-to-end verification" Phase 6 success criterion. Deliberately scoped to what a fast, deterministic E2E test can actually verify -- full breach-timing behavior (which requires real elapsed wall-clock time relative to SLA deadlines measured in hours) is out of scope for this spec; `src/lib/reporting.ts`'s `getSlaCompliance` and `src/lib/sla.ts`'s `getSlaStatus` already have logic-level test/verification coverage from Phase 5's review (17+ regression tests) -- this spec's job is confirming the UI actually surfaces that data correctly end-to-end, not re-deriving the business logic.
Output: New `e2e/sla-tracking.spec.ts`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-launch-prep/06-CONTEXT.md
@.planning/phases/06-polish-launch-prep/06-04-SUMMARY.md

Relevant source files:
@e2e/fixtures.ts
@src/components/tickets/sla-badge.tsx
@src/app/(dashboard)/tickets/[ticketId]/page.tsx
@src/app/(dashboard)/reports/sla/page.tsx
@src/lib/sla.ts
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: testing-qa-verification-specialist
Task: Write `e2e/sla-tracking.spec.ts` covering SLA badge display and the SLA compliance report page.
Scope:
- Read targets: `e2e/fixtures.ts` (confirm real exports, cross-reference `06-04-SUMMARY.md`), `src/components/tickets/sla-badge.tsx` (confirm its rendered output post-Plan-06-05's theme-token change -- read `06-05-SUMMARY.md` if it exists yet, otherwise the live file), `src/app/(dashboard)/tickets/[ticketId]/page.tsx` (confirm SlaBadge is rendered here and how), `src/app/(dashboard)/reports/sla/page.tsx` (confirm the report's real rendered structure -- filters, summary cards), `src/lib/sla.ts` (read-only, confirm `getSlaStatus`'s possible return values for assertion correctness)
- Write targets: `e2e/sla-tracking.spec.ts` (new)
- Forbidden targets: `src/lib/crypto.ts`, `src/lib/qbo.ts`, `src/lib/actions/tickets.ts`, `src/middleware.ts`, `prisma/schema.prisma`, `prisma/migrations/**`, `playwright.config.ts`, `package.json`, `e2e/fixtures.ts` (read-only), `e2e/tickets.spec.ts`, `e2e/time-entry-to-invoice.spec.ts`, any file under `src/components/**` or `src/app/**`
Allowed tools/actions:
- Read, Edit, Write on the write targets listed above
- Bash: `npx tsc --noEmit`
Forbidden actions:
- Do not modify `e2e/fixtures.ts` -- if it lacks something needed, emit BLOCKED naming the exact missing export.
- Do not run `npx playwright test` as part of this plan's verification -- `npx tsc --noEmit` passing is the bar; note in the SUMMARY that actual execution is a follow-up step.
- Do not attempt to simulate an actual SLA breach by waiting real wall-clock hours -- that is explicitly out of scope per this plan's objective; assert on badge presence/status-not-being-"No SLA" and report-page rendering with real data instead.
- Do not self-defer planned work.
Implementation sequence:
1. Read `06-04-SUMMARY.md` and `e2e/fixtures.ts` to confirm real fixture exports. If `06-05-SUMMARY.md` exists (Plan 06-05 may complete before or after this plan within Wave 2 -- both are Wave 2, order is not guaranteed), read it to confirm `SlaBadge`'s post-theme-token rendered output; otherwise read the live `sla-badge.tsx`.
2. Read the ticket detail page to confirm where/how `SlaBadge` renders, and `reports/sla/page.tsx` to confirm the report's real structure (filters, what a "no filter" org-wide view shows).
3. Write `e2e/sla-tracking.spec.ts`:
   - `loginAs(page, "dispatcher")`, create a ticket against a company/contract confirmed (via reading seed data or creating one) to have non-null `slaResponseMinutes`/`slaResolutionMinutes` set, so the created ticket has real SLA deadlines snapshotted onto it.
   - Navigate to the ticket's detail page, assert a real `SlaBadge` element is present and its text is NOT "No SLA" (i.e., a genuine status like "On track"/"Approaching"/"Breached"/"Met" renders, confirming the badge correctly reflects an SLA-bearing ticket).
   - `loginAs(page, "finance")` (or `dispatcher`/`admin` -- any `report:view_all` role), navigate to `/reports/sla`, assert the page renders without error and shows non-placeholder data (e.g. a non-zero ticket count in the underlying compliance denominator, or the specific test ticket's company visible via a company filter if the report supports filtering) reflecting that real ticket data flows into the report.
4. Run `npx tsc --noEmit`.
Required interfaces/content structure:
- File imports `loginAs`/`ROLE_CREDENTIALS` from `"./fixtures"`.
- Uses Playwright's standard `test`/`expect` imports from `@playwright/test`.
Edge/error cases:
- If no company with a defined SLA-bearing contract can be confirmed to exist in seed data, the spec must create one (or use a contract-creation flow) rather than assuming one exists -- confirm via reading seed data/CRM contract-creation UI first.
- The SLA compliance report's date-range default (current calendar month, per `05-CONTEXT.md`) means a ticket created "now" during test execution should fall within the report's default range without needing to manipulate the date filter -- confirm this assumption holds by reading the report page's actual default-range behavior.
Verification criteria:
- `npx tsc --noEmit` exits 0
- Spec imports and uses `e2e/fixtures.ts`'s real exports
- Spec asserts a real, non-"No SLA" badge on an SLA-bearing ticket, and real data rendering on the SLA compliance report page
- No files outside `files_modified` were touched
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made (which contract/company used for guaranteed SLA terms, exact report-page assertion used)
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `e2e/fixtures.ts` does not export what `06-04-SUMMARY.md` claims.
- No SLA-bearing contract/company can be confirmed to exist or be creatable within this spec's scope.
- `reports/sla/page.tsx`'s actual structure provides no way to assert "real data is present" deterministically (e.g. if the only rendered output is aggregate percentages with no way to confirm they reflect the newly created ticket specifically).
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, `06-04-SUMMARY.md`, and `06-08-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Re-run `npx tsc --noEmit` before continuing.
5. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Write the SLA tracking E2E spec</name>
  <files>e2e/sla-tracking.spec.ts</files>
  <action>
Read `06-04-SUMMARY.md`, `e2e/fixtures.ts`, `sla-badge.tsx` (or `06-05-SUMMARY.md` if it exists), the ticket detail page, and `reports/sla/page.tsx` to derive real selectors. Write `e2e/sla-tracking.spec.ts` covering: creating a ticket against an SLA-bearing contract and asserting a real (non-"No SLA") `SlaBadge` renders on its detail page, then asserting the SLA compliance report page renders with real data reflecting the created ticket.

> verification: test -f e2e/sla-tracking.spec.ts
> verification: grep -q 'loginAs' e2e/sla-tracking.spec.ts
> verification: grep -q 'test(' e2e/sla-tracking.spec.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f e2e/sla-tracking.spec.ts
grep -q 'loginAs' e2e/sla-tracking.spec.ts
grep -q 'test(' e2e/sla-tracking.spec.ts
npx tsc --noEmit
  </verify>
  <done>e2e/sla-tracking.spec.ts exercises SLA badge display on ticket creation and the SLA compliance report's rendering of real data, using real page selectors and the shared login fixture, type-checking cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] Spec asserts a real, non-"No SLA" badge and real report-page data rendering
- [ ] Spec uses real selectors derived from reading actual page source
- [ ] Spec creates or confirms SLA-bearing test data rather than assuming
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- SLA tracking core workflow has automated E2E verification (ROADMAP.md success criterion), scoped to what a fast deterministic test can actually assert
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-launch-prep/06-08-SUMMARY.md` documenting what was done, verification results, the contract/company used, and any deviations.
</output>
