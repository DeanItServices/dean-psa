---
phase: 06-polish-launch-prep
plan: 09
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05", "06-06", "06-07", "06-08"]
files_modified:
  - DEPLOYMENT.md
files_forbidden:
  - src/lib/crypto.ts
  - src/lib/qbo.ts
  - src/lib/actions/tickets.ts
  - src/middleware.ts
  - prisma/schema.prisma
  - prisma/migrations/**
  - playwright.config.ts
  - package.json
  - e2e/**
  - src/components/**
  - src/app/**
  - .env.example
  - docker-compose.yml
  - Dockerfile
sequential_files: []
expected_artifacts:
  - path: "DEPLOYMENT.md"
    provides: "Reproducible deployment runbook for the MSP's self-hosted infrastructure"
    required: true
autonomous: true
agents: ["engineering-infrastructure-devops"]
requirements: ["Docker-based deployment is documented and reproducible on the MSP's own infrastructure"]
user_setup: []
verification_commands:
  - "test -f DEPLOYMENT.md"
  - "grep -q 'TOKEN_ENCRYPTION_KEY' DEPLOYMENT.md"
  - "grep -q 'docker compose' DEPLOYMENT.md"
  - "grep -q 'db:migrate:deploy' DEPLOYMENT.md"

must_haves:
  truths:
    - "DEPLOYMENT.md documents every environment variable actually present in .env.example as it exists after Plan 06-01's TOKEN_ENCRYPTION_KEY addition -- cross-checked against the real file, not reconstructed from memory."
    - "DEPLOYMENT.md documents the exact reproducible sequence: clone/pull, .env setup, docker compose build/up, database migration (npm run db:migrate:deploy, which chains prisma migrate deploy + scripts/post-migrate.sh), and first-run verification (e.g. logging in with a real account -- not the NODE_ENV=production-guarded seed users)."
    - "DEPLOYMENT.md documents the rate-limiting and ownership-scoped-delete behavior changes from Plan 06-02 only insofar as they affect deployment/operations (e.g. rate-limit thresholds an admin might need to tune) -- it does not re-document application-level RBAC logic that belongs in code comments, not a deployment runbook."
    - "DEPLOYMENT.md documents how to run the new E2E test suite (npm run test:e2e, plus the one-time npx playwright install --with-deps step) as a pre-deployment or post-deployment verification step, matching ROADMAP's 'core workflows pass end-to-end verification' criterion being operationally actionable, not just existing as code."
  artifacts:
    - path: "DEPLOYMENT.md"
      provides: "Deployment runbook"
      min_lines: 40
      contains: "docker compose"
  key_links: []
---

<objective>
Write a deployment runbook documenting the full, reproducible steps to deploy this application on the MSP's own self-hosted infrastructure via Docker Compose, incorporating every environment variable and operational step introduced across this phase's other plans (token encryption key, E2E test execution, rate-limit configuration).

Purpose: Directly implements ROADMAP.md's "Docker-based deployment is documented and reproducible on the MSP's own infrastructure" Phase 6 success criterion. Sequenced last (Wave 3, depends on all other Phase 6 plans) because it must document the FINAL state of the app after all other Phase 6 changes land, not a snapshot from before them.
Output: New `DEPLOYMENT.md` at the repository root.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-launch-prep/06-CONTEXT.md
@.planning/phases/06-polish-launch-prep/06-01-SUMMARY.md
@.planning/phases/06-polish-launch-prep/06-02-SUMMARY.md
@.planning/phases/06-polish-launch-prep/06-03-SUMMARY.md
@.planning/phases/06-polish-launch-prep/06-04-SUMMARY.md

Relevant source files:
@docker-compose.yml
@Dockerfile
@.env.example
@package.json
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report

Role: engineering-infrastructure-devops
Task: Write `DEPLOYMENT.md` documenting the full reproducible deployment sequence, incorporating this phase's changes.
Scope:
- Read targets: all 8 prior Phase 6 plans' SUMMARY.md files (to confirm exactly what each actually shipped -- do not document intended behavior from PLAN.md files if a SUMMARY.md shows a deviation), `docker-compose.yml` (full, current state), `Dockerfile` (full, current state), `.env.example` (full, current state -- post-Plan-06-01's `TOKEN_ENCRYPTION_KEY` addition), `package.json` (full, current state -- post-Plan-06-04's `test:e2e` script addition)
- Write targets: `DEPLOYMENT.md` (new, repository root)
- Forbidden targets: every code file from the other 8 Phase 6 plans (this plan documents, does not modify, their output), `docker-compose.yml`, `Dockerfile` (read-only -- if this plan's author believes either needs a change to be truly deployable, note it as a finding in the SUMMARY rather than editing infrastructure files outside this plan's scope)
Allowed tools/actions:
- Read, Write on `DEPLOYMENT.md`
- Bash: none required (this is a documentation-only plan; `autonomous: true` since no agent-specific execution judgment beyond writing accurate documentation is needed, though `engineering-infrastructure-devops` personality is still assigned for domain-appropriate structure/tone)
Forbidden actions:
- Do not modify any file other than `DEPLOYMENT.md`.
- Do not document aspirational/planned behavior -- read each prior plan's actual SUMMARY.md and document what was actually shipped, noting any deviation from the original plan explicitly if it affects deployment steps.
- Do not duplicate `.env.example`'s inline comments verbatim as the sole content -- synthesize them into an operator-facing runbook with sequence and rationale, not just a copy-paste of the env file.
- Do not self-defer planned work.
Implementation sequence:
1. Read all 8 prior Phase 6 plans' SUMMARY.md files in full to confirm actual shipped behavior (env vars added, scripts added, any deviations from plan).
2. Read `docker-compose.yml`, `Dockerfile`, `.env.example`, and `package.json` in their current (post-Phase-6) state.
3. Write `DEPLOYMENT.md` with these sections:
   - **Prerequisites**: Docker + Docker Compose installed on the target host, network/firewall considerations for a self-hosted MSP deployment (e.g. which ports need to be reachable, and from where -- internal network only vs. internet-reachable, referencing the new rate-limiting from Plan 06-02 as a relevant consideration for internet exposure).
   - **First-time setup**: clone the repo, copy `.env.example` to `.env`, fill in every required variable (walk through each group -- DB_PORT/DATABASE_URL, AUTH_SECRET/AUTH_URL/AUTH_TRUST_HOST, Microsoft Graph vars for the email poller, QBO vars, and the new `TOKEN_ENCRYPTION_KEY` from Plan 06-01 with its generation command).
   - **Build and start**: `docker compose build`, `docker compose up -d`, confirm all 3 services (`app`, `email-poller`, `db`) are healthy.
   - **Database migration**: run `npm run db:migrate:deploy` (documenting that this chains `prisma migrate deploy` + `scripts/post-migrate.sh`, the latter applying the raw-SQL partial index from Phase 4 idempotently) against the running `db` service.
   - **First-run verification**: log in with a real (non-seed, non-`NODE_ENV=production`-guarded) admin account -- note that the seeded dev users documented in STATE.md are explicitly guarded against production use and must not be relied on for a real deployment; the operator must create a real admin account (reference however account creation actually works in this app -- check if there's a signup flow or if the first admin must be seeded/created via a script, and document whichever is actually true).
   - **Running E2E verification**: `npx playwright install --with-deps chromium` (one-time), then `npm run test:e2e`, as a pre-launch or periodic verification step for the three core workflows (ticket lifecycle, time entry to invoice, SLA tracking).
   - **Operational notes**: the `DB_PORT` per-environment convention (relevant if the MSP ever runs a staging instance alongside production on the same host), the rate-limiting thresholds from Plan 06-02 and how an operator would adjust them if needed, and a pointer to `.planning/phases/04-time-tracking-billing/04-REVIEW.md`'s QBO Item-mapping caveat (the `SalesItemLineDetail.ItemRef.value` hardcoded placeholder) as a known limitation to be aware of before relying on QBO push in production.
4. Verify all documented commands/scripts actually exist by cross-referencing `package.json`'s real scripts (do not invent a script name that doesn't exist).
Required interfaces/content structure:
- Markdown document, `DEPLOYMENT.md`, repository root.
- Sections as listed in Implementation sequence Step 3, in that order.
- Every command shown is copy-pasteable and matches a real script/command confirmed to exist in this codebase.
Edge/error cases:
- If any of the 8 prior plans' SUMMARY.md files don't exist yet at the time this plan executes (should not happen given Wave 3's `depends_on` on all of them, but defend against a wave-execution ordering issue), emit `BLOCKED` naming which SUMMARY is missing rather than guessing its content.
- If `docker-compose.yml`/`Dockerfile` genuinely need a change to support a documented step (e.g. exposing a port DEPLOYMENT.md instructs the operator to use, but the compose file doesn't actually expose it), do not edit those files -- document the gap as a finding in this plan's SUMMARY.md instead, since they are outside this plan's write scope.
Verification criteria:
- `DEPLOYMENT.md` exists with all required sections
- Every documented env var matches what's actually in `.env.example`
- Every documented command matches a real `package.json` script or a real Docker Compose command
- No files outside `files_modified` were touched
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- Verification commands and outputs
- Decisions made
- Issues/errors (including any docker-compose.yml/Dockerfile gap found but not fixed, per the edge case above)
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- One or more of the 8 prior Phase 6 plans' SUMMARY.md files does not exist at execution time.
- **CORRECTED after plan critique**: one or more of the 8 prior Phase 6 plans' SUMMARY.md files EXISTS but its `Status:` field reads `Partial`, `Failed`, or `BLOCKED` rather than `Complete` -- do NOT proceed to document a deployment runbook for a phase where an upstream plan (especially 06-01, the CRITICAL encryption plan) did not fully land; a documented-but-wrong runbook is worse than no runbook, since it creates false confidence that the deployment is reproducible when it is not. Emit BLOCKED naming which plan(s) are incomplete and their actual status.
- `.env.example`'s actual current contents cannot be confirmed (file missing or unreadable).
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, the phase CONTEXT.md, and `06-09-SUMMARY.md` if it exists.
2. Run `git diff --stat` and inspect `git diff` for every file already changed.
3. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
4. Continue only from the next unverified task; do not rely on memory-only claims.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Write DEPLOYMENT.md</name>
  <files>DEPLOYMENT.md</files>
  <action>
Read all 8 prior Phase 6 plans' SUMMARY.md files plus the current `docker-compose.yml`, `Dockerfile`, `.env.example`, and `package.json`. Write `DEPLOYMENT.md` at the repository root with sections: Prerequisites, First-time setup (every env var including the new `TOKEN_ENCRYPTION_KEY`), Build and start, Database migration, First-run verification (real admin account, not seed users), Running E2E verification (`npx playwright install` + `npm run test:e2e`), and Operational notes (DB_PORT convention, rate-limit tuning, QBO Item-mapping caveat pointer). Every command documented must match a real script or command confirmed to exist.

> verification: test -f DEPLOYMENT.md
> verification: grep -q 'TOKEN_ENCRYPTION_KEY' DEPLOYMENT.md
> verification: grep -q 'docker compose' DEPLOYMENT.md
> verification: grep -q 'db:migrate:deploy' DEPLOYMENT.md
> verification: grep -q 'test:e2e' DEPLOYMENT.md
  </action>
  <verify>
test -f DEPLOYMENT.md
grep -q 'TOKEN_ENCRYPTION_KEY' DEPLOYMENT.md
grep -q 'docker compose' DEPLOYMENT.md
grep -q 'db:migrate:deploy' DEPLOYMENT.md
grep -q 'test:e2e' DEPLOYMENT.md
  </verify>
  <done>DEPLOYMENT.md documents the complete, accurate, reproducible deployment sequence reflecting Phase 6's final shipped state, with every command verified against real scripts/files.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `DEPLOYMENT.md` exists with all required sections
- [ ] Every documented env var matches the real current `.env.example`
- [ ] Every documented command matches a real script or Docker Compose command
- [ ] Any infrastructure-file gap found (but not fixed, since out of scope) is documented in the SUMMARY
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Docker-based deployment is documented and reproducible on the MSP's own infrastructure (ROADMAP.md success criterion), reflecting Phase 6's complete final state
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-launch-prep/06-09-SUMMARY.md` documenting what was done, verification results, any infrastructure gaps found but not fixed, and any deviations.
</output>
