---
phase: 07-account-management-session-freshness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/**
  - types/next-auth.d.ts
  - prisma/seed.ts
files_forbidden:
  - src/lib/session.ts
  - src/auth.ts
  - src/auth.config.ts
  - src/lib/permissions.ts
  - src/middleware.ts
  - src/lib/actions/**
  - src/app/**
  - src/components/**
  - docker-compose.yml
  - package.json
sequential_files: []
expected_artifacts:
  - path: "prisma/schema.prisma"
    provides: "User.isActive and User.mustChangePassword boolean columns with defaults"
    required: true
  - path: "types/next-auth.d.ts"
    provides: "Session.user and JWT carry isActive and mustChangePassword"
    required: true
  - path: "prisma/seed.ts"
    provides: "seeded users get isActive true and mustChangePassword false on both create and update"
    required: true
autonomous: false
agents: ["engineering-backend-architect"]
requirements: ["Additive migration adds User.isActive and User.mustChangePassword; prisma/seed.ts sets both explicitly so the E2E login fixture keeps working"]
user_setup: []
verification_commands:
  - "grep -q 'isActive' prisma/schema.prisma"
  - "grep -q 'mustChangePassword' prisma/schema.prisma"
  - "grep -q 'isActive' types/next-auth.d.ts"
  - "grep -q 'mustChangePassword' prisma/seed.ts"
  - "npx prisma validate"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "The migration is purely additive: two boolean columns with defaults, no backfill required, no column dropped or renamed, no data destroyed."
    - "isActive defaults to true and mustChangePassword defaults to false, so every pre-existing User row is active and unblocked without a data migration."
    - "prisma/seed.ts writes isActive: true and mustChangePassword: false in BOTH the create and update branches of its upsert. The current upsert uses `update: {}`, which means a re-seed against an existing database would NOT set these columns on accounts that already exist -- the E2E login fixture would then break the moment 07-02's gate goes live."
    - "types/next-auth.d.ts is updated in the same plan as the schema, because getCurrentUser() cannot return the new fields in 07-02 without this augmentation and tsc would reject it."
    - "The Role enum, hashedPassword nullability, and every existing User field are unchanged."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "two new User boolean columns"
      contains: "mustChangePassword"
    - path: "types/next-auth.d.ts"
      provides: "session/JWT type augmentation for the new fields"
      contains: "isActive"
  key_links:
    - from: "types/next-auth.d.ts"
      to: "prisma/schema.prisma"
      via: "the augmented Session.user fields must match the column names and types added to the User model exactly"
      pattern: "mustChangePassword"
---

<objective>
Add the two boolean columns every other plan in this phase depends on, and make the type system and the seed data agree with them.

Purpose: `User` has no `isActive` and no `mustChangePassword` field today (`prisma/schema.prisma:20-36`). Plans 07-02 through 07-05 all read or write at least one of them, so this is Wave 1 and nothing else can start until it lands.
Output: Modified `prisma/schema.prisma` (two columns), a new timestamped migration under `prisma/migrations/`, modified `types/next-auth.d.ts` (session + JWT augmentation), modified `prisma/seed.ts` (explicit values on create and update).
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-account-management-session-freshness/07-CONTEXT.md
@.planning/explorations/2026-09-02-launch-readiness-design.md

Relevant source files:
@prisma/schema.prisma
@prisma/seed.ts
@types/next-auth.d.ts
@src/auth.ts

Map chunks: db-002 (schema), db-005 (seed), inf-004 (next-auth type augmentation)
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report
Role: engineering-backend-architect
Task: Add `isActive` and `mustChangePassword` to the `User` model with an additive migration; extend the next-auth module augmentation to carry both; make the seed set them explicitly.
Scope:
- Read targets: `prisma/schema.prisma` (the `User` model at lines 20-36 and the `Role` enum above it), `prisma/seed.ts` (full file — note the `upsert` at lines 42-51 and its `update: {}`), `types/next-auth.d.ts` (full file, 23 lines), `src/auth.ts` (read only, to confirm what the jwt/session callbacks currently put on the token — do NOT modify it in this plan; 07-02 owns that file)
- Write targets: `prisma/schema.prisma`, `prisma/migrations/**` (new migration directory only), `types/next-auth.d.ts`, `prisma/seed.ts`
- Forbidden targets: `src/lib/session.ts` and `src/auth.ts` (07-02 owns both), `src/lib/actions/**` (07-03), anything under `src/app/**` or `src/components/**` (07-04/07-05), `src/lib/permissions.ts`, `docker-compose.yml`, `package.json`
Allowed tools/actions:
- Read, Edit, Write on the write targets listed above
- Bash: `npx prisma validate`, `npx prisma migrate dev --name add_user_activation_fields`, `npx prisma generate`, `npx tsc --noEmit`
Forbidden actions:
- Do not make the columns nullable and do not omit defaults. `isActive Boolean @default(true)` and `mustChangePassword Boolean @default(false)` are required so existing rows need no backfill and no existing user is locked out the moment 07-02 lands.
- Do not add an index on either column. Both are low-cardinality booleans on a table sized for well under 25 rows; `getCurrentUser()` looks up by primary key. An index here would be cargo-culted, not earned.
- Do not touch `src/auth.ts`'s jwt/session callbacks even though they are adjacent to the type augmentation — plan 07-02 owns that file and a concurrent edit in Wave 2 would conflict.
- Do not change `hashedPassword`'s nullability, the `Role` enum, or any existing `User` field.
- Do not edit or delete any existing migration under `prisma/migrations/` — add a new one.
- Do not self-defer planned work.
Implementation sequence:
1. Read `prisma/schema.prisma`'s `User` model in full. Confirm the existing field set and the `@@` block conventions used elsewhere in the file.
2. Add `isActive Boolean @default(true)` and `mustChangePassword Boolean @default(false)` to `User`. Place them near `role` rather than at the end, so the account-state fields read together.
3. Run `npx prisma validate`, then `npx prisma migrate dev --name add_user_activation_fields`. Confirm the generated SQL contains two `ADD COLUMN ... DEFAULT` statements and no `DROP`, no `ALTER COLUMN` on an existing column, and no data-moving statement. If the generated SQL contains anything destructive, emit BLOCKED rather than applying it.
4. Read `types/next-auth.d.ts` in full. Add `isActive: boolean` and `mustChangePassword: boolean` to the `Session["user"]` interface, to the `User` interface, and to the `JWT` interface. All three are needed: 07-02 returns these fields from `getCurrentUser()` (Session), `authorize()` returns a user object (User), and the jwt callback may carry them (JWT).
5. Read `prisma/seed.ts` in full. In the `db.user.upsert` call, add `isActive: true, mustChangePassword: false` to the `create` object AND replace `update: {}` with `update: { isActive: true, mustChangePassword: false }`. Leave `TEST_PASSWORD`, the `NODE_ENV` guard rail, and the user list untouched.
6. Run `npx prisma generate` so the Prisma client types include the new fields, then `npx tsc --noEmit`.
Required interfaces/content structure:
- `User` model gains exactly two fields: `isActive Boolean @default(true)`, `mustChangePassword Boolean @default(false)`.
- `types/next-auth.d.ts`: `Session["user"]`, `User`, and `JWT` each gain `isActive: boolean` and `mustChangePassword: boolean`.
- `prisma/seed.ts`: both branches of the upsert set both fields.
Edge/error cases:
- `npx prisma migrate dev` requires a reachable database. If `DATABASE_URL` is unset or the database is unreachable, emit BLOCKED with the exact error — do not hand-write a migration directory to work around it, because the checksum and the `_prisma_migrations` bookkeeping must come from Prisma itself.
- `npx tsc --noEmit` may fail after step 4 and before step 5 if the Prisma client has not been regenerated. Run `npx prisma generate` before concluding that a type error is real.
- If `prisma generate` reports the client is already current but `tsc` still cannot see the new fields, the generated client is stale — re-run generate with the schema path explicit rather than editing generated output.
Verification criteria:
- `npx prisma validate` exits 0
- The new migration's SQL is additive only — two `ADD COLUMN` statements with defaults, nothing else
- `npx tsc --noEmit` exits 0
- `prisma/seed.ts` sets both fields in create and in update
- No file outside `files_modified` was touched
Final result format:
- Status: Complete | Partial | Failed | BLOCKED
- Files changed
- The generated migration's SQL, quoted verbatim
- Verification commands and outputs
- Decisions made (field placement, migration name)
- Issues/errors
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `npx prisma migrate dev` generates SQL containing anything beyond two additive `ADD COLUMN` statements — in particular any `DROP`, any `ALTER COLUMN` against an existing column, or any statement that rewrites existing rows.
- The database is unreachable, so the migration cannot be generated by Prisma. Do not hand-author a migration directory.
- `types/next-auth.d.ts`'s actual structure differs from the three-interface shape described above in a way that makes it unclear where the new fields belong.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md and `07-CONTEXT.md`.
2. Run `git status --short` and `git diff` for every changed file.
3. Run `ls prisma/migrations/` and check whether an `add_user_activation_fields` migration already exists — if it does, do NOT generate a second one.
4. Compare changed files against `files_modified` and `files_forbidden`; if any unapproved change exists, emit `BLOCKED`.
5. Re-run `npx prisma validate` and `npx tsc --noEmit` before continuing.
6. Continue only from the next unverified task.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Add the two columns and generate the migration</name>
  <files>prisma/schema.prisma, prisma/migrations/**</files>
  <action>
Read `prisma/schema.prisma`'s `User` model (lines 20-36) in full. Add `isActive Boolean @default(true)` and `mustChangePassword Boolean @default(false)`, placed adjacent to `role` so the account-state fields read together. Do not add indexes on either column, do not change any existing field, and do not touch the `Role` enum. Run `npx prisma validate`, then `npx prisma migrate dev --name add_user_activation_fields`. Inspect the generated SQL before accepting it: it must contain exactly two additive `ADD COLUMN` statements with defaults and nothing destructive. If it contains anything else, emit BLOCKED.

> verification: grep -q 'isActive' prisma/schema.prisma
> verification: grep -q 'mustChangePassword' prisma/schema.prisma
> verification: npx prisma validate
  </action>
  <verify>
grep -q 'isActive' prisma/schema.prisma
grep -q 'mustChangePassword' prisma/schema.prisma
npx prisma validate
ls prisma/migrations | grep -q 'add_user_activation_fields'
  </verify>
  <done>The User model carries isActive (default true) and mustChangePassword (default false), and a new additive migration exists whose SQL adds exactly those two columns.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the next-auth module augmentation</name>
  <files>types/next-auth.d.ts</files>
  <action>
Read `types/next-auth.d.ts` in full (23 lines). Add `isActive: boolean` and `mustChangePassword: boolean` to all three declared interfaces: `Session["user"]`, `User`, and `JWT`. All three are required — 07-02 returns these fields from `getCurrentUser()` (which is typed off `Session`), `authorize()` returns a `User`, and the jwt callback carries `JWT`. Run `npx prisma generate` first so the Prisma client types exist, then `npx tsc --noEmit`.

> verification: grep -q 'isActive' types/next-auth.d.ts
> verification: grep -q 'mustChangePassword' types/next-auth.d.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -c 'isActive' types/next-auth.d.ts
grep -q 'mustChangePassword' types/next-auth.d.ts
npx prisma generate
npx tsc --noEmit
  </verify>
  <done>Session.user, User, and JWT all declare isActive and mustChangePassword, and tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Make the seed set both fields explicitly</name>
  <files>prisma/seed.ts</files>
  <action>
Read `prisma/seed.ts` in full. In the `db.user.upsert` call (lines 42-51), add `isActive: true` and `mustChangePassword: false` to the `create` object, and replace `update: {}` with `update: { isActive: true, mustChangePassword: false }`. The `update: {}` is the important half: a re-seed against a database that already has these accounts would otherwise leave the columns untouched, and the E2E login fixture would break the moment 07-02's `mustChangePassword` gate goes live. Leave `TEST_PASSWORD`, the `NODE_ENV`/`ALLOW_SEED_IN_PRODUCTION` guard rail, and the `TEST_USERS` list exactly as they are.

> verification: grep -q 'mustChangePassword' prisma/seed.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -c 'mustChangePassword' prisma/seed.ts
grep -q 'update: { isActive' prisma/seed.ts || grep -A3 'update:' prisma/seed.ts | grep -q 'isActive'
npx tsc --noEmit
  </verify>
  <done>Both the create and the update branch of the seed's upsert set isActive true and mustChangePassword false, so re-seeding an existing database keeps the fixture accounts loginable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` exits 0
- [ ] The new migration's SQL is two additive `ADD COLUMN` statements with defaults and nothing else
- [ ] `npx tsc --noEmit` exits 0
- [ ] `types/next-auth.d.ts` declares both fields on Session.user, User, and JWT
- [ ] `prisma/seed.ts` sets both fields in create AND update
- [ ] `src/auth.ts`, `src/lib/session.ts`, and everything under `src/lib/actions/` are untouched
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- Additive migration adds `User.isActive` (default true) and `User.mustChangePassword` (default false)
- `prisma/seed.ts` sets both explicitly so the E2E login fixture keeps working
- The type system can express the new fields, unblocking 07-02
</success_criteria>

<output>
Write `07-01-SUMMARY.md` recording: the migration directory name and its verbatim SQL, the three interfaces updated in `types/next-auth.d.ts`, the exact seed change (including that `update: {}` was replaced, and why), and any decision about field placement.
</output>
