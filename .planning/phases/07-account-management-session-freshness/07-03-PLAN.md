---
phase: 07-account-management-session-freshness
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/lib/actions/users.ts
  - src/lib/validations/user.ts
  - src/lib/permissions.ts
files_forbidden:
  - src/lib/session.ts
  - src/auth.ts
  - src/auth.config.ts
  - src/middleware.ts
  - prisma/**
  - types/next-auth.d.ts
  - src/app/**
  - src/components/**
  - e2e/**
  - scripts/**
sequential_files: []
expected_artifacts:
  - path: "src/lib/actions/users.ts"
    provides: "createUser, updateUserRole, resetUserPassword, deactivateUser, reactivateUser with concurrency-safe guard rails"
    required: true
  - path: "src/lib/validations/user.ts"
    provides: "zod schemas plus the shared MIN_PASSWORD_LENGTH constant"
    required: true
autonomous: false
agents: ["engineering-backend-architect", "engineering-security-engineer"]
requirements:
  - "New-user creation lowercase-normalizes email to match authorize(), and shows the generated temp password exactly once without logging it"
  - "Guard rails hold: an admin cannot deactivate or demote themselves, and at least one active admin always remains"
user_setup:
  - "npm install && npx prisma generate in this worktree"
  - "07-01's migration applied to the database this worktree points at"
verification_commands:
  - "test -f src/lib/actions/users.ts"
  - "grep -q 'toLowerCase' src/lib/actions/users.ts"
  - "grep -q 'ADMIN_MANAGE_ROLES' src/lib/permissions.ts"
  - "grep -q 'Serializable\\|advisory\\|updateMany' src/lib/actions/users.ts"
  - "grep -q 'MIN_PASSWORD_LENGTH' src/lib/validations/user.ts"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "Every exported action calls requireRole(ADMIN_MANAGE_ROLES) first, then parses, then writes, then revalidates -- the convention in src/lib/actions/companies.ts."
    - "createUser lowercase-normalizes the email before writing. authorize() looks up email.toLowerCase() (src/auth.ts:51); different casing makes the account permanently unreachable with no signal to the admin."
    - "CRITICAL -- the last-active-admin invariant is enforced by a NAMED serialization mechanism, not by wrapping a COUNT in db.$transaction(). Prisma interactive transactions on Postgres default to READ COMMITTED: a COUNT inside one does not block a concurrent transaction updating a different row, so two admins deactivating each other both see one remaining admin and both commit, leaving zero. Use one of: $transaction with isolationLevel 'Serializable' plus P2034 retry; a pg_advisory_xact_lock on a fixed key; or a conditional updateMany whose where-clause cannot match when the invariant would break."
    - "resetUserPassword refuses a self-target. A sole admin who resets their own password, loses the once-shown value, and later has their 8-hour JWT expire has NO recovery path -- bootstrap:admin refuses existing emails and self-serve reset is deferred. They already have /change-password for their own password."
    - "The generated temp password is returned once in the action's return value and never written to a log, an error message, or the database in plaintext -- only its bcrypt hash is persisted."
    - "Password policy lives in ONE exported constant that 07-04 and 07-06 import. Three independent implementations of 'minimum length' is how a bootstrap admin ends up with a one-character password."
    - "Deactivation is never deletion. No action deletes a User row; tickets, comments and time entries carry billing history."
  artifacts:
    - path: "src/lib/actions/users.ts"
      provides: "user lifecycle server actions"
      contains: "toLowerCase"
  key_links:
    - from: "src/lib/actions/users.ts"
      to: "src/lib/permissions.ts"
      via: "gates on a named ADMIN_MANAGE_ROLES constant, matching CRM_MANAGE_ROLES and TICKET_MANAGE_ROLES"
      pattern: "ADMIN_MANAGE_ROLES"
---

<objective>
Build the server-side user lifecycle — create, re-role, reset password, deactivate, reactivate — with guard rails that hold under concurrency and that cannot strand the last admin.

Purpose: There is no `db.user.create` anywhere outside `prisma/seed.ts`. This is the write half of account management; 07-05 renders it. It runs parallel with 07-02 and 07-04 in Wave 2 on a disjoint file set.
Output: New `src/lib/actions/users.ts`, new `src/lib/validations/user.ts`, one added constant in `src/lib/permissions.ts`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-account-management-session-freshness/07-CONTEXT.md
@.planning/explorations/2026-09-02-launch-readiness-design.md
@.planning/phases/07-account-management-session-freshness/07-01-SUMMARY.md

Relevant source files:
@src/lib/permissions.ts
@src/lib/actions/companies.ts
@src/lib/validations/company.ts
@src/lib/db.ts
@src/auth.ts
@prisma/seed.ts

Map chunks: auth-004 (permissions matrix), act-006 (canonical action shape), val-001 (validation convention), db-005 (bcrypt cost factor 10)
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report
Role: engineering-backend-architect (transaction boundaries) + engineering-security-engineer (password handling, privilege-escalation guard rails)
Task: Create the user-lifecycle Server Action module, its zod schemas and shared password constant, and the `ADMIN_MANAGE_ROLES` constant.
Scope:
- Read targets: `src/lib/permissions.ts` (full — `admin:manage_users` at 10, the `*_ROLES` block at 79-131), `src/lib/actions/companies.ts` (full — the canonical gate/parse/write/revalidate shape and `{ error }` returns), `src/lib/validations/company.ts`, `src/auth.ts` (READ ONLY — `email.toLowerCase()` at 51), `prisma/seed.ts` (READ ONLY — bcrypt cost factor 10), `src/lib/db.ts`
- Write targets: `src/lib/actions/users.ts` (new), `src/lib/validations/user.ts` (new), `src/lib/permissions.ts` (one constant)
- Forbidden targets: `src/lib/session.ts` and `src/auth.ts` (07-02, parallel), `src/app/(auth)/**` (07-04, parallel), all of `prisma/**` and `types/next-auth.d.ts` (07-01), `src/app/**`, `src/components/**` (07-05), `scripts/**` (07-06), `e2e/**` (07-07), `src/middleware.ts`
Allowed tools/actions:
- Read, Write, Edit on the write targets
- Bash: `npx tsc --noEmit`
Forbidden actions:
- Do not add a new `Permission` literal — `admin:manage_users` already exists at `src/lib/permissions.ts:10`.
- Do not use inline role literals in the action file; import `ADMIN_MANAGE_ROLES`.
- Do not implement the last-active-admin invariant as a plain `db.$transaction` around a COUNT. That is READ COMMITTED and does not serialize. Name and use a real mechanism (see must_haves) and state which in the summary.
- Do not log, console.log, or return in an error string the generated temp password. Structured non-secret logging (actor id, action, target id) IS permitted and encouraged — the ban is on password material, not on logs.
- Do not store any password in plaintext.
- Do not delete User rows.
- Do not add an email-sending dependency — delivery is out-of-band by design.
- Do not define a password minimum length inline; export one constant from `src/lib/validations/user.ts`.
- Do not self-defer planned work.
Implementation sequence:
1. Read `src/lib/permissions.ts`. Add `export const ADMIN_MANAGE_ROLES: Role[] = ["admin"];` to the `*_ROLES` block with a matching doc comment.
2. Write `src/lib/validations/user.ts`: export `MIN_PASSWORD_LENGTH` (choose a value and justify it — 12 with no composition rules is the recommended modern default), a `createUserSchema` and an `updateUserRoleSchema`. Zod is v4 (`package.json:36`) — the email idiom is `z.email()`, and transform ordering matters: normalize before validating, e.g. `z.string().trim().toLowerCase().pipe(z.email())`. Do not copy `validations/company.ts` blindly; it is a single-field schema and does not demonstrate this.
3. Read `src/lib/actions/companies.ts` in full for the action shape.
4. Write `src/lib/actions/users.ts` with `"use server"` and five exports: `createUser`, `updateUserRole`, `resetUserPassword`, `deactivateUser`, `reactivateUser`. Each gates on `requireRole(ADMIN_MANAGE_ROLES)` first and ends with `revalidatePath("/admin/users")`.
5. Guard rails: `updateUserRole` and `deactivateUser` refuse a self-target (compare against the id `requireRole` resolved, never a client-supplied id) and refuse to leave zero active admins. `resetUserPassword` refuses a self-target — a sole admin who resets their own password and loses the once-shown value has no recovery path at all.
6. Implement the invariant with the chosen serialization mechanism, counting only `{ role: "admin", isActive: true }` and excluding the target. Counting inactive admins would wrongly refuse demoting an already-inactive one.
7. Hash with bcrypt at cost factor 10 (matching the seed) BEFORE opening any transaction — it is CPU-bound and would hold a connection.
8. `npx tsc --noEmit`.
Required interfaces/content structure:
- Five async actions returning a success object or `{ error: string }`.
- `createUser` and `resetUserPassword` return `{ tempPassword: string }` — the only place that value appears.
- `MIN_PASSWORD_LENGTH` exported from `src/lib/validations/user.ts` for 07-04 and 07-06 to import.
Edge/error cases:
- Target id absent: `{ error: "User not found" }`, not a raw Prisma error.
- Email collision including case-only: P2002 → a clear message.
- Self-target on deactivate, demote, or password reset: refuse each with a distinct honest message.
- Last active admin: refuse with a message saying why.
- Peer admins: any admin may reset, demote or deactivate any *other* admin. This is accepted (admins are mutually trusted; the audit log is deferred) — but emit a structured non-secret log line for these actions so it is at least attributable.
- Serializable isolation raises P2034 on conflict — retry a bounded number of times rather than surfacing it.
Verification criteria:
- `npx tsc --noEmit` exits 0
- Every action's first statement gates on `ADMIN_MANAGE_ROLES`
- The invariant uses a named serialization mechanism, not a bare `$transaction`
- `resetUserPassword` refuses a self-target
- The temp password appears only in return values
- No User row is ever deleted
- No files outside `files_modified` were touched
Final result format:
- Status / files changed / the guard-rail code quoted showing the serialization mechanism / `MIN_PASSWORD_LENGTH` value and justification / temp-password generation choices / verification outputs / issues
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- 07-01 has not landed.
- None of the three named serialization mechanisms is available in this Prisma version. Report it rather than shipping a read-then-write race — a bare `$transaction` is NOT an acceptable fallback.
- `src/lib/actions/companies.ts`'s convention differs materially from the described shape.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, `07-CONTEXT.md`, `07-01-SUMMARY.md`.
2. `git status --short` and `git diff` for every changed file.
3. Confirm `git diff --name-only` lists neither `src/lib/session.ts`, `src/auth.ts`, nor anything under `src/app/(auth)/` — 07-02 and 07-04 own those in this wave.
4. Compare changes against `files_modified`/`files_forbidden`; any unapproved change means `BLOCKED`.
5. Re-run `npx tsc --noEmit`.
6. Continue from the next unverified task.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: ADMIN_MANAGE_ROLES, validation schemas, and the shared password constant</name>
  <files>src/lib/permissions.ts, src/lib/validations/user.ts</files>
  <action>
Read `src/lib/permissions.ts` in full (note `admin:manage_users` at line 10, the `*_ROLES` block at 79-131). Add `export const ADMIN_MANAGE_ROLES: Role[] = ["admin"];` with a doc comment matching the neighbours. Add no new `Permission` literal. Then write `src/lib/validations/user.ts` exporting `MIN_PASSWORD_LENGTH` (pick a value and justify it in a comment; 12 with no composition rules is the recommended default) plus `createUserSchema` and `updateUserRoleSchema` with inferred Input types. Zod is v4 — use `z.email()` and order transforms so normalization happens before validation, e.g. `z.string().trim().toLowerCase().pipe(z.email())`. `validations/company.ts` is a single-field schema and does not demonstrate this ordering; do not copy it blindly. This constant is the phase's single source of password policy — 07-04 and 07-06 import it rather than reinventing a minimum.

> verification: grep -q 'ADMIN_MANAGE_ROLES' src/lib/permissions.ts
> verification: grep -q 'MIN_PASSWORD_LENGTH' src/lib/validations/user.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q 'ADMIN_MANAGE_ROLES' src/lib/permissions.ts
grep -q 'MIN_PASSWORD_LENGTH' src/lib/validations/user.ts
grep -q 'createUserSchema' src/lib/validations/user.ts
npx tsc --noEmit
  </verify>
  <done>ADMIN_MANAGE_ROLES sits with the other role-group constants, and one exported MIN_PASSWORD_LENGTH plus Zod-4-correct schemas give the whole phase a single password policy.</done>
</task>

<task type="auto">
  <name>Task 2: createUser and resetUserPassword with safe temp-password handling</name>
  <files>src/lib/actions/users.ts</files>
  <action>
Read `src/lib/actions/companies.ts` for the action shape and `prisma/seed.ts` for the bcrypt cost factor (10). Create `src/lib/actions/users.ts` with `"use server"`. `createUser(formData)`: gate on `requireRole(ADMIN_MANAGE_ROLES)`, parse with `createUserSchema`, lowercase-normalize the email to match `authorize()`'s lookup at `src/auth.ts:51`, generate a temp password with `crypto.randomBytes` — never `Math.random` — hash with bcrypt at cost 10, create with `mustChangePassword: true` and `isActive: true`, return `{ tempPassword }`. Map P2002 to a duplicate-email message. `resetUserPassword(id)`: same, but **refuse when `id` is the caller's own id** — a sole admin who resets their own password and loses the once-shown value has no recovery path, since `bootstrap:admin` refuses existing emails and self-serve reset is deferred; they already have `/change-password`. Hash outside any transaction. The password must appear only in the return value; structured non-secret logging (actor, action, target) is permitted and encouraged.

> verification: grep -q 'toLowerCase' src/lib/actions/users.ts
> verification: grep -q 'randomBytes' src/lib/actions/users.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q 'toLowerCase' src/lib/actions/users.ts
grep -q 'randomBytes' src/lib/actions/users.ts
grep -q 'resetUserPassword' src/lib/actions/users.ts
npx tsc --noEmit
  </verify>
  <done>createUser and resetUserPassword generate a cryptographically random temp password, persist only its bcrypt hash, force a first-login change, lowercase the email, and resetUserPassword refuses a self-target so no admin can strand themselves.</done>
</task>

<task type="auto">
  <name>Task 3: Role and activation actions with a real serialization mechanism</name>
  <files>src/lib/actions/users.ts</files>
  <action>
Add `updateUserRole(id, formData)`, `deactivateUser(id)` and `reactivateUser(id)`, each gated on `requireRole(ADMIN_MANAGE_ROLES)`. `updateUserRole` and `deactivateUser` refuse a self-target (compare against the id `requireRole` resolved, never a client-supplied one) and refuse to leave zero active admins. Implement that invariant with a NAMED serialization mechanism — `$transaction` with `isolationLevel: "Serializable"` plus bounded P2034 retry, a `pg_advisory_xact_lock` on a fixed key, or a conditional `updateMany` whose where-clause cannot match when the invariant would break. A bare `db.$transaction` around a COUNT is READ COMMITTED and does NOT serialize: two admins deactivating each other concurrently each count one remaining admin and both commit, leaving zero. State the chosen mechanism in the summary. Count only ACTIVE admins excluding the target, or demoting an already-inactive admin is wrongly refused. `reactivateUser` needs no guard rail. Never delete a User row. End each with `revalidatePath("/admin/users")`.

> verification: grep -q 'Serializable\|advisory\|updateMany' src/lib/actions/users.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q 'updateUserRole' src/lib/actions/users.ts
grep -q 'deactivateUser' src/lib/actions/users.ts
grep -q 'reactivateUser' src/lib/actions/users.ts
grep -q 'Serializable\|advisory\|updateMany' src/lib/actions/users.ts
test "$(grep -c 'user.delete' src/lib/actions/users.ts)" = "0"
npx tsc --noEmit
  </verify>
  <done>Role changes and deactivation refuse self-targeting and cannot leave zero active admins even under concurrent writes, using a named serialization mechanism rather than a bare transaction, and no action deletes a User row.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0
- [ ] Every action gates on `requireRole(ADMIN_MANAGE_ROLES)` first; no inline role literals
- [ ] No new `Permission` literal was added
- [ ] `createUser` lowercase-normalizes the email
- [ ] The temp password appears only in return values; no password material is logged
- [ ] `resetUserPassword`, `updateUserRole` and `deactivateUser` all refuse a self-target
- [ ] The last-active-admin invariant uses a named serialization mechanism, documented in the summary
- [ ] `MIN_PASSWORD_LENGTH` is exported for 07-04 and 07-06
- [ ] No action deletes a User row
- [ ] `src/lib/session.ts`, `src/auth.ts` and `src/app/(auth)/**` are untouched
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- New-user creation lowercase-normalizes email to match `authorize()`, and shows the generated temp password exactly once without logging it
- Guard rails hold: an admin cannot deactivate or demote themselves, and at least one active admin always remains — under concurrency, not just sequentially
</success_criteria>

<output>
Write `07-03-SUMMARY.md` recording: each action and its guard rails; the serialization mechanism chosen for the last-active-admin invariant, quoted, with why the bare-transaction approach was rejected; the `MIN_PASSWORD_LENGTH` value and justification; temp-password length, alphabet and randomness source; and the peer-admin behaviour accepted (any admin may act on any other) with whatever structured logging was added.
</output>
