---
phase: 07-account-management-session-freshness
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - src/app/(dashboard)/admin/users/page.tsx
  - src/components/admin/user-create-form.tsx
  - src/components/admin/user-row-actions.tsx
  - src/components/nav/app-sidebar.tsx
  - src/components/ui/alert-dialog.tsx
files_forbidden:
  - src/lib/actions/**
  - src/lib/permissions.ts
  - src/lib/session.ts
  - src/auth.ts
  - src/middleware.ts
  - prisma/**
  - types/next-auth.d.ts
  - src/app/(auth)/**
  - scripts/**
  - e2e/**
  - src/components/ui/** (EXCEPT the new alert-dialog.tsx named in files_modified)
sequential_files: []
expected_artifacts:
  - path: "src/app/(dashboard)/admin/users/page.tsx"
    provides: "admin-gated user list with create form and per-row lifecycle actions"
    required: true
  - path: "src/components/nav/app-sidebar.tsx"
    provides: "a real Admin section containing both QuickBooks and Users links"
    required: true
autonomous: false
agents: ["engineering-frontend-developer"]
requirements: ["/admin/users exists, gated on admin-only authorization and linked from the sidebar's Admin section, supporting create / edit-role / reset-password / deactivate / reactivate"]
user_setup:
  - "npm install && npx prisma generate in this worktree"
  - "Wave 2 merged. It has NOT been integration-verified -- 07-07 owns that gate and runs in Wave 4, after this plan. Run `npx prisma generate && npx tsc --noEmit` on the merged tree yourself before starting."
verification_commands:
  - "test -f 'src/app/(dashboard)/admin/users/page.tsx'"
  - "grep -q 'requireRole' 'src/app/(dashboard)/admin/users/page.tsx'"
  - "grep -q '/admin/users' src/components/nav/app-sidebar.tsx"
  - "grep -q '/admin/quickbooks' src/components/nav/app-sidebar.tsx"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "The page is authorization-gated server-side. A non-admin who types /admin/users directly is refused; hiding the sidebar link is not authorization."
    - "SIDEBAR REALITY -- there is currently NO Admin section and no link labelled QuickBooks. src/components/nav/app-sidebar.tsx:74-98 is a single <li>, gated on can(role,'admin:manage_users'), containing a ternary whose true-branch renders ONE link labelled 'Admin' pointing at /admin/quickbooks, and whose else-branch is dead code (the condition can(qbo:manage) || can(admin:manage_users) is always true inside a block already gated on the latter). This plan must RESTRUCTURE that <li> into a real section with two labelled links -- it is not a one-line addition."
    - "GATING REALITY -- src/app/(dashboard)/admin/quickbooks/page.tsx does NOT use requireRole. It uses getCurrentUser() + can(user.role,'qbo:manage') + manual redirects (lines 50-58). That page predates the requireRole convention and is the model for LAYOUT only, not for gating. This plan uses requireRole(ADMIN_MANAGE_ROLES), the codebase's stated authoritative boundary."
    - "The temp password from createUser and resetUserPassword is displayed exactly once with an explicit not-shown-again warning, and never written to localStorage, sessionStorage, a URL parameter, or any state surviving navigation."
    - "Every mutation goes through 07-03's Server Actions. This plan adds no database writes and no authorization logic of its own; reads in the Server Component are expected and fine."
    - "Deactivated users stay visible with a clear inactive indicator and a Reactivate control. An offboarded user you cannot see is one you cannot re-enable."
    - "Confirmation for destructive controls uses a NEW src/components/ui/alert-dialog.tsx following the existing shadcn wrapper pattern. All 8 radix imports in this repo live under src/components/ui/ as styled wrappers (see dropdown-menu.tsx), components.json exists with ui: @/components/ui, and raw radix is headless -- importing AlertDialog directly into src/components/admin/ would ship an unstyled overlay that drifts from every other primitive."
  artifacts:
    - path: "src/app/(dashboard)/admin/users/page.tsx"
      provides: "the /admin/users route"
      contains: "requireRole"
  key_links:
    - from: "src/app/(dashboard)/admin/users/page.tsx"
      to: "src/lib/actions/users.ts"
      via: "the page and its children invoke 07-03's Server Actions; they never write via Prisma directly"
      pattern: "createUser"
---

<objective>
Put an admin-facing surface on the user lifecycle built in 07-03, and turn the sidebar's single Admin link into a real section that can hold more than one destination.

Purpose: `admin:manage_users` has existed as a `Permission` since Phase 1 and has gated a sidebar entry ever since — but that entry is one link labelled "Admin" pointing at QuickBooks. The authorization plumbing exists; the navigation structure has to be built, not merely extended.
Output: New `src/app/(dashboard)/admin/users/page.tsx`, new components under `src/components/admin/`, and a restructured Admin block in `src/components/nav/app-sidebar.tsx`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-account-management-session-freshness/07-CONTEXT.md
@.planning/phases/07-account-management-session-freshness/07-02-SUMMARY.md
@.planning/phases/07-account-management-session-freshness/07-03-SUMMARY.md

Relevant source files:
@src/app/(dashboard)/admin/quickbooks/page.tsx
@src/components/nav/app-sidebar.tsx
@src/lib/actions/users.ts
@src/lib/permissions.ts
@src/components/crm/company-form.tsx
@src/components/invoices/push-to-qbo-button.tsx

Map chunks: rt-006 (the existing /admin route — layout model only), cmp-001 (sidebar), cmp-009 (shadcn primitives — note no dialog), cmp-006 (client-form convention)
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report
Role: engineering-frontend-developer
Task: Build `/admin/users` and its components, and restructure the sidebar Admin block into a two-link section.
Scope:
- Read targets: `src/app/(dashboard)/admin/quickbooks/page.tsx` (full — LAYOUT model only; note its gating at 50-58 is the older `getCurrentUser()`+`can()` pattern, not what this plan uses), `src/components/nav/app-sidebar.tsx` (full — the `<li>` at 74-98 and its dead else-branch), `src/lib/actions/users.ts` (READ ONLY — the real signatures 07-03 produced), `src/lib/permissions.ts` (READ ONLY — `ADMIN_MANAGE_ROLES`), `src/components/crm/company-form.tsx` (client-form convention), `src/components/invoices/push-to-qbo-button.tsx` (action-button convention), `src/components/ui/` (READ ONLY — inventory; there is no dialog)
- Write targets: `src/app/(dashboard)/admin/users/page.tsx` (new), `src/components/admin/user-create-form.tsx` (new), `src/components/admin/user-row-actions.tsx` (new), `src/components/ui/alert-dialog.tsx` (new — the ONLY permitted addition under `ui/`), `src/components/nav/app-sidebar.tsx`
- Forbidden targets: `src/lib/actions/**` (07-03 — if an action is wrong, report it, do not edit it), `src/lib/permissions.ts`, `src/lib/session.ts`, `src/auth.ts`, all of `prisma/**`, `src/app/(auth)/**`, `scripts/**`, `e2e/**`, and every file under `src/components/ui/` EXCEPT the new `alert-dialog.tsx`
Allowed tools/actions:
- Read, Write, Edit on the write targets
- Bash: `npx tsc --noEmit`, `npm run lint`
Forbidden actions:
- Do not perform Prisma WRITES from the page or any component. Reads in the Server Component are expected and correct; every mutation goes through a 07-03 Server Action.
- Do not invent a new authorization check. Gate with `requireRole(ADMIN_MANAGE_ROLES)`. Do NOT copy the QuickBooks page's `getCurrentUser()`+`can()` gating — that page predates the convention.
- Do not persist the temp password anywhere.
- Do not add a new npm dependency. You MAY add exactly one new file, `src/components/ui/alert-dialog.tsx`, wrapping `AlertDialog` from the already-installed `radix-ui` and following `src/components/ui/dropdown-menu.tsx`'s wrapper pattern (`data-slot` attributes, `cn()`, matching class names). Do not edit any other file under `src/components/ui/`.
- Do not use `window.confirm()` — it is inconsistent with the rest of the UI and awkward to drive from Playwright in 07-07.
- Do not filter deactivated users out of the list.
- Do not add a delete-user control.
- Do not self-defer planned work.
Implementation sequence:
1. Read `src/lib/actions/users.ts` and record the five actions' real signatures and return shapes. Build against what 07-03 produced, not this plan's description.
2. Read `src/app/(dashboard)/admin/quickbooks/page.tsx` for layout, and note explicitly that you are NOT copying its gating.
3. Write `src/app/(dashboard)/admin/users/page.tsx` as an async Server Component gated with `requireRole(ADMIN_MANAGE_ROLES)`, reading id/name/email/role/isActive/createdAt and rendering a table with a clear indicator for inactive accounts. No pagination — the project is sized for under 25 users.
4. Write `src/components/admin/user-create-form.tsx` (client): name, email, role; calls `createUser`; on success renders the returned temp password once with a copy control and a "this will not be shown again" warning; surfaces `{ error }` inline.
5. Add `src/components/ui/alert-dialog.tsx` as a styled shadcn-pattern wrapper over radix `AlertDialog`, matching `dropdown-menu.tsx`. Then write `src/components/admin/user-row-actions.tsx` (client): edit-role, reset-password, deactivate, reactivate. Reset-password gives the same one-time treatment. Deactivate and reset-password each require confirmation via radix-ui `AlertDialog`. Surface `{ error }` returns — including 07-03's guard-rail refusals — as visible messages.
6. Restructure `src/components/nav/app-sidebar.tsx`'s Admin `<li>`: replace the ternary with an "Admin" heading over a nested list containing `/admin/quickbooks` labelled "QuickBooks", gated on `can(role, "qbo:manage")`, and `/admin/users` labelled "Users", gated on `can(role, "admin:manage_users")` — today both resolve to admin only, but the two routes genuinely check different permissions (`quickbooks/page.tsx:56` uses `qbo:manage`), so collapsing them onto one guard bakes in a latent mismatch. Keep the section heading on the outer `admin:manage_users` guard. Delete the dead else-branch (its condition can never be false inside a block already gated on `admin:manage_users`) or state explicitly why it stays. Keep the outer `can(role, "admin:manage_users")` guard. Match the existing link class names.
7. `npx tsc --noEmit` and `npm run lint`.
Required interfaces/content structure:
- Route `/admin/users`, server-gated on `ADMIN_MANAGE_ROLES`.
- Page is a Server Component; the form and row actions are client components.
Edge/error cases:
- 07-03's guard-rail refusals (self-deactivation, self-demotion, self-password-reset, last active admin) return `{ error }` — display them. A control that silently does nothing is worse than one that explains itself.
- The viewing admin's own row: their deactivate/role/reset controls will be refused server-side. Disabling them client-side is good UX but the server refusal remains the guarantee.
- The temp password block must survive a re-render of its own component but not a navigation. Do not make it recoverable.
- If `src/lib/actions/users.ts` does not export what this plan expects, emit BLOCKED naming the mismatch.
Verification criteria:
- `npx tsc --noEmit` exits 0 and `npm run lint` passes
- `/admin/users` is refused server-side for a non-admin
- All five lifecycle operations are reachable
- The temp password is shown once and not persisted
- The sidebar has one Admin section with two labelled links, and the dead else-branch is resolved
- No files outside `files_modified` were touched
Final result format:
- Status / files changed / the action signatures consumed / the sidebar block before and after, quoted / verification outputs / issues
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- 07-03 has not landed or `src/lib/actions/users.ts` lacks the five actions. Do not stub them; that file is forbidden here.
- 07-02 has not landed — `requireRole()` would gate on a stale JWT role, making this entire surface reachable by a demoted ex-admin for up to eight hours.
- `radix-ui` does not export `AlertDialog` in the installed version (it did at v1.6.7 when this plan was written). Report it; do not add a dependency.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, `07-CONTEXT.md`, `07-02-SUMMARY.md`, `07-03-SUMMARY.md`.
2. `git status --short` and `git diff` for every changed file.
3. Confirm 07-03 landed: `grep -c 'export async function' src/lib/actions/users.ts` should report five.
4. Confirm 07-02 landed: `grep -q 'findUnique' src/lib/session.ts`.
5. Compare changes against `files_modified`/`files_forbidden`; any unapproved change means `BLOCKED`.
6. Re-run `npx tsc --noEmit`.
7. Continue from the next unverified task.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: The admin-gated user list page</name>
  <files>src/app/(dashboard)/admin/users/page.tsx</files>
  <action>
Read `src/lib/actions/users.ts` and record the five actions' real signatures. Read `src/app/(dashboard)/admin/quickbooks/page.tsx` for LAYOUT — but note its gating (`getCurrentUser()` + `can()` + manual redirects, lines 50-58) predates the `requireRole` convention and is deliberately NOT what you copy. Write `src/app/(dashboard)/admin/users/page.tsx` as an async Server Component gated with `requireRole(ADMIN_MANAGE_ROLES)`, reading id/name/email/role/isActive/createdAt and rendering a table from the existing `src/components/ui/table` primitives with a clear indicator for inactive accounts. Reads via Prisma here are expected; writes are not. Do not filter deactivated users out — an offboarded user you cannot see cannot be reactivated. No pagination.

> verification: grep -q 'requireRole' 'src/app/(dashboard)/admin/users/page.tsx'
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f 'src/app/(dashboard)/admin/users/page.tsx'
grep -q 'requireRole' 'src/app/(dashboard)/admin/users/page.tsx'
grep -q 'isActive' 'src/app/(dashboard)/admin/users/page.tsx'
npx tsc --noEmit
  </verify>
  <done>/admin/users lists every user including deactivated ones and is gated server-side with requireRole(ADMIN_MANAGE_ROLES).</done>
</task>

<task type="auto">
  <name>Task 2: Create-user form and row actions with AlertDialog confirmation</name>
  <files>src/components/admin/user-create-form.tsx, src/components/admin/user-row-actions.tsx</files>
  <action>
Read `src/components/crm/company-form.tsx` for the client-form convention. Write `user-create-form.tsx` (name, email, role → `createUser`), rendering the returned temp password once with a copy control and an explicit not-shown-again warning, never writing it to localStorage/sessionStorage/URL/navigable state, and surfacing `{ error }` inline. Write `user-row-actions.tsx` with edit-role, reset-password, deactivate and reactivate wired to 07-03's actions; reset-password gets the same one-time password treatment. Deactivate and reset-password each require confirmation. First add `src/components/ui/alert-dialog.tsx` as a styled wrapper over radix `AlertDialog`, following `dropdown-menu.tsx`'s pattern — every one of the repo's 8 radix imports lives under `ui/`, and raw radix is headless so importing it straight into `admin/` would render unstyled; `window.confirm()` is forbidden as it is inconsistent with the UI and awkward for 07-07's Playwright specs. Display every `{ error }` return, including 07-03's guard-rail refusals, as a visible message.

> verification: grep -q 'AlertDialog' src/components/admin/user-row-actions.tsx
test -f src/components/ui/alert-dialog.tsx
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q 'createUser' src/components/admin/user-create-form.tsx
grep -q 'AlertDialog' src/components/admin/user-row-actions.tsx
test "$(grep -rc 'window.confirm' src/components/admin/ | grep -v ':0' | wc -l)" = "0"
test "$(grep -rc 'localStorage\|sessionStorage' src/components/admin/ | grep -v ':0' | wc -l)" = "0"
npx tsc --noEmit
  </verify>
  <done>An admin can create a user and run all four lifecycle operations, temp passwords appear exactly once with no persistence, destructive controls confirm via AlertDialog, and guard-rail refusals are visible.</done>
</task>

<task type="auto">
  <name>Task 3: Restructure the sidebar Admin block into a real section</name>
  <files>src/components/nav/app-sidebar.tsx</files>
  <action>
Read `src/components/nav/app-sidebar.tsx:74-98` carefully — this is NOT a one-line addition. Today it is a single `<li>` gated on `can(role,"admin:manage_users")` containing a ternary: the true-branch renders one link labelled "Admin" pointing at `/admin/quickbooks`, and the else-branch renders a dead "(Coming soon)" span whose condition can never be false inside a block already gated on `admin:manage_users`. Replace this with an "Admin" heading over a nested list containing two labelled links: "QuickBooks" → `/admin/quickbooks` gated on `can(role,"qbo:manage")`, and "Users" → `/admin/users` gated on `can(role,"admin:manage_users")` — the two routes check different permissions, so do not collapse them onto one guard. Keep the outer permission guard. Delete the dead else-branch, or state explicitly in the summary why it remains. Match the existing link class names exactly so the nav stays visually consistent.

> verification: grep -q '/admin/users' src/components/nav/app-sidebar.tsx
> verification: grep -q '/admin/quickbooks' src/components/nav/app-sidebar.tsx
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q '/admin/users' src/components/nav/app-sidebar.tsx
grep -q '/admin/quickbooks' src/components/nav/app-sidebar.tsx
grep -q 'admin:manage_users' src/components/nav/app-sidebar.tsx
npx tsc --noEmit
npm run lint
  </verify>
  <done>The sidebar has one Admin section holding both a QuickBooks and a Users link, still gated on admin:manage_users, with the dead else-branch resolved.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0 and `npm run lint` passes
- [ ] `/admin/users` is gated with `requireRole(ADMIN_MANAGE_ROLES)` — a direct visit by a non-admin is refused
- [ ] All five lifecycle operations are reachable from the UI
- [ ] The temp password is displayed once with a warning; no localStorage/sessionStorage/URL persistence
- [ ] Confirmation uses a styled `src/components/ui/alert-dialog.tsx` wrapper following the `dropdown-menu.tsx` pattern; no `window.confirm`; no other file under `src/components/ui/` touched
- [ ] Deactivated users remain listed with a reactivate control
- [ ] Guard-rail refusals surface as visible errors
- [ ] The sidebar has one Admin section with two labelled links and the dead branch resolved
- [ ] No Prisma writes and no new authorization logic were added here
- [ ] `src/lib/actions/users.ts` is untouched
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- `/admin/users` exists, gated on admin-only authorization and linked from the sidebar's Admin section, supporting create / edit-role / reset-password / deactivate / reactivate
- The generated temp password is shown exactly once, without being logged or persisted
</success_criteria>

<output>
Write `07-05-SUMMARY.md` recording: the routes and components created; the exact action signatures consumed from 07-03; how the one-time temp password display works and why it is unrecoverable; the alert-dialog wrapper added and how it follows dropdown-menu.tsx; and the sidebar block quoted before and after, including what happened to the dead else-branch.
</output>
