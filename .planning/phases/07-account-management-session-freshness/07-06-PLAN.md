---
phase: 07-account-management-session-freshness
plan: 06
type: execute
wave: 3
depends_on: ["07-01", "07-04"]
files_modified:
  - scripts/create-admin.ts
  - package.json
  - DEPLOYMENT.md
files_forbidden:
  - src/lib/session.ts
  - src/auth.ts
  - src/auth.config.ts
  - src/middleware.ts
  - src/lib/actions/**
  - src/lib/permissions.ts
  - prisma/schema.prisma
  - prisma/migrations/**
  - prisma/seed.ts
  - types/next-auth.d.ts
  - src/app/**
  - src/components/**
  - e2e/**
sequential_files:
  - package.json
expected_artifacts:
  - path: "scripts/create-admin.ts"
    provides: "bootstrap path to the first real admin, with an explicit break-glass reset"
    required: true
  - path: "DEPLOYMENT.md"
    provides: "onboarding runbook with migration ordering, TLS precondition, and recovery procedure"
    required: true
autonomous: false
agents: ["engineering-backend-architect", "infrastructure-devops-engineer"]
requirements: ["npm run bootstrap:admin creates the first real admin, retiring ALLOW_SEED_IN_PRODUCTION as the documented path"]
user_setup:
  - "npm install && npx prisma generate in this worktree"
  - "A reachable DATABASE_URL with 07-01's migration applied, to exercise the script"
verification_commands:
  - "test -f scripts/create-admin.ts"
  - "grep -q 'bootstrap:admin' package.json"
  - "grep -q 'role: \"admin\"' scripts/create-admin.ts"
  - "grep -q 'bootstrap:admin' DEPLOYMENT.md"
  - "npx tsc --noEmit"

must_haves:
  truths:
    - "The script explicitly sets role: 'admin'. Prisma's User.role defaults to technician (prisma/schema.prisma:26), so omitting it produces a technician account with no Admin nav, on a deployment where the previously-documented fallback has just been removed. A post-create read-back must print the created row's role so the operator sees what they got."
    - "The password is NEVER accepted as a command-line argument. `npm run bootstrap:admin -- email pass` writes the production admin password into shell history and exposes it in ps output. Email may come from argv; the password must come from an interactive prompt with echo suppressed."
    - "The email is lowercase-normalized exactly as authorize() does (src/auth.ts:51), or the account is unreachable."
    - "The script refuses to silently overwrite an existing account, BUT provides an explicit, loud, non-default break-glass flag to reset an existing admin's password. Without it there is no recovery path for a locked-out sole admin: resetUserPassword needs an admin session, self-serve reset is deferred, and the seed is being retired -- leaving manual SQL as the only option, which is exactly what this phase exists to eliminate."
    - "DEPLOYMENT.md states the migration ordering constraint: db:migrate:deploy must complete and both columns must exist BEFORE the app restarts. Otherwise Prisma raises P2022 on every request and loginAction reports 'Invalid email or password' to every user including every admin."
    - "DEPLOYMENT.md states that onboarding must not be performed until the deployment is behind TLS. Phase 8 delivers Caddy; until then docker-compose.yml publishes 3000 directly and every temp password and session cookie crosses plaintext HTTP."
    - "DEPLOYMENT.md notes that a deactivated user sees 'Invalid email or password' by design (the anti-enumeration property), so deactivation must be communicated out-of-band."
  artifacts:
    - path: "scripts/create-admin.ts"
      provides: "first-admin bootstrap"
      contains: "role: \"admin\""
  key_links:
    - from: "scripts/create-admin.ts"
      to: "prisma/seed.ts"
      via: "follows the standalone-script Prisma setup (own PrismaClient with PrismaPg) rather than importing the Next-coupled src/lib/db.ts"
      pattern: "PrismaPg"
---

<objective>
Give a fresh deployment a defensible way to create its first admin, and rewrite the runbook so the operational hazards this phase introduces are written down where an operator will read them.

Purpose: the only way to get a real admin today is a seed run with `ALLOW_SEED_IN_PRODUCTION=true` or a direct database insert. `DEPLOYMENT.md` documents that honestly; this plan replaces it.
Output: New `scripts/create-admin.ts`, a `bootstrap:admin` npm script, and a rewritten onboarding section in `DEPLOYMENT.md`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-account-management-session-freshness/07-CONTEXT.md
@.planning/phases/07-account-management-session-freshness/07-01-SUMMARY.md
@.planning/phases/07-account-management-session-freshness/07-04-SUMMARY.md

Relevant source files:
@prisma/seed.ts
@prisma/schema.prisma
@package.json
@DEPLOYMENT.md
@src/auth.ts
@docker-compose.yml

Map chunks: db-005 (seed and its standalone Prisma setup), inf-002 (npm scripts), inf-001 (compose topology — no proxy, port published)
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report
Role: engineering-backend-architect (the script) + infrastructure-devops-engineer (the runbook)
Task: Write the first-admin bootstrap script, wire its npm entry, and rewrite DEPLOYMENT.md's account-creation and onboarding guidance.
Scope:
- Read targets: `prisma/seed.ts` (READ ONLY — the standalone-script Prisma setup with `PrismaPg`, and bcrypt cost 10), `prisma/schema.prisma` (READ ONLY — confirm `role` defaults to `technician` at line 26), `package.json` (the `scripts` block; how `email-poller` invokes `tsx`), `DEPLOYMENT.md` (full — find the section documenting `ALLOW_SEED_IN_PRODUCTION` or a direct insert), `src/auth.ts` (READ ONLY — `email.toLowerCase()` at 51), `docker-compose.yml` (READ ONLY — confirm the app publishes 3000 with no proxy, for the TLS warning)
- Write targets: `scripts/create-admin.ts` (new), `package.json` (one script entry), `DEPLOYMENT.md`
- Forbidden targets: all of `src/**`, all of `prisma/**` (including `seed.ts` — its guard rail stays), `types/next-auth.d.ts`, `e2e/**`
Allowed tools/actions:
- Read, Write, Edit on the write targets
- Bash: `npx tsc --noEmit`, `npm run lint`
Forbidden actions:
- Do not accept the password as a command-line argument or an environment variable in the normal path. Interactive prompt with echo suppressed, via `node:readline/promises` — no new dependency.
- Do not omit `role: "admin"`. The schema default is `technician`.
- Do not import `src/lib/db.ts` if it is Next-runtime-coupled — follow `prisma/seed.ts` and construct a `PrismaClient` with `PrismaPg`. Check before importing.
- Do not remove the `ALLOW_SEED_IN_PRODUCTION` guard rail from `prisma/seed.ts` — that file is forbidden here and the guard rail remains correct. This plan retires it as the *documented path*, nothing more.
- Do not make the break-glass reset the default behaviour or a silent one. It must require an explicit flag and an interactive confirmation.
- Do not self-defer planned work.
Implementation sequence:
1. Read `prisma/seed.ts` for the standalone Prisma setup and cost factor; read `prisma/schema.prisma:26` and see the `technician` default for yourself.
2. Write `scripts/create-admin.ts`: accept the email from argv or a prompt; take the password ONLY from an interactive prompt with echo suppressed; lowercase-normalize the email; refuse an existing email with a clear message and a non-zero exit; create with `role: "admin"`, `isActive: true`, `mustChangePassword: false` (the operator chose this password themselves); read the row back and print `id`, `email` and `role` so the operator can see it is an admin. Never echo the password.
3. Add an explicit `--reset-password` break-glass path: with the flag, and only after an interactive confirmation naming the account, set a new hash on an existing admin. Document it as the locked-out-sole-admin recovery.
4. Catch a missing-column Prisma failure (P2022) and print guidance pointing at `npm run db:migrate:deploy` rather than surfacing a raw error.
5. Add `"bootstrap:admin": "tsx scripts/create-admin.ts"` to `package.json`, matching the `email-poller` invocation.
6. Rewrite `DEPLOYMENT.md`'s account-creation section to cover: (a) the migration-before-restart ordering constraint and why — P2022 makes `loginAction` report "Invalid email or password" to everyone including admins; (b) `npm run bootstrap:admin` as the path to a first account; (c) the full onboarding flow — bootstrap, log in, create the team from `/admin/users`, each forced through `/change-password`; (d) an explicit precondition that onboarding must NOT be performed until the deployment is behind TLS, since Phase 8 delivers Caddy and today the app publishes 3000 in the clear; (e) the `--reset-password` recovery procedure; (f) a note that a deactivated user sees "Invalid email or password" by design, so deactivation must be communicated out-of-band. Leave an explicit `<!-- Phase 8: revisit for Caddy/TLS topology -->` marker in the onboarding section.
7. `npx tsc --noEmit` and `npm run lint`.
Required interfaces/content structure:
- `npm run bootstrap:admin` runs the script; `npm run bootstrap:admin -- --reset-password` is the break-glass path.
Edge/error cases:
- Unmigrated database → P2022 → actionable message, not a raw Prisma error.
- Existing email without the flag → clear refusal, non-zero exit, no write.
- Non-TTY environment (CI) → fail with a clear message rather than silently reading an empty password.
- `package.json` is in `sequential_files`; no other plan in this wave modifies it, but take the write lock if that changes.
Verification criteria:
- `npx tsc --noEmit` exits 0 and `npm run lint` passes
- The script sets `role: "admin"` and reads the row back
- The password is never taken from argv
- `DEPLOYMENT.md` covers migration ordering, the TLS precondition, onboarding, and recovery
- `prisma/seed.ts` is untouched
- No files outside `files_modified` were touched
Final result format:
- Status / files changed / the script's argument interface / the DEPLOYMENT.md sections rewritten / verification outputs / issues
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `src/lib/db.ts` cannot be safely imported from a standalone `tsx` script AND `prisma/seed.ts`'s adapter setup does not transfer. Report the failure rather than shipping a script that only works under `next dev`.
- 07-01 has not landed, so `isActive`/`mustChangePassword` are not columns.
- Echo suppression cannot be achieved with `node:readline/promises` in this Node version. Report it; do not fall back to argv, and do not add a dependency.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, `07-CONTEXT.md`, `07-01-SUMMARY.md`.
2. `git status --short` and `git diff` for every changed file.
3. `grep bootstrap:admin package.json` — do not add a second entry.
4. Compare changes against `files_modified`/`files_forbidden`; any unapproved change means `BLOCKED`.
5. Re-run `npx tsc --noEmit`.
6. Continue from the next unverified task.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap script that actually produces an admin</name>
  <files>scripts/create-admin.ts</files>
  <action>
Read `prisma/seed.ts` for the standalone Prisma setup (`PrismaPg` + own `PrismaClient`) and bcrypt cost 10; check whether `src/lib/db.ts` is safely importable from a plain `tsx` script before reaching for it. Read `prisma/schema.prisma:26` and confirm `role` defaults to `technician` — this is why the script must set `role: "admin"` explicitly, or the documented-and-only bootstrap path silently produces a technician with no Admin nav. Write `scripts/create-admin.ts`: email from argv or prompt; password ONLY from an interactive prompt with echo suppressed via `node:readline/promises` (never argv — that writes the production password into shell history and `ps`); lowercase-normalize the email to match `src/auth.ts:51`; refuse an existing email with a non-zero exit; create with `role: "admin"`, `isActive: true`, `mustChangePassword: false`; then read the row back and print id, email and role. Catch P2022 and point at `npm run db:migrate:deploy`. Fail clearly in a non-TTY environment.

> verification: grep -q 'role: "admin"' scripts/create-admin.ts
> verification: grep -q 'toLowerCase' scripts/create-admin.ts
> verification: npx tsc --noEmit
  </action>
  <verify>
test -f scripts/create-admin.ts
grep -q 'role: "admin"' scripts/create-admin.ts
grep -q 'toLowerCase' scripts/create-admin.ts
grep -q 'readline' scripts/create-admin.ts
npx tsc --noEmit
  </verify>
  <done>The script creates a genuine admin, reads it back to prove the role, takes the password only from a hidden prompt, and gives an actionable message on an unmigrated database.</done>
</task>

<task type="auto">
  <name>Task 2: Break-glass reset and the npm entry</name>
  <files>scripts/create-admin.ts, package.json</files>
  <action>
Add an explicit `--reset-password` flag to `scripts/create-admin.ts` that, only after an interactive confirmation naming the target account, sets a new hash on an existing admin. This is the recovery path for a locked-out sole admin — without it there is none: `resetUserPassword` needs an admin session, self-serve reset is deferred, and the seed is being retired, leaving manual SQL. It must never be the default and never silent. Then add `"bootstrap:admin": "tsx scripts/create-admin.ts"` to `package.json`'s scripts block, matching how `email-poller` invokes tsx.

> verification: grep -q 'reset-password' scripts/create-admin.ts
> verification: grep -q 'bootstrap:admin' package.json
> verification: npx tsc --noEmit
  </action>
  <verify>
grep -q 'reset-password' scripts/create-admin.ts
grep -q 'bootstrap:admin' package.json
npx tsc --noEmit
  </verify>
  <done>npm run bootstrap:admin creates the first admin, and an explicit confirmed --reset-password flag exists as the documented recovery for a locked-out sole admin.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the deployment runbook</name>
  <files>DEPLOYMENT.md</files>
  <action>
Read `DEPLOYMENT.md` and find the section documenting `ALLOW_SEED_IN_PRODUCTION` (or a direct database insert) as the way to a first account. Rewrite it to cover, in order: the migration-before-restart constraint and why it matters — Prisma raises P2022 on every request if application code lands first, and `loginAction` reports "Invalid email or password" to every user including every admin, with recovery needing shell and psql; `npm run bootstrap:admin` as the path to a first account; the onboarding flow (bootstrap → log in → create the team at `/admin/users` → each forced through `/change-password`); an explicit precondition that onboarding must NOT happen until the deployment is behind TLS, because Phase 8 delivers Caddy and `docker-compose.yml` currently publishes 3000 in the clear so every temp password and session cookie crosses plaintext HTTP; the `--reset-password` recovery procedure; and a note that a deactivated user sees "Invalid email or password" by design, so deactivation must be communicated out-of-band. Leave a `<!-- Phase 8: revisit for Caddy/TLS topology -->` marker in the onboarding section. Do not touch `prisma/seed.ts` — its guard rail stays; only the documentation changes.

> verification: grep -q 'bootstrap:admin' DEPLOYMENT.md
> verification: grep -q 'db:migrate:deploy' DEPLOYMENT.md
  </action>
  <verify>
grep -q 'bootstrap:admin' DEPLOYMENT.md
grep -q '/admin/users' DEPLOYMENT.md
grep -q 'change-password' DEPLOYMENT.md
grep -qi 'tls\|https' DEPLOYMENT.md
grep -q 'Phase 8' DEPLOYMENT.md
  </verify>
  <done>DEPLOYMENT.md documents bootstrap:admin as the account-creation path and records the migration-ordering hazard, the TLS precondition, the recovery procedure, and the deliberate deactivation error message.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` exits 0 and `npm run lint` passes
- [ ] The script sets `role: "admin"` explicitly and reads the row back
- [ ] The password is never accepted from argv or an env var in the normal path
- [ ] `--reset-password` exists, is non-default, and requires interactive confirmation
- [ ] `DEPLOYMENT.md` covers migration ordering, bootstrap, onboarding, the TLS precondition, recovery, and the deactivation message
- [ ] A `<!-- Phase 8: ... -->` marker is present in the onboarding section
- [ ] `prisma/seed.ts` is untouched and its guard rail intact
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- `npm run bootstrap:admin` creates the first real admin, retiring `ALLOW_SEED_IN_PRODUCTION` as the documented path
- The operational hazards this phase introduces are written down where an operator will actually read them
</success_criteria>

<output>
Write `07-06-SUMMARY.md` recording: the script's argument interface and how echo suppression was achieved; the duplicate-email and `--reset-password` behaviours; the read-back output proving `role: "admin"`; and the DEPLOYMENT.md sections rewritten, quoting the migration-ordering and TLS-precondition paragraphs.
</output>
