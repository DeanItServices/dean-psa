---
phase: 07-account-management-session-freshness
plan: 07
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06"]
files_modified:
  - e2e/user-lifecycle.spec.ts
  - e2e/fixtures.ts
  - src/app/(auth)/change-password/actions.ts
files_forbidden:
  - src/lib/session.ts
  - src/auth.ts
  - src/auth.config.ts
  - src/middleware.ts
  - src/lib/actions/**
  - src/lib/permissions.ts
  - prisma/**
  - types/next-auth.d.ts
  - src/app/(dashboard)/**
  - src/components/**
  - scripts/**
  - package.json
  - DEPLOYMENT.md
sequential_files: []
expected_artifacts:
  - path: "e2e/user-lifecycle.spec.ts"
    provides: "executable proof of the phase's load-bearing behavioural claims"
    required: true
autonomous: false
agents: ["testing-qa-verification-specialist", "engineering-frontend-developer"]
requirements: ["Integration verification for the phase: every behavioural claim asserted in the prior six plans is either executable or observed and recorded"]
user_setup:
  - "npm install && npx prisma generate in this worktree"
  - "npx playwright install --with-deps chromium if the browser is not already present"
  - "A running database with all migrations applied, and the seeded fixture accounts present"
verification_commands:
  - "test -f e2e/user-lifecycle.spec.ts"
  - "npx tsc --noEmit"
  - "npm run lint"
  - "npm run test:e2e -- --grep @user-lifecycle"

must_haves:
  truths:
    - "This plan exists because the other six verify with grep and tsc only. The phase's load-bearing claims -- an inactive user resolves to null, deactivation is immediate, role changes are immediate, a mustChangePassword user cannot invoke gated actions, at least one active admin always remains -- were asserted in must_haves and were not executable anywhere."
    - "The wave-close integration gate runs on the MERGED tree. 07-02 changes getCurrentUser()'s return type while 07-03 and 07-04 build new consumers in parallel worktrees; each ran tsc against its own tree only, so a merge-level type or behavioural mismatch is invisible until here."
    - "The spec creates and destroys its OWN throwaway users. playwright.config.ts sets fullyParallel: true against the shared dev database -- touching any of the five seeded fixture accounts would race the login fixture used by all three existing specs."
    - "BLOCKING vs ADVISORY -- only the user-lifecycle spec is a blocking gate. The three pre-existing specs (tickets, sla-tracking, time-entry-to-invoice) have NEVER been executed against a browser, and ROADMAP Phase 9 owns running them for real and fixing what breaks. Making that first-ever full-suite run blocking here would stall Phase 7 on Phase 9's debt, and this plan forbids editing those specs anyway. Run the full suite as EVIDENCE and hand any pre-existing failures to Phase 9."
    - "The password-policy reconciliation is real work, not bookkeeping: 07-04 deliberately defined a local minimum because 07-03's src/lib/validations/user.ts did not exist yet in its wave. Leaving two independent minimums is how the policy drifts."
  artifacts:
    - path: "e2e/user-lifecycle.spec.ts"
      provides: "user lifecycle end-to-end coverage"
      contains: "deactivate"
  key_links:
    - from: "e2e/user-lifecycle.spec.ts"
      to: "e2e/fixtures.ts"
      via: "reuses loginAs for the admin actor but creates its own subject users rather than mutating the shared fixture accounts"
      pattern: "loginAs"
---

<objective>
Prove the phase actually works, and reconcile the one deliberate duplication left by parallel execution.

Purpose: fifteen tasks across six plans verify themselves with `grep -q` and `tsc`. Nothing in the phase logs in, flips a flag, or observes a redirect. The headline claim — that deactivation takes effect on the next request — would otherwise ship with no executable proof and no way to detect a regression.
Output: New `e2e/user-lifecycle.spec.ts`, a reconciled password-policy constant, and a recorded integration run.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-account-management-session-freshness/07-CONTEXT.md
@.planning/phases/07-account-management-session-freshness/07-01-SUMMARY.md
@.planning/phases/07-account-management-session-freshness/07-02-SUMMARY.md
@.planning/phases/07-account-management-session-freshness/07-03-SUMMARY.md
@.planning/phases/07-account-management-session-freshness/07-04-SUMMARY.md
@.planning/phases/07-account-management-session-freshness/07-05-SUMMARY.md
@.planning/phases/07-account-management-session-freshness/07-06-SUMMARY.md

Relevant source files:
@e2e/fixtures.ts
@e2e/tickets.spec.ts
@playwright.config.ts
@src/lib/validations/user.ts
@src/app/(auth)/change-password/actions.ts

Map chunks: tst-001 (fixtures), tst-006 (playwright config, fullyParallel against the dev database), tst-002 (existing spec conventions)
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report
Role: testing-qa-verification-specialist (spec design, evidence) + engineering-frontend-developer (selectors, the reconciliation edit)
Task: Run the wave-close integration gate, write the user-lifecycle E2E spec, reconcile the password constant, and record manual evidence for what cannot be automated.
Scope:
- Read targets: all six prior SUMMARY files (they carry the real signatures, selectors and decisions — build against those, not against this plan's expectations), `e2e/fixtures.ts`, `e2e/tickets.spec.ts` (spec conventions and real selector style), `playwright.config.ts`, `src/lib/validations/user.ts` (07-03's `MIN_PASSWORD_LENGTH`), `src/app/(auth)/change-password/actions.ts` (07-04's local minimum), `src/components/admin/*` (READ ONLY — the real selectors to drive)
- Write targets: `e2e/user-lifecycle.spec.ts` (new), `e2e/fixtures.ts` (add a `loginWith` helper only — do not weaken `loginAs`), `src/app/(auth)/change-password/actions.ts` (the reconciliation import only)
- Forbidden targets: everything else. In particular do not "fix" a prior plan's implementation from here — report it and let it be corrected in its own plan.
Allowed tools/actions:
- Read, Write, Edit on the write targets
- Bash: `npx prisma generate`, `npx tsc --noEmit`, `npm run lint`, `npm run test:e2e`, `npx playwright install --with-deps chromium`
Forbidden actions:
- Do not modify, deactivate, re-role or reset the password of any of the five seeded fixture accounts. `fullyParallel: true` means the other three specs are logging in as those accounts concurrently.
- Do not add `test.fixme` to avoid a failing assertion. If a claim cannot be proven, report it as a finding.
- Do not edit `e2e/tickets.spec.ts`, `e2e/sla-tracking.spec.ts` or `e2e/time-entry-to-invoice.spec.ts`, and do not block Phase 7 on their failures — Phase 9 owns them.
- Do not weaken `loginAs`'s post-login assertion that 07-02 added.
- Do not weaken an assertion to make it pass. A failing spec here is the plan working.
- Do not edit implementation files other than the single import in `change-password/actions.ts`.
- Do not self-defer planned work.
Implementation sequence:
1. **Wave-close integration gate, before anything else.** On the merged tree run, in order: `npx prisma generate`, `npx tsc --noEmit`, `npm run lint`. These three are BLOCKING -- Wave 2's three plans and Wave 3's two each verified only their own tree, so this is the first check of the integrated result. If any fails, report precisely which plan's change is implicated before continuing. Then run the full `npm run test:e2e` as ADVISORY EVIDENCE only: those three specs have never run against a browser, Phase 9 owns their first real run and fixing what breaks, and this plan may not edit them. Record their outcome and hand any pre-existing failure to Phase 9 rather than blocking Phase 7 on it.
2. Reconcile the password policy: import `MIN_PASSWORD_LENGTH` from `src/lib/validations/user.ts` into `src/app/(auth)/change-password/actions.ts`, replacing 07-04's deliberately-local constant. If the two values differ, keep 07-03's and note the change.
3. Read the prior summaries and `src/components/admin/*` for the real selectors. Read `e2e/tickets.spec.ts` for this codebase's spec conventions.
4. Write `e2e/user-lifecycle.spec.ts`, tagged `@user-lifecycle` so it can be run alone. Every subject user is created by the spec with a unique email (timestamp or random suffix); the admin actor uses `loginAs("admin")`.
   - **Cleanup**: no user-deletion path exists anywhere in the app by design (07-03 forbids `db.user.delete`, 07-05 forbids a delete control). Tear down in `afterAll` with a direct `PrismaClient` keyed on the unique email prefix — say so explicitly rather than leaving "cleaned up" undefined, or every run accretes users in the shared dev database forever.
   - **Logging in as a created user**: 07-02 hardened `loginAs` to assert the post-login pathname is `/`, which is exactly what a `mustChangePassword` user will NOT hit. Add a narrow `loginWith(page, email, password, { expectPath })` helper to `e2e/fixtures.ts`; do not weaken `loginAs`.
   Cover:
   - create a user via `/admin/users`, capture the once-shown temp password
   - that user logs in and is redirected to `/change-password`
   - they cannot reach a dashboard route while the flag is set
   - after setting a new password they land on `/` and stay there
   - an admin changes their role; the change is visible to that user on the next request without re-login
   - an admin deactivates them; their next request lands on `/login`
   - they cannot log in again — `authorize()` refuses, with the same message as a wrong password
   - **email normalization**: create a user with a mixed-case email (e.g. `Test.User@Example.com`) and assert they can log in with it — the bug class 07-03 calls "permanently unreachable with no signal to the admin"
   - **reactivate**: after deactivating, reactivate the same user and assert they can log in again
   - **admin-only gating**: `loginAs("technician")`, navigate directly to `/admin/users`, assert `/unauthorized` — 07-05's own must_have says hiding the sidebar link is not authorization
   - **self-target refusals**: the acting admin attempts to deactivate, demote, and password-reset their own row; assert all three show a visible error and do not take effect
5. The **last-active-admin** guard rail and the **concurrency invariant** both go on the manual-evidence track, not the spec. Neither is reliably producible in Playwright here: making the last-admin guard fire requires exactly one active admin to remain, and the actor is the seeded `admin@mspdemo.local` which this plan may not mutate — creating a second admin moves the state further from the trigger, not closer. Exercise both by hand against the running stack and record the observed database state. Say explicitly in the summary that they are manually verified, and why.
6. Record manual evidence in the summary for anything not automatable, with observed outcomes rather than expectations.
7. Re-run the full gate from step 1 and record the final outputs.
Required interfaces/content structure:
- `e2e/user-lifecycle.spec.ts` following the existing specs' structure and real-selector discipline.
Edge/error cases:
- `fullyParallel: true` on a shared dev database: unique emails per run, no reliance on global user counts, no mutation of fixture accounts.
- The last-active-admin assertion needs a second admin to exist; create one in the spec rather than assuming.
- Chromium may not be installed — `npx playwright install --with-deps chromium` is permitted.
- A genuine product bug found here is a finding for the owning plan, not something to patch from this plan.
Verification criteria:
- `npx prisma generate`, `npx tsc --noEmit` and `npm run lint` all pass on the merged tree, with outputs recorded; the full E2E run is recorded as evidence with any pre-existing failure referred to Phase 9
- `e2e/user-lifecycle.spec.ts` covers every bullet in step 4, with no `test.fixme`
- No fixture account was mutated
- The password minimum is imported from one place
- No files outside `files_modified` were touched
Final result format:
- Status / files changed / all four gate outputs verbatim, before and after / the spec's coverage mapped to the phase's success criteria / manual evidence with observed outcomes / any findings referred back to their owning plan / issues
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- Any of 07-01 through 07-06 has not landed on the merged tree.
- The integration gate fails for a reason that requires editing a file this plan may not touch. Report which plan owns the fix.
- The E2E suite cannot run at all (no browser, no database, no dev server). Report the specific blocker — do not write specs that were never executed.
- Note: a pre-existing failure in one of the three legacy specs is NOT a blocker. Record it and refer it to Phase 9.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, `07-CONTEXT.md`, and all six prior SUMMARY files.
2. `git status --short` and `git diff` for every changed file.
3. Confirm all six prior plans landed.
4. Compare changes against `files_modified`/`files_forbidden`; any unapproved change means `BLOCKED`.
5. Re-run the full gate before continuing.
6. Continue from the next unverified task.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Wave-close integration gate and password-policy reconciliation</name>
  <files>src/app/(auth)/change-password/actions.ts</files>
  <action>
Before writing any spec, run the BLOCKING gate on the merged tree and record every output verbatim: `npx prisma generate`, `npx tsc --noEmit`, `npm run lint`. Then run the full `npm run test:e2e` as ADVISORY evidence only — the three pre-existing specs have never run against a browser and Phase 9 owns their first real run and fixing what breaks, so a failure there is recorded and referred onward, never a Phase 7 blocker. This is the first time the integrated result is checked — Wave 2's three plans and Wave 3's two each ran tsc against their own worktree only, so a merge-level mismatch (for example 07-03 or 07-04 consuming `getCurrentUser()`'s pre-07-02 shape) is invisible until now. If anything fails, identify which plan's change is implicated and report before proceeding. Also confirm the runbook and the app agree: `DEPLOYMENT.md` tells operators to create the team at `/admin/users`, which 07-05 built in parallel with 07-06 and which 07-06 does not depend on — assert the route file exists, and report a mismatch as a finding for whichever plan is short. Then reconcile the password policy: import `MIN_PASSWORD_LENGTH` from `src/lib/validations/user.ts` into `src/app/(auth)/change-password/actions.ts`, replacing the local constant 07-04 defined because 07-03's file did not yet exist in its wave. If the values differ, keep 07-03's.

> verification: npx tsc --noEmit
> verification: npm run lint
  </action>
  <verify>
npx tsc --noEmit
npm run lint
grep -q 'MIN_PASSWORD_LENGTH' 'src/app/(auth)/change-password/actions.ts'
test -f 'src/app/(dashboard)/admin/users/page.tsx'
  </verify>
  <done>The merged tree type-checks, lints and passes the existing E2E suite, all outputs are recorded, and the phase has exactly one password-minimum constant.</done>
</task>

<task type="auto">
  <name>Task 2: Write the user-lifecycle E2E spec</name>
  <files>e2e/user-lifecycle.spec.ts</files>
  <action>
Read the six prior SUMMARY files for the real action signatures and selectors, `src/components/admin/*` for what actually renders, and `e2e/tickets.spec.ts` for this codebase's spec conventions and real-selector discipline. Write `e2e/user-lifecycle.spec.ts` covering: create a user via `/admin/users` and capture the once-shown temp password; that user logs in and is redirected to `/change-password`; they cannot reach a dashboard route while flagged; after setting a new password they land on `/`; an admin changes their role and it takes effect on their next request without re-login; an admin deactivates them and their next request lands on `/login`; they cannot log in again and see the same message as a wrong password; deactivating the last active admin returns a visible error. Every subject user is created by the spec with a unique email and cleaned up — never touch the five seeded fixture accounts, because `fullyParallel: true` means the other three specs are logging in as them concurrently. Use `loginAs("admin")` only for the actor. No `test.fixme`, and never weaken an assertion to make it pass — a failure here is this plan doing its job.

> verification: test -f e2e/user-lifecycle.spec.ts
> verification: npm run test:e2e -- --grep @user-lifecycle
  </action>
  <verify>
test -f e2e/user-lifecycle.spec.ts
test "$(grep -c 'test.fixme' e2e/user-lifecycle.spec.ts)" = "0"
grep -q 'deactivate' e2e/user-lifecycle.spec.ts
grep -q 'reactivate' e2e/user-lifecycle.spec.ts
grep -q 'unauthorized' e2e/user-lifecycle.spec.ts
npm run test:e2e -- --grep @user-lifecycle
  </verify>
  <done>The phase's behavioural claims are executable and passing: forced password change, immediate role change, immediate deactivation, refused re-login, and the last-admin guard rail.</done>
</task>

<task type="auto">
  <name>Task 3: Manual evidence for what cannot be automated</name>
  <files>e2e/user-lifecycle.spec.ts</files>
  <action>
The last-active-admin guard rail and the concurrency invariant both belong here rather than in the spec: neither is reliably producible in Playwright, because making the last-admin guard fire needs exactly one active admin to remain and the actor is the seeded `admin@mspdemo.local`, which this plan may not mutate — creating a second admin moves further from the trigger, not closer. Exercise both by hand: with the stack running, issue two concurrent requests and record what the database shows afterwards. Then record manual evidence in the summary for the rest of the flow, with observed outcomes rather than expectations: bootstrap an admin with `npm run bootstrap:admin` on a fresh database and confirm the printed role is `admin`; confirm a deactivated user sees "Invalid email or password"; confirm the temp password is not recoverable after navigating away. Finally re-run the blocking gate (`npx tsc --noEmit`, `npm run lint`, `npm run test:e2e -- --grep @user-lifecycle`) and record the outputs. Any product bug found is a finding for the owning plan — report it, do not patch it from here.

> verification: npm run test:e2e -- --grep @user-lifecycle
  </action>
  <verify>
npm run test:e2e -- --grep @user-lifecycle
npx tsc --noEmit
  </verify>
  <done>Every phase success criterion is backed by either a passing assertion or a recorded manual observation, and the concurrency invariant is either tested or explicitly documented as manually verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `prisma generate`, `tsc --noEmit` and `lint` passed on the merged tree, recorded verbatim
- [ ] The full E2E suite was run as evidence; any pre-existing failures are recorded and referred to Phase 9, not treated as Phase 7 blockers
- [ ] `e2e/user-lifecycle.spec.ts` exists, contains no `test.fixme`, and passes
- [ ] No seeded fixture account was created, mutated, deactivated or re-roled by the spec
- [ ] Subject users are created with unique emails and cleaned up
- [ ] `MIN_PASSWORD_LENGTH` is imported from one place, and `scripts/create-admin.ts` imports it too
- [ ] `DEPLOYMENT.md`'s `/admin/users` reference resolves to a route that actually exists (07-05 and 07-06 ran in parallel and 07-06 does not depend on 07-05)
- [ ] The last-active-admin guard rail and the concurrency invariant are recorded as manual evidence, with the reason they are not automated
- [ ] The spec covers email normalization, reactivate, admin-only gating, and the three self-target refusals
- [ ] Manual evidence records observed outcomes, not expectations
- [ ] No implementation file other than the single reconciliation import was edited
- [ ] No files outside `files_modified` were touched
- [ ] Every planned task is completed, blocked with evidence, or escalated; no planned work is self-deferred.
</verification>

<success_criteria>
- The phase's load-bearing behavioural claims are executable and passing, not merely asserted in plan frontmatter
- The merged tree type-checks, lints, and passes the full E2E suite
- A reviewer asking "did Phase 7 work?" has evidence to look at
</success_criteria>

<output>
Write `07-07-SUMMARY.md` recording: the integration gate outputs verbatim, before and after; the spec's coverage mapped one-to-one onto Phase 7's success criteria, naming which criteria are covered by manual evidence instead; the last-active-admin and concurrency outcomes and how each was manually verified; manual evidence with observed results; the password-policy reconciliation; and any findings referred back to their owning plan.
</output>
