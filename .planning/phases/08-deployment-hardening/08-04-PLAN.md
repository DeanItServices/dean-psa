---
phase: 08-deployment-hardening
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/proxy.ts
  - DEPLOYMENT.md
files_forbidden:
  - docker-compose.yml
  - Caddyfile
  - .env.example
  - src/auth.config.ts
  - src/lib/**
  - src/app/**
  - prisma/**
  - scripts/**
  - e2e/**
  - package.json
sequential_files: []
expected_artifacts:
  - path: "src/proxy.ts"
    provides: "getClientIp() trust-boundary comment stating the boundary is now enforced by Caddy"
    required: true
  - path: "DEPLOYMENT.md"
    provides: "TLS topology, retracted onboarding precondition, ALTER USER procedure, exec-based migrations, Intuit re-registration"
    required: true
autonomous: false
agents: ["engineering-infrastructure-devops", "engineering-security-engineer"]
requirements: ["getClientIp() is trustworthy because Caddy overwrites X-Forwarded-For — the trust-boundary warning in the file is updated to say the boundary is now enforced", "DEPLOYMENT.md and .env.example reflect the new topology, including that POSTGRES_PASSWORD applies only at initdb so an existing volume needs ALTER USER"]
user_setup:
  - "08-02 and 08-03 have landed and their SUMMARY files exist"
verification_commands:
  - "grep -qi 'caddy' src/proxy.ts"
  - "grep -qi 'ALTER USER' DEPLOYMENT.md"
  - "grep -qi 'bootstrap:admin' DEPLOYMENT.md"
  - "npx tsc --noEmit"
  - "npm run lint"
---

<objective>
Make the security commentary true and the runbook match reality. The `getClientIp()`
trust-boundary warning currently says the rate limiter offers no protection — correct once,
false now that Caddy fronts the app. `DEPLOYMENT.md` still carries Phase 7's standing
instruction not to onboard anyone until TLS lands; this phase is what lands it.
</objective>

<context>
- `.planning/phases/08-deployment-hardening/08-CONTEXT.md`
- `.planning/specs/08-deployment-hardening-spec.md` — "Compatibility Constraints" and "Failure Modes"
- `08-02-SUMMARY.md` (env-read limits, for documenting the overrides)
- `08-03-SUMMARY.md` — **read the verified `X-Forwarded-For` finding before writing the
  comment.** If 08-03 found Caddy appends rather than overwrites, the comment must say what
  is actually true, not what this plan's title assumes.
- `DEPLOYMENT.md` carries `<!-- Phase 8: revisit for Caddy/TLS topology -->` at line 143,
  left by Phase 6 for exactly this work. Lines 25-40 (network/firewall), 143-149 (the TLS
  precondition), 129 (host-side migrations), 272 (DB_PORT convention) all become wrong.
- Codebase map chunk `mw-002` is the comment being rewritten.
</context>

<execution_contract>
Harness: read-before-write -> evidence-before-action -> minimal diff -> verify-before-report
Role: engineering-infrastructure-devops (runbook, topology) + engineering-security-engineer
(the trust-boundary claim is a security assertion and must be exactly as strong as the
evidence supports)
Task: Update the `getClientIp()` trust-boundary comment to reflect the enforced boundary, and
rewrite `DEPLOYMENT.md` for the Caddy topology.

Scope:
- Read targets: `08-03-SUMMARY.md` (the header finding — read FIRST), `08-02-SUMMARY.md`,
  `src/proxy.ts` (the `getClientIp()` block and its warning), `DEPLOYMENT.md` (full, 282),
  `Caddyfile` (READ ONLY), `docker-compose.yml` (READ ONLY), `.env.example` (READ ONLY)
- Write targets: `src/proxy.ts` (comments only), `DEPLOYMENT.md`
- Forbidden targets: `Caddyfile`, `docker-compose.yml`, `.env.example` — 08-03 owns them, and
  they are already correct. Do not "fix" them here.

Allowed tools/actions:
- Read, Edit on the write targets
- Bash: `npx tsc --noEmit`, `npm run lint`, `grep`, and `docker compose exec` experiments if a
  stack is available

Forbidden actions:
- **Do not change any logic in `src/proxy.ts`.** This plan edits comments only. The diff for
  that file must contain no executable change.
- Do not overstate the trust boundary. If 08-03's finding is that Caddy overwrites the header,
  say the boundary is enforced. If the finding is weaker or conditional, say exactly that
  instead. An over-strong security comment is worse than the old warning, because the old one
  at least made readers cautious.
- Do not delete the history of why the warning existed. Amend it; a reader should still learn
  what the exposure was and what closed it.
- Do not claim an end-to-end verification you did not run.
- Do not edit the files 08-03 owns.
- Do not self-defer planned work.

Implementation sequence:
1. Read `08-03-SUMMARY.md` first, specifically the verified `X-Forwarded-For` finding, then
   `08-02-SUMMARY.md`, then the current `getClientIp()` block in `src/proxy.ts`.
2. Rewrite that comment to match the finding. It should state: what the exposure was (a
   client-settable header with no proxy in front); what closed it (Caddy owning the header —
   with whatever qualification 08-03's evidence requires); and what remains true (the store
   is per-process and in-memory, so this is still a brute-force speed bump, not a distributed
   limiter). Keep it a comment change only — `npx tsc --noEmit` must still pass and `git diff`
   for this file must show no logic change.
3. Rewrite `DEPLOYMENT.md`, removing the `<!-- Phase 8: revisit -->` marker at :143:
   - **Network/firewall (:25-40)**: 80 and 443 are now the reachable ports, published by
     `caddy`. `app` and `db` publish nothing. Revise the rate-limiting paragraph — it
     currently says a reverse proxy is needed for the limiter to mean anything; that proxy now
     exists. Document the `RATE_LIMIT_*` overrides from 08-02 with their defaults.
   - **Retract the onboarding precondition (:143-149)**: replace with a statement that TLS is
     in place and onboarding may proceed, **conditional on** `AUTH_URL` being an `https://`
     URL. Keep the reasoning visible — a reader should understand why it was blocked.
   - **First-time setup / Build and start**: add `SITE_ADDRESS` and `POSTGRES_PASSWORD` with
     the generation command; note that Caddy obtains its certificate on first request via
     HTTP-01 and that inbound :80 must be reachable for the challenge.
   - **Migrations (:129)**: host-side `npm run db:migrate:deploy` no longer works — the `db`
     port is unpublished. Document the `docker compose exec` path as the procedure. Try it if
     a stack is available and record what actually worked, including whether
     `scripts/post-migrate.sh`'s `.env` sourcing behaves under `exec` (spec Open Question 4).
   - **New section — rotating an existing database password**: `POSTGRES_PASSWORD` applies
     only at initdb, so an existing `pgdata` volume keeps the old credential and the app
     silently fails to connect. Give the `ALTER USER postgres WITH PASSWORD '…'` procedure
     inside the running container, then the `.env` update, then the restart.
   - **QuickBooks**: `QBO_REDIRECT_URI` must move to `https://` **and** be updated in the
     Intuit developer app registration. Nothing in this repo can do that; if it is missed the
     callback fails with an opaque Intuit error.
   - **Session logout note**: changing the scheme to `https://` changes the Auth.js cookie
     name to the `__Secure-` prefixed form, so every existing session is logged out once.
     Expected, not a defect — same class of event as Phase 7's `tokenVersion` rollout.
   - **`DB_PORT` (:272)**: the committed compose file no longer publishes the port; the
     variable only matters with a local override.
4. `npx tsc --noEmit`, `npm run lint`, and confirm the `src/proxy.ts` diff is comments only.

Required interfaces/content structure:
- `src/proxy.ts`: comment text only; zero executable lines changed
- `DEPLOYMENT.md`: no `<!-- Phase 8: revisit -->` marker; contains `ALTER USER` and
  `docker compose exec`; no instruction to avoid onboarding staff

Edge/error cases:
- 08-03 found a weaker header guarantee → write the weaker, accurate claim and flag it in the
  summary as a deviation from the success criterion's wording.
- No running stack to test `docker compose exec` migrations → document the intended procedure
  and say explicitly in the summary that it was not executed.
- `post-migrate.sh` cannot source `.env` under `exec` → document the working invocation; do
  not edit the script, which is out of scope.

Verification criteria:
- `src/proxy.ts` mentions Caddy; its diff has no logic change; typecheck and lint pass
- `DEPLOYMENT.md` contains `ALTER USER` and `docker compose exec`, and no longer tells the
  reader not to create accounts
- The `<!-- Phase 8: revisit -->` marker is gone
- The trust-boundary claim matches 08-03's evidence exactly

Final result format:
- Status / files changed / the new `getClientIp()` comment quoted in full / the DEPLOYMENT.md
  sections rewritten, listed / whether the exec-based migration was actually executed or only
  documented / any deviation between 08-03's header finding and P8-5's wording / verification
  outputs / issues
</execution_contract>

<stop_gates>
Emit `BLOCKED` and stop instead of guessing when:
- `08-03-SUMMARY.md` does not exist or records no verified `X-Forwarded-For` finding.
- **The `Caddyfile` does not actually contain the directive that finding identified**
  (`grep -qE 'header_up +X-Forwarded-For' Caddyfile`). The comment must follow the CONFIG,
  not the summary prose — a correct research note plus a missing directive is exactly how a
  false security comment ships. Do not invent the claim.
- 08-03's finding contradicts P8-5 (Caddy does not make the header trustworthy). Report it;
  the success criterion needs revisiting rather than a comment that overstates the guarantee.
- Editing the comment cannot be done without a logic change.
</stop_gates>

<recovery>
After compaction, interruption, or context loss:
1. Re-read this PLAN.md, `08-CONTEXT.md`, `08-03-SUMMARY.md` (header finding), `08-02-SUMMARY.md`.
2. `git status --short` — only `src/proxy.ts` and `DEPLOYMENT.md` may differ.
3. `git diff src/proxy.ts` — confirm comments only; any logic change must be reverted.
4. `grep -n 'Phase 8: revisit\|ALTER USER\|docker compose exec' DEPLOYMENT.md` to see progress.
5. Re-run `npx tsc --noEmit` and `npm run lint`.
</recovery>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the getClientIp() trust-boundary comment to match 08-03's evidence</name>
  <files>src/proxy.ts</files>
  <action>
Read `08-03-SUMMARY.md`'s verified finding on Caddy's `X-Forwarded-For` handling FIRST, then
the current `getClientIp()` warning in `src/proxy.ts`.

Rewrite the comment to say what the evidence supports and no more: what the exposure was (a
client-settable header with no proxy in front, making the limiter a no-op against a
deliberate attacker), what closed it (Caddy owning the header, with any qualification 08-03's
finding requires), and what is still true (per-process in-memory store — a brute-force speed
bump, not a distributed limiter). If 08-03 found the guarantee is weaker or conditional, write
that instead; an over-strong security comment is worse than the warning it replaces, because
the old one at least kept readers cautious.

Comments only. `git diff src/proxy.ts` must show no executable change.

> verification: npx tsc --noEmit
> verification: grep -qi 'caddy' src/proxy.ts
  </action>
  <verify>
grep -qi 'caddy' src/proxy.ts
grep -qE 'header_up +X-Forwarded-For' Caddyfile
npx tsc --noEmit
npm run lint
  </verify>
  <done>The trust-boundary comment states the boundary as exactly as strong as 08-03's evidence supports, retains why the warning existed, and introduces no logic change.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite DEPLOYMENT.md for the Caddy topology and retract the TLS precondition</name>
  <files>DEPLOYMENT.md</files>
  <action>
Rewrite the sections the new topology invalidates, removing the `<!-- Phase 8: revisit for
Caddy/TLS topology -->` marker at :143:

- Network/firewall (:25-40): 80/443 published by `caddy`; `app` and `db` publish nothing;
  revise the rate-limiting paragraph, which currently says a reverse proxy would be needed —
  it now exists. Document the `RATE_LIMIT_*` overrides and their defaults.
- The onboarding precondition (:143-149): replace with a statement that TLS is in place and
  onboarding may proceed, conditional on `AUTH_URL` being `https://`. Keep the reasoning
  visible rather than deleting the history.
- Setup: `SITE_ADDRESS`, `POSTGRES_PASSWORD` and its generation command; HTTP-01 on first
  request; inbound :80 required for the challenge.
- QuickBooks: `QBO_REDIRECT_URI` moves to `https://` AND must be re-registered at Intuit.
- Sessions: the `https://` switch changes the Auth.js cookie to the `__Secure-` form, logging
  every existing session out once. Expected, same class as Phase 7's `tokenVersion` rollout.
- `DB_PORT` (:272): still meaningful — `db` now binds to `127.0.0.1:${DB_PORT}`, so the
  per-worktree convention keeps working and host tooling keeps working. Say that Postgres is
  no longer reachable off-host.
- **`:274` "Rate-limit threshold tuning"** — currently says the thresholds are "hardcoded in
  `src/middleware.ts`, not environment-configurable… there is no runtime env var for this
  today" and tells the operator to rebuild the image. After this phase that is wrong twice
  over and names a deleted file, while the section at `:36` says the opposite. Replace it
  with the `RATE_LIMIT_*` procedure (`docker compose up -d app`, no rebuild).
- **`:36`** likewise names `src/middleware.ts` and says a reverse proxy would be needed for
  the limiter to mean anything. Update both halves.
- Sweep `grep -n 'src/middleware.ts' DEPLOYMENT.md` and leave no hits.

> verification: ! grep -q 'Phase 8: revisit' DEPLOYMENT.md
  </action>
  <verify>
! grep -q 'Phase 8: revisit' DEPLOYMENT.md
! grep -q 'src/middleware.ts' DEPLOYMENT.md
! grep -q 'no runtime env var for this today' DEPLOYMENT.md
grep -qi 'caddy' DEPLOYMENT.md
grep -q '443' DEPLOYMENT.md
grep -q 'RATE_LIMIT_AUTH' DEPLOYMENT.md
! grep -q 'do not create accounts for the team' DEPLOYMENT.md
  </verify>
  <done>DEPLOYMENT.md describes the Caddy topology, retracts the onboarding block conditional on https AUTH_URL, and documents the Intuit re-registration and one-time session logout.</done>
</task>

<task type="auto">
  <name>Task 3: Document password rotation and exec-based migrations</name>
  <files>DEPLOYMENT.md</files>
  <action>
Add a section on rotating the database password on an existing deployment.
`POSTGRES_PASSWORD` applies only at initdb, so an existing `pgdata` volume keeps the old
credential and the app simply fails to connect with no obvious cause. Give the procedure:
`ALTER USER postgres WITH PASSWORD '…'` inside the running container, then update `.env`,
then restart the dependent services.

Then confirm the migration instructions at `:129` still hold. **They do** — `db` now binds to
`127.0.0.1:${DB_PORT}`, so host-side `npm run db:migrate:deploy`, `db:seed`,
`bootstrap:admin` (`:163-190`) and the E2E suite all keep working. Update the wording to say
Postgres is reachable from the host only, and add a note that `docker compose exec app npm
run db:migrate:deploy` does NOT work as an alternative: the runner image is
`node:20.20-alpine` with no `bash` and no `psql`, and `scripts/post-migrate.sh` needs both.
Recording that saves the next operator the discovery.

Do not edit `post-migrate.sh` or the `Dockerfile`; both are out of scope.

Finally, if a stack can be brought up, run one end-to-end check and record the result:
`docker compose up -d`, `curl -I http://localhost/` (expect a redirect to https), log in once
and **observe the session cookie name**. `__Secure-authjs.session-token` confirms `AUTH_URL`
is https and the Secure cookie is in force; a bare `authjs.session-token` means the
downgrade this phase exists to prevent. If no stack is available, say so plainly — do not
report an end-to-end result you did not observe.

> verification: grep -qi 'ALTER USER' DEPLOYMENT.md
  </action>
  <verify>
grep -qi 'ALTER USER' DEPLOYMENT.md
grep -qi 'bootstrap:admin' DEPLOYMENT.md
git status --porcelain | awk '{print $2}' | grep -qvE '^(src/proxy\.ts|DEPLOYMENT\.md)$' && { echo "SCOPE VIOLATION"; exit 1; } || echo "scope ok"
  </verify>
  <done>The initdb-only caveat and its ALTER USER remedy are documented, and migrations are described via docker compose exec with execution status stated honestly.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit
npm run lint
grep -qi 'caddy' src/proxy.ts
grep -qi 'ALTER USER' DEPLOYMENT.md
! grep -q 'Phase 8: revisit' DEPLOYMENT.md
! grep -q 'src/middleware.ts' DEPLOYMENT.md
grep -qE 'header_up +X-Forwarded-For' Caddyfile
git status --porcelain | awk '{print $2}' | grep -qvE '^(src/proxy\.ts|DEPLOYMENT\.md)$' && { echo "SCOPE VIOLATION"; exit 1; } || echo "scope ok"
</verification>

<success_criteria>
- `getClientIp()`'s comment states the boundary as exactly as strong as 08-03's evidence, no more
- The `src/proxy.ts` diff contains no executable change
- `DEPLOYMENT.md` documents the Caddy topology, 80/443, and unpublished app/db ports
- The onboarding precondition is retracted, conditional on `https://` `AUTH_URL`
- `ALTER USER` rotation and `docker compose exec` migrations are documented
- Intuit re-registration and the one-time session logout are called out
- `npx tsc --noEmit` and `npm run lint` exit 0
</success_criteria>

<output>
Write `.planning/phases/08-deployment-hardening/08-04-SUMMARY.md` with: status, files changed,
the new `getClientIp()` comment quoted in full, the DEPLOYMENT.md sections rewritten, **the cookie
name observed after first login** (`__Secure-authjs.session-token` / `authjs.session-token` /
not executed), whether the end-to-end check ran, any deviation between 08-03's header finding
and P8-5's wording, verification output, and issues.
</output>
